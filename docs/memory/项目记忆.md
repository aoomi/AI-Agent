# AI Agent 项目记忆

最后更新：2026-08-11

- M9.163（人物首格已被M9.166覆盖）：六格、Blender七角度、32B/72B、H3→Qwen‑Edit、版本与LoRA裁决继续有效；当前人物首格与TripoSR输入为左45°全身。

- M9.160（最终稽查通过，已关闭）：基础设施注册表以`extension_point + provider_id`保存多个已安装提供方，每点只有一个active绑定；支持activate回滚、按provider启停/卸载。无效replace写入前拒绝，并发无空绑定；软件108项及3个子测试与最终稽查通过。
- M9.160边界：内置刷新采用replace_provider，不能删除插件或抢回插件活动绑定；管理接口披露active。有状态且hot_swappable=false的基础设施不得原地替换活实例，生产切换需排空、迁移、重启与恢复演练。
- M9.160测试证据：关联108项及3个子测试、失败替换原子性和8读1写并发动态验证、Python编译通过；正式8个扩展点均唯一active，资源与模型队列空闲。
- M9.159（最终稽查通过，已关闭）：生产能力唯一键升级为`capability + provider_id`，同能力可同时挂本地、远程和插件提供方；选择时过滤disabled/unhealthy，按priority并在同级确定性轮转，也支持显式provider。
- M9.159安全边界：默认提供方失败立即失败，禁止非幂等生成任务静默换模型重复执行；只有显式allow_fallback才尝试后备。内置刷新使用replace_provider，不移除插件绑定；管理接口返回priority/healthy/enabled。
- M9.159测试证据：关联105项及3个子测试、动态多提供方与200次并发轮转、Python编译通过；正式8787健康与管理接口均披露27条完整绑定，Ollama与Comfy队列空闲。

- M9.158（历史首格，已被M9.166覆盖）：六格与Qwen‑Edit人物档案能力继续保留；当前左45°全身为首格及固定种子源。

- M9.157（最终稽查通过，已关闭）：资源调度器支持可插拔资源池、池容量与队列上限。短剧Mac节点将text/audit/image/video/3d/upscale放入accelerator单并发池，audio放入cpu-media双并发池，control为8并发；GPU安全互斥不变，但控制与CPU音频不再被GPU长任务阻塞。
- M9.157运行证据：池内保持优先级、超时与按job取消，满队列立即backpressure；健康快照新增active_items与pools并保留active兼容字段，worker capacity/active改为真实池容量与活动票据数。多实例仍由共享发现、健康路由和owner/generation租约负责。
- M9.157节点回收：WorkerRegistry与WorkloadRouter均按30秒心跳超时清退，服务受控重启后的旧PID投影不会长期污染管理接口或容量统计。

- M9.156（历史四格口径，已被M9.166覆盖）：保留为历史记录，不得作为当前执行依据。
- M9.156半身照：严格正面平视特写型，人物居中、头顶微小留白、腰部裁切、手部完全出画，头部至腰部约占画高75%，双肩不触边、左右留白适中、纯色无杂物；补强脸部/妆发/领口/近景/表情/口型，不作TripoSR几何输入。
- M9.156四图锁定：分镜推理与审核中0°半身为唯一`face_primary`，0°全身为`clothing_body`，90°/180°为`angle_continuity`；四图均为有效约束，人物图禁止标为`audit_only`。
- M9.156历史契约已被M9.166废止：当前人物基准、Klein9B元数据与后端视觉验收统一`left_45_full`。
- M9.156确认：0°人工确认需等ledger确认成功，失败保持待确认且90°/180°不启动；项目切换阻断旧项目晚到确认。
- M9.156角度隔离：90°/180°生成链有独立控制器与project/session围栏，旧项目90°晚到不落盘、不继续180°。
- M9.156 LoRA门禁继续有效，但厚涂加载目标由0°首图改为M9.166左45°首图。
- M9.156恢复边界继续有效；其0°人物建模输入已被M9.166左45°基准覆盖。
- M9.156历史3D门禁已被M9.166覆盖：当前请求标记`left_45_full`，服务端核验项目持久确认记录与精确基准URL。

- M9.155（开发完成，待软件测试）：场景/道具固定角度使用Flux.2 Klein 9B 8-bit，强制CFG 1.5、20步并回写结果元数据；人物多角度继续使用Qwen‑Edit CFG 1.0。

- M9.154（历史口径，已被M9.156覆盖）：曾采用手动上传；其project/session单飞与晚到隔离继续保留。
- M9.154项目隔离：资产提取单飞按项目/session键控，旧项目晚到成功或异常不得覆盖新项目资产、状态、错误或持久数据。
- M9.154手动上传：分镜下一步显示“上传资产图片”；资产页主操作仅提示逐卡导入，旧自动任务恢复时不续跑生图模型。
- M9.153（已完成）：正常合片迁入服务端LangGraph `composition`阶段。前端先同步镜头级视频/音频/字幕证据，再单次提交全剧可合片分集；服务端批量执行并返回待确认成片。单集人工返修保留局部接口。
- M9.153项目隔离：合片同步、提交、响应、替换与持久化均校验项目ID/session/abort，旧项目晚到结果不得写入新项目。
- M9.153批次门禁：composition执行前整批验证集数为正整数且唯一，空批、非法或重复集数均整体拒绝。

- M9.152（最终稽查通过，已关闭）：分镜后的资产提取已从前端旧接口迁入服务端LangGraph `assets`阶段。点击生成图片会先自动确认分镜、持久化并同步台账，再单次调用`runStage(assets)`返回人物/场景/道具/census；前端不再承担该阶段编排。
- M9.152确认门禁：分镜审核、持久化、台账同步或项目上下文任一失败均返回false并阻断资产阶段。
- M9.152资产入口使用single-flight Promise，重复点击共用同一事务，不会重复提交`runStage(assets)`。

- M9.151（最终稽查通过，已关闭）：Klein 9B左45°基准照、TripoSR与Blender静态链之后新增Blender 24FPS/96帧真实源视频。分镜视频同时取得已确认3D源视频和人物正面定妆照时进入本地MiniMax H3 Ref2VA；`<Video 1>`只锁几何/运镜/遮挡/时序，`<Picture 1>`只锁2D身份，缺项保留Wan2.2。H3 prompt ID、心跳、超时、停止、恢复、关闭与显存释放已接入现有视频任务生命周期。生产门禁以故事弧批次/分集等权威范围确认推进，台词行和镜头仅作追踪明细；当前项目剧本确认已同步至LangGraph并开放分镜。
- M9.151真实3D证据：白瓷仙壶Blender worker完成1024×576、24FPS、96帧、4秒源视频，结果经确认进入hot归档；服务重启后任务、确认和视频规格保持，媒体接口HTTP 200。
- M9.151分镜生产修复：禁止Qwen3-VL-32B一次重写全集长JSON；模型仅输出512-token视觉导演方案，服务端按已确认剧本时间段确定性编译并硬校验。正式实测20镜、0—60秒、HTTP 200且模型释放；故事事实库把同集多镜归并为一个分集事实后校验，结果可持久化为待确认。错误展示优先使用服务端具体消息。
- M9.151稽查整改：新3D任务结果强制携带worker报告的`source_video_spec`；分镜遗漏任意一条剧本台词或旁白即硬失败，不再容许比例性漏词。
- 分镜对白校验锁定“说话人+正文”，禁止不同角色用相同正文冒充覆盖。

- M9.150（历史审核暂停口径，已被M9.163覆盖）：当前唯一执行口径为`qwen3-vl:32b`生成、`qwen2.5:72b`固定执行初审、最多一次修正和终审。

- M9.149（最终稽查残留整改完成，待复测）：LangGraph为唯一服务端内核；公开确认先图校验再写台账并支持补偿回滚，旧JSON checkpoint仅作图状态投影。兼容流水线取消先写LangGraph，服务重启后保持`cancelled`，并禁止approve、resume、begin和execute继续原流程。24处重负载入口在执行前和结果提交前双重校验owner/generation；五个前端入口只提交一次阶段命令，服务端接管拆批、有限重试、终审、修复与媒体包。共享发现、远端转发、25项能力、8项基础设施和三元媒体包门禁保持有效。规范见`docs/specs/短剧统一生产内核架构规范.md`。

- M9.147：对话框图片上传已接入结构化提示词反推；RealESRGAN x4图片超分、RealESRGAN x4+RIFE 4.9视频增强、MuseTalk口型同步、PuLID-FLUX FaceNet身份修正及ReActor最终修正均已接入。增强产物使用独立路径和版本，不覆盖原始资产；MuseTalk主链失败时才回退LatentSync。真实图片、视频、口型与两种身份修正烟测及26项回归测试通过。

- M9.146：`短剧3D资产生产规范.md`升级为3.0。核心人物3D脸永不进入成片，人物头部以蒙版交给2D身份链；多人镜头按摄影机深度从远到近逐层生成；TTS与动作偏差超过0.5秒退回分镜；3D空间硬错误优先修正2D，无法归类时暂停；系统级三次失败只进入人工降级审核；共享资产采用版本快照，资产ID冲突禁止静默覆盖；三机位以上锁定gaze_target。

- M9.145（历史角度口径，已被M9.163覆盖）：当时角度与Blender五角度链仅作历史记录；当前人物六格、Blender七角度及H3→Qwen‑Edit串行职责统一以M9.163为准。

## 当前主线

- 项目：可插拔、可配置、可对话操作的 AI Agent 平台。
- 当前目标：M8.10 通用对话智能体闭环增强；M11 已按用户指令暂停。
- 任务来源：根目录 `Dev_MainDev.md`。
- 长期愿景：`docs/product/VISION.md`。

## 已完成

- M9.143：本机 ComfyUI 0.31 自带的官方“一键多角度人物”模板已核对并接入。新增 Apache-2.0 的 Qwen Image Edit 2511 Multiple-Angles LoRA（295,140,688字节，SHA-256 `42426ded...6765`）与 Lightning 4步 LoRA（849,608,296字节，SHA-256 `22226e8d...904f`）；两者底模均为Qwen Image Edit 2511。苏清璃双参考右90度实图在BF16、两LoRA各1.0、4步、CFG1、Euler/simple、denoise1.0下成功生成严格纯侧面，头顶、全身与双鞋完整入框；40步质量档30分钟超时并已精确回收。正式代码候选已切为Qwen双参考独立角度工作流，旧RealVisXL+FaceID+Clothing+OpenPose+Depth叠加链路保留为隔离诊断能力；正式服务尚未重启，等待独立软件测试与稽查。

- M9.142（历史人物角度链，已被M9.163覆盖）：上述RealVisXL/IP-Adapter/OpenPose/Depth组合仅保留为历史验证记录；当前固定角度唯一执行链为Qwen-Edit多参考六格。

- M9.141：故事大纲、全剧剧本和全剧分镜脚本标题右侧已统一增加“复制全部剧集”和“引用全部剧集”图标；复制内容按剧集编号顺序包含剧名、全部集标题及全部正文，引用将同一完整文本逐行写入项目对话输入框。剪贴板写入补充浏览器兼容降级。真实网页已验证剧本完整复制1452字、首尾段落齐全、引用完整回填及图标对齐；Vue类型检查和Vite正式构建通过。

- M9.141：短剧大纲生成已补齐跨集状态闭环：分集前建立剧情阶段与不可逆变化状态表，标题、核心事件和人物姓名全剧唯一；后续批次携带全部既有分集，禁止阶段倒流、冲突重演和反派手段循环；后端直接拒绝分集数量/编号异常、重复标题及重复梗概。

- M9.141（历史文本路由，已被M9.163覆盖）：曾使用9B/27B/122B三档Qwen3.5；当前唯一口径为qwen3-vl:32b生成、qwen2.5:72b初审→最多一次修正→终审。剧情连续性、语义去重与结构化门禁继续保留。
- M9.142：官方 `stabilityai/TripoSR` 已下载至 `models/3d/TripoSR/`。`model.ckpt`大小1,677,246,742字节，SHA-256为`429e2c6b22a0923967459de24d67f05962b235f79cde6b032aa7ed2ffcd970ee`，与官方LFS记录一致；MIT许可证、来源清单和三维模型目录ADR已落盘，权重549个张量通过`weights_only`安全元数据加载。当前仅完成下载与校验，尚未接入网页三视图生产链路。
- M9.99：短剧运行数据目录已从迁移前项目 `Drama_Pipeline_ceshi/output` 改为当前项目根目录 `AI Agent/output`，避免旧目录被清理后项目、流程状态和生成按钮失效。
- M9.98（历史人物角度链，已被M9.163覆盖）：曾启用IP-Adapter锁脸锁服装及OpenPose；当前固定角度唯一执行链为Qwen-Edit多参考六格。
- M9.97：生图重负载任务已使用唯一任务ID并持久化排队、PID、进程组、心跳、超时和重试次数；超时及停止会终止完整进程树，普通失败最多重试一次，新任务启动与服务重启会回收旧状态和残留进程，独立看门狗持续检测任务/进程双向不一致。当前“符咒”道具图已解除旧人物角度任务占用并生成完成。
- M9.96：国风浅涂人物 LoRA 库已新增男性清逸出尘、温润清雅、浅染古风、漫光仙侠、淡墨清雅、雾感仙姿及女性清柔仙韵、浅涂淡颜、雾感温婉、清雅灵秀、柔光稚态、淡彩仙姿；保留男女空灵仙气、仙侠幻想，其他34个旧人物 LoRA 已移入废纸篓，正式目录16个文件与商用索引、大小和 SHA-256 全部一致。
- M9.95：人物、道具和场景资产统一为每个资产一张横向卡片；卡片内固定按基准照、后续角度照顺序横向排列并显示角度名称，窄屏保持横向滚动；导入和重新生成严格作用于当前图片槽位。
- M9.92：新建项目和项目设置已恢复“视觉风格”选择；项目 `category`、`style` 与生图风格统一使用当前所选本地 LoRA 风格，移除隐藏的“现代真人电影质感”默认覆盖。

- M9.48：全部短剧生产流程已统一为增量展示；大纲和剧本按集显示，剧本按段显示，分镜脚本按镜头显示，资产图、分镜图、分镜视频和成片按单项完成立即持久化并显示；生成期间自动展示全部已完成内容。
- M9.49：分镜脚本生成已从整集阻塞返回改为逐镜头真实调用；每个镜头生成成功后立即写入项目阶段并显示，接口已使用本地 Qwen3 8B 实测返回有效镜头。
- M9.50：资产生图任务存储已兼容旧版键值对数组并自动归一化，修复 FLUX 批量任务的列表索引错误；基础图生成顺序统一为男主角、女主角、配角、道具、场景，角色提取新增角色类型与性别字段。
- M9.51：资产页已增加每2秒实时结果回填；图片文件生成完成后会自动从任务记录回填项目资产地址并立即显示，不再等待整批任务完成或手动刷新。
- M9.52：资产页门禁改为依据实际完整分镜数据判定，修复分镜已完成仍提示“请先生成分镜脚本”；资产生成期间不再显示历史失败提示，批次结束后仅显示真实未完成项。
- M9.53：生图权威规范新增人物默认国籍与面孔规则；项目未明确指定其他国家、地区或族裔时，基准图、分镜图和视频首帧统一使用中国人身份、自然东亚面孔和中国影视角色审美，生图接口同步强制注入该规则。
- M9.54（历史三槽口径，已被M9.163覆盖）：人物基准确认按钮继续保留；确认后的当前固定角度链为Qwen-Edit多参考六格并逐槽确认。
- M9.55：“就要这张”已并入统一媒体操作组件，按钮使用与图内操作一致的深色半透明样式，白色遮罩已移除。
- M9.56：资产轮播已分离点击预览与拖动手势；轮播仅在移动超过阈值时滚动，静止点击图片直接打开统一放大预览，图内操作按钮不再触发轮播抢占；真实界面点击验证通过。

- M1：Skill 注册、独立智能体、1:1 映射、生命周期、上下文隔离、HTTP 与串联恢复。
- M2：短剧 11 节点、真实能力提供方、持久检查点、人工关口、取消、恢复和导出。
- M3：隔离任务查询、详情、取消、恢复、事件投影与前端任务中心。
- 平台基线：Python 安装入口、健康检查、身份上下文、事件总线、任务队列和插件生命周期。
- 契约基线：身份、授权、项目、任务、插件、资产、错误、事件和短剧流程契约。
- UI：原 Drama Pipeline 前端迁入 `plugins/builtin/short_drama/frontend/` 并通过平台插槽加载。
- M4.1：开发/稽查角色、模型、配置版本、对话消息和提案契约。
- M4.2：`system_main_developer` 与 `system_inspector` 内置 Skill；稽查 Skill 无写权限。
- M4.3：模型注册表、启停、能力/上下文约束、确定性选择和显式失败。
- M4.4：智能体配置创建、乐观版本更新、历史追溯、Skill 权限边界和模型能力绑定。
- M4.5：真实模型客户端驱动的对话、结构化配置/复杂任务提案、显式确认/拒绝、配置版本应用、任务执行入口及稽查只读约束。
- M4.6：模型注册/查询、智能体配置创建/更新/历史、对话消息与提案确认/拒绝 HTTP 路径及 OpenAPI 登记。
- M4.7：开发/稽查 Skill 选择、模型注册/切换、配置版本、对话和提案确认/拒绝前端界面。
- M4.8：角色隔离、模型切换、配置版本、复杂任务确认执行及无真实模型显式失败集成测试。
- M4 最终验证：相关功能验收通过；当前全项目累计 131 项 Python 测试通过。
- M5.1：模型与媒体提供方统一配置、密钥引用、限流配置和健康状态契约。
- M5.2—M5.8：OpenAI 兼容客户端、取消/超时/结构化校验、四类提供方注册、限流重试熔断降级、用量成本产物审计、短剧真实提供方绑定、HTTP/前端和生产集成测试。
- M5 最终验证：151 项 Python 测试、提供方 TypeScript 契约编译、Vue 类型检查和 Vite 正式构建通过。
- M6.1—M6.8：插件签名/SBOM 契约、安装前校验、文件/进程/网络/数据隔离、工具授权与脱敏、哈希链审计、供应链门禁和插件管理界面。
- M6 最终验证：163 项 Python 测试、SBOM 门禁、TypeScript 契约编译、Vue 类型检查和 Vite 正式构建通过。
- M7.1—M7.8：协作契约、只读稽查、整改回流、隔离、LangGraph 串并行/重试/暂停恢复/人工接管、HTTP、前端和闭环测试。
- M7 最终验证：166 项 Python 测试、SBOM 门禁、Vue 类型检查和 Vite 正式构建通过。
- M8.1—M8.10：11 节点/LangGraph 检查点契约、真实前期链路、媒体版本与局部重做、视频双模式、音画时间线、审核修复确认导出、工作台、持久恢复测试、中文 LoRA 项目锁定和上下文驱动通用对话闭环。
- M8 最终验证：199 项 Python 测试、SBOM 门禁、Python 编译、Vue 类型检查和 Vite 正式构建通过。
- M9.1—M9.8：行业/流程/Skill/机器人契约、通用 manifest 注册、租户项目隔离机器人、独立模型版本、LangGraph 拓扑、对话式配置、通用界面、短剧首模板和第二行业扩展验证。
- M9 最终验证：184 项 Python 测试、SBOM 门禁、Vue 类型检查和 Vite 正式构建通过。
- M10.1—M10.8：三环境模板、容器/健康/安装、持久化适配、日志指标链路告警、备份恢复、升级回滚、故障容量测试和运维文档。
- M10 最终验证：196 项 Python 测试、SBOM 门禁、Python 编译、Vue 类型检查和 Vite 正式构建通过。
- M9 产品范围已确认：全行业一个业务流程对应一个独立 Skill 机器人，每个机器人可独立设置模型；短剧作为首个行业模板。
- M8.9 产品范围已确认：新建项目和项目设置提供中文 LoRA 风格设置；默认自动确定性匹配并锁定，支持固定选择、手动覆盖和主动探索模式。
- 平台界面自适应修复：任务中心、能力提供方和插件管理已改为独立浮层；主工作区恢复全视口显示，并完成 760px、1280px、1920px 宽度验证。
- 本地开发 API 路由已分流：平台 `/api/v1` 接入 8080，短剧 `/api` 接入 8787；“新项目”创建、界面写回及清理接口均返回 HTTP 200。
- “生成图片/继续生成图片”已修复为立即进入“分镜画面”阶段；前置确认不足时在目标阶段明确显示门禁信息，满足条件后直接启动镜头图生成。
- M8.10：机器人通用对话闭环已完成，支持 Skill/项目/任务上下文、持久化长期偏好记忆、必要澄清控制、Skill 选择校验、结构化计划和确认式变更；199 项 Python 测试、供应链门禁、Python 编译、Vue 类型检查及 Vite 构建通过。
- 图片生成门禁已修复：分镜未确认时停留在分镜步骤并显示复检错误；资产未确认时自动进入资产步骤；错误详情恢复可见，禁止提前提交镜头图任务。
- M9.9：短剧 Skill 已按六类机器人接通前后端；支持一键启停、项目隔离机器人、自动模型策略、持久化恢复和执行门禁；当前项目六类机器人已全部启用并通过真实界面验证。
- M9.10：每个短剧 Skill 机器人卡片已增加“模型与 API”入口；支持云端提供方、密钥引用、模型注册、自动选择及独立模型绑定，并通过真实界面和接口验证。
- M9.11：Skill 模型配置已增加本地模型入口，支持 Ollama、LM Studio、LocalAI 和其他 OpenAI 兼容服务；本地 HTTP 仅允许 `localhost`/回环地址。
- M9.12：审核机器人界面已精简为省成本、均衡、高质量三档方案；审核页直接提供初审/终审切换，文本、视觉、音频可分别选择不同模型，不再显示通用“使用模型”。
- 短剧 Skill 界面名称已统一移除“机器人”字样，历史项目配置加载时自动兼容旧名称。
- “审核”开关已统一接管文本、图片、视频和音频自动审核；关闭后中止进行中的文本审核并直接进入人工确认，不再触发初审、终审或自动修复。
- 剧本完成后的“生成剧本”按钮已替换为“生成分镜”，点击后确认当前剧本并直接进入分镜生成。
- FLUX.2 MLX 启动入口已修复受限工作目录导致的导入失败；本地 256×256 真实图片生成通过，前端错误信息不再泄露原始 Python 堆栈。
- 短剧生产永久取消自动审核与自动推进；大纲、剧本、分镜、资产和镜头均由用户逐步人工确认，只有当前节点人工确认完成后才显示并允许手动启动下一节点。
- 资产页已支持从本地生成任务结果自动恢复遗漏的图片状态，并为生成中/待生成卡片提供稳定占位布局；当前项目 9 张资产图已全部回填为待人工确认。
- M9.13：模型配置已重构为两层选择；第一层选择当前机器人、初审或终审，第二层选择本地或云端，模型均使用预设下拉框，仅服务地址手动填写；三类绑定按项目独立保存。
- M9.14：进入机器人模型配置后 Skill 分类左栏自动收起为返回栏；返回按钮已移入左栏，返回机器人列表后左栏自动恢复。
- M9.15：本地模型和云端模型已改为互斥二选一；切换来源会取消当前角色的旧来源绑定，每个主机器人、初审或终审最终只保存一个模型。
- M9.16：本地与云端模型的独立接入按钮已移除；统一使用“保存当前配置”自动完成模型注册和机器人角色绑定。
- M9.17：已发现并注册本机 Ollama 的 Qwen2.5 72B、Qwen3 32B、DeepSeek R1 70B、LLaVA 7B；六类机器人及各自初审、终审已写入项目级默认本地模型绑定。
- M9.18：主模型、初审和终审已使用独立模型草稿及持久化键；切换角色不会继承上一角色的下拉值，保存后分别恢复各自模型。
- M9.19：Skill 列表、类型切换、模型来源和保存按钮已统一为白底、灰色描边、深色文字样式，选中状态使用浅灰底区分。
- M9.20：模型保存已修复；项目绑定先持久化，平台重启后自动重注册所选模型，作用域行业机器人可被配置服务解析，保存结果已通过项目阶段和机器人配置 API 双重验证。
- M9.21：模型配置已增加文本、视觉、音频、视频类别；项目策划和编剧默认筛选文本模型，视觉资产和视频制作默认筛选视觉模型，未检测到的音频/视频本地模型明确标记不可用。
- M9.22：本地视觉模型目录已补充 LLaVA、Qwen2.5-VL、Gemma 3 Vision、MiniCPM-V，并区分已安装/未安装；Skill 启用状态使用浅绿色，停用状态使用灰色。
- M9.23：Mac 快速跑通配置已完成；Qwen3 8B 已安装并验证响应，CosyVoice2 0.5B 与 Wan2.1 T2V 1.3B 已下载，项目策划、编剧、后期制作使用 Qwen3 8B，视觉资产与视频制作使用 LLaVA，审核机器人及全部初审/终审绑定已关闭。
- M9.24：Skill 卡片的“模型与 API”入口及其配置子界面已移除，界面仅保留流程启停，现有项目模型由系统自动配置。
- M9.25：人物、场景、道具、细节图和分镜图片已接入统一点击放大预览，支持键盘回车打开、缩放和前后切换。
- M9.26：短剧 8787 持久化兼容服务已恢复；历史项目、剧本、分镜、资产阶段与结果媒体重新接入，当前“都市女王的逆袭”项目和 9 张历史图片已验证可读取。
- M9.27：故事总纲与分集梗概生成接口已恢复，Qwen3 8B 结构化输出已通过真实接口测试，页面不再返回 `not_found`。
- M9.28：项目对话、历史记录、草稿、会话及能力查询接口已恢复；历史消息可读取，Qwen3 8B 对话响应已通过真实接口测试。
- M9.29：剧本单集与全剧分镜兼容接口已恢复，Qwen3 8B 真实剧本和连续时间轴分镜生成通过；页面不再显示 `not_found`。
- M9.30：资产提取、FLUX.2 MLX 人物/场景/道具与分镜图片生成接口已恢复；资源保存、项目版本和人工生产确认接口已补齐。
- 资产全部人工确认后，资产页右上角显示“生成分镜”按钮，由用户手动启动分镜画面生成，不自动推进。
- 资产历史任务的 `not_found` 失败状态已自动校正：已有有效图片时统一恢复为待人工确认并清除错误提示；当前项目资产失败项为 0。
- 资产页已移除人物、道具、场景下拉框，统一改为三个分类按钮；点击分类仅显示对应类别的资产图片。
- 分镜脚本完成后自动提取人物、道具和场景并建立全部资产图片框；空框显示“等待生图”，生成中的图片框显示“正在生图”；资产页顶部状态标题已移除。
- 图片放大预览在普通移动鼠标时保持不动；滚轮缩放时以鼠标所在位置为缩放中心。
- 全部流程顶部已统一为“流程名称　No:当前/总数　Time:耗时”格式；资产分类按钮移入内容区。
- 五个短剧 Skill 固定默认开启；每张卡片右上角开关改为该流程的“是否审核”开关，项目级独立保存，默认关闭。
- 大纲、剧本、分镜脚本、资产、分镜画面、分镜视频、成片和导出均使用项目级独立真实计时；开始生成时从 0 计时，结束后冻结实际耗时，不再共用累计时间。
- 视频运动策略固定为 70% 运镜、30% 人物或主体动作；分镜生成和视频任务提交均携带该比例。
- 历史分镜人物链（已被M9.163覆盖）：IP-Adapter Plus Face SDXL仅保留为历史验证能力；当前分镜人物一致性必须消费已确认Qwen-Edit六格人物档案，不属于单镜LoRA强度微调例外。
- 分镜视频任务增加进程内活动登记和中断恢复：服务重启遗留的 `generating` 状态会自动转为可重试失败，继续生成会重新提交，不再永久卡住。
- M9.29：人物、场景和道具资产卡片已改为双列等宽 9:16 大图布局，图片下方统一显示“导入图片”和“重新生成”，窄屏自动切换单列。
- M9.30：资产图片的“导入图片、重新生成”已移入图片内部底部；左上角新增“图片简介”，点击后在图片内显示名称、简介、状态和确认操作浮层。
- M9.31：资产、分镜画面和分镜视频已取消单张确认；点击下一流程按钮自动确认当前已生成批次，“生成分镜视频”可直接启动视频生产。
- 分镜画面页已新增“生成分镜视频”直达按钮；点击后自动保存已生成镜头、切换分镜视频页并立即启动视频生成，未完成镜头仍保留“继续生成”。
- M9.32：分镜视频接口已恢复并接入本地 Wan2.2；第1集镜头1真实视频已生成并通过 HTTP 200 媒体验证，历史 `not_found` 和卡住状态已清除。
- M9.33：本地 Qwen3-TTS 配音、MuseTalk 主口型、LatentSync 1.6 备用口型、CampPlus 声纹检查、情绪检查、口型完整性检查及音频混合接口已全部接入；真实音频、MuseTalk 视频和 LatentSync 视频均已生成验证。
- M9.34：OpenPose 全身、手部和面部骨骼提取已接入；SDXL OpenPose ControlNet 已与 IP-Adapter 人物身份锁定联合绑定，真实骨骼图和受姿态约束的人物图均已生成验证。
- Skill 左侧“软件开发”分类仅保留“代码稽查、主力开发”两个默认开启 Skill；两者均已保存独立 Skill 清单和系统提示词文件。
- 软件开发双 Skill 已接入协作闭环：主力开发提交交付物，代码稽查只读检查；不通过时自动生成整改任务交回主力开发，整改后可再次提交复验，超过轮次转人工处理。
- ComfyUI 的 79GB 共享模型、输入和输出目录已统一迁移到 `/Users/aoo/AI/ComfyUI-Shared`；短剧服务、ComfyUI 启动器、批处理和预览脚本均已切换新路径，旧根目录别名已移除。
- 任务列表已接通实时项目流程、单镜头任务和视频作业状态；任务窗口每2秒刷新，实时显示排队、运行、完成、暂停与完整失败信息，不再使用旧生产记录覆盖当前状态。
- M9.35：Wan2.2 分镜视频已增加持久化内存等待队列和5秒实时监控；macOS 使用系统内存压力的可回收内存判定，内存不足时保留任务，达到安全阈值后自动串行续跑，服务重启后自动恢复等待任务。
- M9.36：项目对话已接通任务意图识别、主力开发/代码稽查 Skill 自动路由、连续对话上下文、全部项目 Markdown 规范注入、Codex 文件/终端/测试工具执行和真实验证闭环；不再要求用户手动提供项目代码。
- 资产页的“人物、道具、场景”分类按钮已移入顶部 No/Time 同一行最右侧，距右边缘 10px。
- M9.37：系统 AI 对话已改为持久化异步任务，前端每 2 秒查询心跳；单条请求不再阻塞后续消息，31 分钟超时自动释放，服务重启自动恢复排队/运行任务，未回复历史消息自动续办，请求 ID 防止重复执行；“修复/解决/整改/处理”优先路由主力开发。
- M9.38：合成成片前已增加分镜级强制门禁；有对白镜头必须先完成配音、口型同步和字幕文本/时间轴，全部镜头完成后才允许合片；前端按钮与后端合片接口同时校验。
- M9.39：成片阶段已接入项目内自动生成 BGM、对白与 BGM 混音、字幕烧录顺序门禁；默认 BGM 音量为 18%，口型输出强制封装对白音轨，字幕重烧继续保留 BGM 和对白。
- 所有短剧流程操作栏已永久隐藏“未生成、待确认、已生成、未合片、未导出”等静态状态文字，仅保留真实运行中的动态进度提示和错误信息。
- 所有短剧流程操作栏的“导入”按钮已永久移除；单项素材替换能力保持不变。
- 三栏工作区顶部已按实时屏幕宽度自动压缩左右栏，窄屏自动隐藏预览栏；流程标题、No/Time、资产分类和操作按钮支持换行且不再横向溢出。
- 成片合并已改为逐镜头强制生成一致的 H.264/AAC 音视频流：对白镜头使用原始 TTS 音轨，无对白镜头补静音，再串联合片和混入 BGM；口型主流程已由 MuseTalk 切换为 LatentSync 1.6，旧口型产物会在再次生成成片时自动重做。
- “生成成片”已修复旧成片记录导致的重复生成门禁误判；每次点击会清理旧成片状态并重新执行合片，不再显示“配音、口型同步或字幕尚未完成”的错误提示。
- 成片混音已将对白提高 6dB，关闭 FFmpeg 自动归一化衰减，并使用侧链压缩在对白期间自动降低 BGM；BGM 已由固定持续音改为四段和弦循环，默认音量调整为 14%。
- 可覆盖媒体已关闭浏览器缓存并增加文件修改时间版本号；重新生成的同名分镜、口型或成片会立即加载最新文件，不再播放一小时内的旧缓存。
- 资产固定角度生产恢复强制门禁：第一张为定位基准图，仅生成定位基准图后等待逐项确认；确认后才允许引用该图串行生成其余固定角度，禁止未确认基准图提前生成后续角度图。
- M9.57：分镜脚本完成后的下一流程按钮统一为“生成图片”；点击后自动归档并清空当前项目旧人物、道具、场景资产数据，跳转资产界面，重新提取最新分镜资产并立即开始按顺序生成定位基准图。
- M9.58：分镜逐镜头生成已增加剧本段落进度约束、四次差异化重试和重复内容自动推进兜底；当前“逆光而行”第1集已在真实界面续生成完成15个连续镜头，状态恢复为完成并显示“生成图片”，不再因第11镜头重复而中断。
- M9.59：资产基准图任务已增加每次生成独立随机标识，历史同名任务不再回填到新批次；当前“逆光而行”点击“生成图片”后已真实验证旧资产清空、自动跳转资产页、重新提取14项资产并从0开始生成首张新定位基准图。
- M9.60：人物正面近景基准光照规范已统一为正前方柔和漫射平光、面部均匀通透、下颌颈侧浅阴影、服装保留层次、纯色哑光中性灰背景，并禁止硬阴影、侧逆光、强对比、光斑和渐变。
- M9.61：人物正面近景基准面部优化统一为弱化额头高光与下颌阴影、统一瑕疵、补充柔和眼神光、校正眉眼对称并保持面部亮度均衡。
- M9.62：大纲强制增加“核心人物简介”模块并固定显示在剧名下方、故事总纲与分集剧情之前；每人统一为“姓名｜身份｜性格｜核心动机”，生成结构、运行规范和前端展示已接通。
- M9.63：资产“图片简介”弹层已改为图片内部自适应定位，避免窄栏和首列卡片中向左越界或被滚动容器裁切；真实界面点击、可见性及边界包含验证通过。
- 主线开发永久新增修复回归强制规则：每次修复后必须识别并逐项实测所有可能受影响的关联功能、上下游流程、接口、状态、持久化与界面交互；未完成真实回归验证不得报告已修复。
- M9.64：全流程“生成下一流程”统一为仅清空紧邻下一流程的旧内容、媒体、状态和错误，保留上一流程及其他流程；清空后自动跳转并立即生成。分镜脚本进入资产已真实验证为旧资产即时归零、重新提取17项资产并自动启动首张定位基准图。
- M9.65：分镜脚本完成后自动理解并提取人物、道具、场景，在资产页预建简介和空图片位；点击“生成图片”只清空资产图片、角度图及生成状态，保留本次分镜提取的资产结构与简介并自动生图。
- M9.66：资产生图状态已绑定实际生成图片键；“此图正在生成中”和停止按钮只显示在当前图片卡片，其他角度及其他资产保持等待状态。真实界面已验证仅首张人物基准图显示生成状态，后台生图进程持续运行。
- M9.67：资产批量生图不再自动切换人物、道具、场景分类；后台完成图片只原位更新对应卡片，保持用户当前分类与查看位置。真实验证停留道具页期间人物及道具图片连续完成，页面始终未跳转。
- M9.68：新建项目界面已移除“中文 LoRA 模式”设置，后台继续使用自动题材匹配；人物定位基准照恢复“就要这张”按钮，每张已生成且待确认的基准照可直接确认并生成固定角度图。
- M9.69：大纲人物简介新增层级唯一性规范：全剧仅允许1名第一男主，其余男性必须标注男二号、盟友、反派等定位。
- M9.70：都市逆袭类人物简介强制单列女主；兼任其他职能时使用“女主/职能”双标签，女配不得替代女主。
- M9.71：人物简介规范已合并去重为固定格式、男女主岗位唯一、兼任双标签、正反派褒贬措辞适配和软件测试 Skill 优先验收；大纲模型提示同步执行。
- M9.72：道具和场景资产卡片统一为每行2张，单卡图片占满列宽；窄屏保持单列自适应。
- M9.73（历史人物角度链，已被M9.163覆盖）：曾使用三槽OpenPose/IP-Adapter；当前为Qwen-Edit多参考六格，尺寸与头脚完整等验收规则继续保留。
- M9.74：全部资产定位基准照生成队列已与资产页面从上到下的显示顺序完全统一，移除按主配角和性别重新排序；基准照严格逐张串行生成，上一张结束后才启动下一张，规则已同步写入生图规范。
- M9.75：道具图已强制禁止真人和真实人体，仅允许道具本身为雕像、雕塑、人偶或玩偶；生成后增加本地视觉真人检测，不合格自动重试。资产基准图队列改为首项失败立即暂停，后续保持待生成，界面显示具体资产和真实错误，不再将剩余资产连续标记失败。
- M9.76：资产“生成图片”按钮失效已修复；8787短剧服务恢复持续运行，资产生图任务在提交时立即持久化运行状态并在失败时保存真实错误，页面加载时自动释放服务中断遗留的无效生成状态，按钮不再被残留状态永久锁死。
- M9.77：已建立`models/loras/国风浅涂柔光治愈仙侠淡彩风/`目录，按国风厚涂同结构分为风格LoRA与人物LoRA；下载并登记5个风格、20个女性、20个男性SDXL LoRA，共45个、694MB，许可限定MIT/OpenRAIL++，全部完成Safetensors头、数量、大小、SHA-256和商用授权索引校验。
- M9.78：`国风浅涂柔光治愈仙侠淡彩风`目录新增与人物平级的`灵兽LoRA/`，下载并登记20个传说灵兽、神兽、仙宠和奇幻生物SDXL LoRA，共587MB，许可限定MIT/OpenRAIL++，全部完成Safetensors头、数量、大小、SHA-256和商用授权索引校验。
- M9.73：资产人物预制框强制覆盖大纲核心人物简介全部人物；资产提取请求携带完整人物清单，模型漏项时前端自动补齐并持久化，历史项目加载时同步修复缺失人物框。
- M9.74：补齐现代写实人像 LoRA；现代真人角色自动使用现代写实人像模型，权重0.75，场景和道具继续使用写实基础模型并避免误用人像 LoRA；FLUX.1-dev 开放镜像与 Diffusers MPS 兼容后端已接通，生成结果持续记录实际 LoRA 元数据。
- M9.75：已建立 `models/loras/国风厚涂/` 商用 LoRA 总目录；下分 `风格LoRA` 与 `人物LoRA` 两个子目录，收录高下载量国风厚涂风格5个、男性人物10个、女性人物10个，文件统一使用中文＋拼音＋SDXL命名；25个 safetensors 均完成头部、数量、许可证、来源、大小和 SHA-256 校验，明细写入 `models/loras/商用LoRA索引.json`。
- M9.76：外购 LoRA 工业规范已合并去重并写入 `docs/specs/LORA_ASSET_SPEC.md`，统一风格/角色物理隔离、单项目固定组合、权重区间、商用准入、资产归档、版本锁定、测试、稽查和异常处理规则。
- M9.77：全部生图请求已强制读取 LoRA 主索引和商用索引；实际 LoRA 由索引解析并校验登记、文件路径、SHA-256、触发词和底模，硬编码文件路径已移除，校验失败直接阻止生成。
- M9.78：新建项目“题材类型”已改为实时索引 `models/loras` 顶层风格目录；后端每次请求扫描实际 safetensors，界面打开期间每2秒同步，目录新增或删除后选项自动增减并自动修正失效选择。
- M9.79：“就要这张”点击反馈已改为同步生效；点击后立即关闭简介、隐藏确认按钮、将当前人物切换为生成中、在首张待生成角度图原位显示进度并提示已确认，后台继续执行确认登记和固定角度生产。
- 主力开发与软件测试 Skill 已统一新增根因排查规则：问题处理不得停留在表面故障点，必须追溯根因、扩展检查同类场景及上下游影响，一并整改并完成防复发回归。
- M9.80：人物定位基准照“就要这张”显示条件已改为依据基准图和固定角度的真实状态判定，兼容历史失败与恢复状态；未确认基准照恢复按钮，已进入角度生成或已完成时自动隐藏。
- M9.81：“就要这张”不再受历史固定角度数据影响；所有已有基准图且未完成、未生成中的人物卡片都显示按钮，点击后继续补齐缺失角度图。
- M9.82：固定角度图停止操作已改为精确终止当前角度任务，同步释放生成状态并恢复基准照“就要这张”按钮。
- M9.83：人物定位基准照已增加生成后视觉验收；固定风格 LoRA 继续全剧锁定复用，但纸伞、花木、户外场景、手部、道具、腰腿、非正面或非纯灰背景等违规结果会立即删除并在原槽位串行重试，连续三次不合格时暂停并返回真实验收结果。
- M9.84—M9.85（历史人物角度链，已被M9.163覆盖）：IP-Adapter服装参考仅保留为历史验证能力，不再作为当前固定角度执行链。
- M9.86：已根治剧本固定5秒分段与分镜均分时间轴；每集在60-70秒内依据剧情稳定确定实际总时长，剧本按内容强制分配2-9秒且至少3种段长，分镜继承同一总时长并采用非等长镜头节奏，加载、恢复与完成校验统一使用逐集真实时长。
- M9.87：分镜逐镜头生成已实现可靠断点续传；刷新、连接中断或单镜失败后保留连续有效前缀，从首个缺失镜头继续补齐，生成中半成品不再触发15-23镜头成品门禁，只有时间轴或内容前缀损坏才重建当前集。
- M9.88：所有生产流程标题栏右侧已统一接入集数下拉组件，支持“全剧集/第N集”；选择单集后，大纲、剧本、分镜脚本、分镜画面、分镜视频和成片列表仅显示对应集内容。
- M9.89：现有LoRA与原生MLX矩阵不兼容，LoRA生图保留FLUX.1-dev兼容通道并由20步优化为8步、文本序列限制256；全剧年代画风强制优先锁定LoRA家族，同一资产请求去重，保持928×1664、LoRA权重、串行生成和视觉验收不变。
- M9.83：新增 `docs/specs/styles/国风浅涂视觉生成规范.md`，固化国风浅涂的画风、光影、色彩、肤质、提示词、女主人设、运镜、场景、灵宠、LoRA 公式、锁参、自检和异常处理规则；LoRA 通用规范与短剧生产规范已建立强制索引。
- M9.84（历史三槽口径，已被M9.163覆盖）：原正/侧/背三槽已扩展为Qwen-Edit六格；身份、发型服装、方向、头身比及头脚完整验收继续有效。
- M9.87：分镜脚本“继续生成”已改为保留当前集全部已有镜头，仅按镜头编号补生成缺失镜头；续生成不再先清空未完成集数据。
- M9.88：流程顶部 `Time` 已统一显示该项目当前流程的累计实际运行耗时；继续生成、暂停恢复和再次运行均在历史耗时上累加，不再重新归零。
- M9.89：资产页人物、道具、场景分类按钮已显示各类别需要生成的图片总数，按资产数量乘以该类别固定角度数实时计算；按钮遮罩同步加宽并保持窄屏适配。
- M9.88（历史人物角度链，已被M9.163覆盖）：曾改为FLUX.2 Klein 9B三视图；当前为Qwen-Edit多参考六格。身份验收与禁止宣称像素级锁脸继续有效。
- M9.89：全流程下一阶段门禁已统一改为单集最小完整输入：大纲、剧本、分镜脚本任一完整集即可进入下一阶段；资产按该集分镜实际引用的人物、道具、场景全部有图后即可生成分镜画面；一集全部分镜画面完成即可生成视频，一集视频、配音、口型和字幕齐备即可合片，一集成片即可进入导出。未完成的其他集继续原流程生成，不再阻塞已完整集向后生产。
- M9.90：统一流程操作栏已允许生成任务运行期间同时显示“生成下一流程”按钮；大纲门禁只校验任一集标题与剧情完整，不再被尚未完成的全局字段和全剧生成状态隐藏。实时 Vite 页面已确认按钮条件代码生效。
- M9.91：资产清点已改为严格按当前已完成集数提取，不再混入全剧其他集；人物按本集实际出场名单强制补齐，场景按实体地点归并，道具排除服装、环境与光效。当前项目第一集已修正并通过真实界面验证：人物4个、道具1件、场景2个。
- M9.92：资产页分类角标已由“固定角度图片总数”改为“资产项目数”，当前真实网页显示人物4、道具1、场景2；资产清点增加版本与集数标记，后端拒绝旧页面继续用过期资产清单覆盖新结果。
- M9.93：剧本页已统一使用 WorkflowActionBar 常驻显示“生成分镜脚本”；存在剧本即显示，内容尚未完整时禁用，完整后可直接进入分镜脚本并自动生成。真实网页验证按钮可见且可用。
- M9.94：人物定位基准图改为肩部以上大头像、纯中性灰背景、人脸占画面60%-70%，验收失败自动换种子重试5次；界面不再暴露视觉模型原始 JSON，当前失败项已重置为可继续生成。
- M9.91：资产生图中断假死已修复；后端自动识别服务重启后遗留的无效生成任务，前端轮询回收失败状态并仅自动续跑一次未完成图片；运行中状态与流程累计计时保持同步，不再出现图片卡住且 Time 停止。
- M9.92：人物、场景、道具定位基准照已统一切换为本地 FLUX.2 Klein 9B 量化生成；基准照不再加载不兼容的 FLUX.1/SDXL LoRA，画风由项目锁定提示词控制。真实接口已生成并返回 9B 模型元数据与图片。
- M9.93：资产重复点击冲突状态已修复；同一资产已有真实后台任务时，前端读取后端返回的活动任务编号并恢复对应 nonce、生成中状态和流程计时，不再把“正在生成”误显示为红色失败。
- M9.94：9B 基准照流程已根治重复生成慢与失败循环；FLUX.2 Klein 9B 改为4步推理，移除每张图后串行加载视觉模型及最多5次整图重生成，普通失败不再被当作服务中断自动续跑。928×1664人物基准照真实接口耗时由77秒降至19秒并成功出图，类型检查和正式构建通过。
- M9.95：国风浅涂人物目录新增两个 FLUX.2 Klein 9B 仅测试 LoRA，分别按“男性_清逸出尘”“女性_清柔仙韵”命名；Safetensors、SHA-256、9B 元数据和 224/224 权重键加载通过。两者均按非商用隔离，不进入商用索引；本机加载后推理未产图，暂不接入正式流程。
- M9.96：国风浅涂已补齐男性5个、女性5个、风格5个 FLUX.2 Klein 9B 仅测试 LoRA；15个文件均通过9B元数据和224键校验。基准照正式测试分支已绑定项目固定风格LoRA与按性别/角色固定人物LoRA，真实接口同时加载两个 LoRA 并成功出图；全部保持非商用隔离。
- M9.97：本机文生图底模已按用户要求仅保留 `/Users/aoo/AI/ComfyUI-Shared/models/diffusion_models/flux1-schnell-Q8_0.gguf`；其他文生图底模及缓存共145GB已移入废纸篓隔离目录。图像编辑、ControlNet、IP-Adapter、LoRA、视频、音频、口型和审核模型均未改动。
- M9.97：Klein 9B 双 LoRA 已增加物理权重去重；风格目录与人物目录指向同一来源权重时禁止重复叠加，并从同类别其他人物 LoRA 稳定补选。林婉儿实际任务已加载“橙彩插画”风格与“女性_淡彩仙姿”人物 LoRA，16秒成功生成并返回完整绑定元数据。
- M9.98：已下载 Apache-2.0 的 FLUX.1 Schnell GGUF Q8_0（12,687,821,728字节，SHA-256 `f6694941...f10`），底模仅放在 ComfyUI `diffusion_models` 正式目录；20个 FLUX.1 LoRA 测试入口已直接放入 `models/loras/国风浅涂/` 上一层现有分类目录（人物10个、灵兽5个、风格5个），多余的 `Schnell_GGUF-Q8_0/` 嵌套目录已删除。
- M9.100：FLUX.1 Schnell GGUF Q8_0 已接通正式资产基准照接口；ComfyUI-GGUF、Apache-2.0 的 CLIP-L、T5-XXL FP8 与 FLUX.1 VAE 已安装并纳入共享模型目录，启动白名单已启用 GGUF。项目按 ID 固定1个风格 LoRA，人物按项目、角色与性别固定1个人物 LoRA，并按物理权重去重；真实512×768接口双 LoRA 出图18秒完成并返回完整模型与 LoRA 元数据。
- M9.99：生图服务关闭新增原子门禁，关闭锁内先封禁新任务，模型监督进程仅可在同一锁内完成门禁检查、启动和登记；快照后不再产生无登记重负载进程。国风浅涂正式目录、运行代码、下载脚本和41条商用LoRA索引已统一为`models/loras/国风浅涂/`，20个非商用测试权重保持索引外隔离。
- M9.101：资产基准图已接通两段式独立串行链路；FLUX.1 Schnell 单独完成生成并将中间成品保存到`output/images/intermediate/`、释放模型后，Qwen Image Edit 2511 单独读取该文件，通过真实噪声蒙版执行局部瑕疵修复并输出最终图。两套模型使用不同ComfyUI提示图与prompt ID，服务任务持久化`schnell_generating/qwen_repairing`阶段、prompt ID和心跳，并保持独立模型生命周期；人物、道具、场景角度图均读取已确认基准图，禁止嵌套为同一工作流。
- M9.102：按用户指令清除`models/loras/`全部98个LoRA权重并同步清空模型索引与商用索引；4.1GB文件已移入`/Users/aoo/.Trash/AI-Agent-all-loras-20260809/`，可恢复。LoRA风格接口当前返回空列表，Schnell只能使用无LoRA基础模型。
- M9.103：`models/loras/国风浅涂/`已重新安装20个可商用FLUX.1 LoRA，风格、女性、男性、仙宠各5个，文件名全部带“仙”；全部通过MIT/OpenRAIL++许可、FLUX.1-dev底模cardData与tag双门禁、Safetensors FLUX LoRA权重键、大小和SHA-256校验，并接入Schnell扫描。基准图Qwen局部修复由40.86GB BF16切换为20.53GB FP8Mixed并降至8步；停止、重启、看门狗及失败轮询同步取消Comfy prompt，已清除任务失败后仍占队列的幽灵Qwen任务。
- M9.104（历史三槽口径，已被M9.163覆盖）：独立Qwen Image Edit链继续沿用，但当前输入扩展为六格并逐槽确认；任务持久化、头顶完整与脚部可见门禁继续有效。
- M9.105：剧本任务已接入唯一任务ID、服务端真实活动线程登记、持久心跳、330秒超时、停止及服务重启回收和独立看门狗；前端中断状态会立即持久化为失败，空剧本占位不会再阻塞续生成，只补齐缺失集且普通失败最多重试一次。当前“宗门小师妹，全宗门抢着护我”第1集剧本已完整保存，生成分镜脚本门禁已满足。
- M9.103：生图任务增加 ComfyUI prompt 级防残留机制。重新生成、资产清理、服务启动恢复、异常退出和正常关闭均按持久化 prompt ID 定向取消本流程任务；启动清空 Qwen 临时参考图与蒙版，成功或失败均释放输入和模型，防止旧图及幽灵任务污染后续生成。
- M9.104：项目阶段持久化改为事务锁内按阶段合并，禁止旧副本覆盖整个`stage_state`；每次写入前保留最近50份有效快照，项目版本与回滚真实落盘，主文件损坏时自动读取最近有效快照。
- M9.94：人物 LoRA 已接通项目内唯一绑定；全剧继续固定一套风格 LoRA，每个人物按项目风格和性别独占一个商用 SDXL 人物 LoRA，文件名、路径、SHA-256 与底模持久化到人物档案，重复占用直接阻断。当前项目林婉儿、陆云渊、苏青梧已分别绑定三份不同人物 LoRA。

## 当前约束

- M9.146：`models/loras/国风厚涂/`只保留原生FLUX.2 Klein 9B候选：风格2、女性1、男性1、灵兽1；旧FLUX.1已迁移到缓存退休目录。5项均完成9B 8-bit真实加载出图，仅古代幻想风格达到国风厚涂候选标准，其余因偏平涂、伪文字或非神兽表现禁止进入生产索引。
- M9.146接入：国风厚涂Klein‑9B基准图正式加载已通过的古代幻想风格LoRA；男女与灵兽候选已完成技术接线，但逐模型`production_approved`门禁因视觉或许可不合格而将其排除。生产命令使用`--lora-paths/--lora-scales`，加载前强制校验索引、大小和SHA‑256，返回元数据记录实际权重。

- M9.140（历史分镜人物参考口径，已被M9.163覆盖）：曾仅让0°近照进入人脸推理、侧背图只参与终审；当前Qwen-Edit多参考六格均按已确认状态进入人物档案、生成与审核链。其InsightFace身份及画面一致性门禁继续保留。

- M9.134（历史人物角度链，已被M9.163覆盖）：FaceID/IP-Adapter三视图方案仅保留为历史验证记录；当前固定角度唯一执行链为Qwen-Edit多参考六格，人工确认和身份验收门禁继续保留。

- M9.133（历史人物角度链，已被M9.163覆盖）：OpenPose/Depth/IP-Adapter三视图方案已废止；当前为Qwen-Edit多参考六格，原尺寸、比例、方向与身份服装机器门禁继续保留。

- M9.129：道具资产必须是单个独立可拿取实体的纯中性灰产品图；场景、建筑、商店、街道、房间、家具、招牌、文字、人物或人体任一出现即禁止放行。道具基准图不加载人物风格 LoRA。
- M9.128：人物正面全身、90°侧面和180°背面统一强制上下各约5%画高安全边距，分别以发顶最高点和完整鞋底最低点测量；任一边距不通过即禁止放行。
- M9.127：两份 FLUX.1 风格 LoRA 已获准直接使用：国风浅涂项目的 Schnell 基准图加载“国风仙韵”，国风厚涂项目加载“油画仙韵”；固定角度阶段不加载任何 LoRA。
- M9.126（历史人物角度链，已被M9.163覆盖）：RealVisXL/IP-Adapter/OpenPose方案已废止；当前为Qwen-Edit多参考六格并逐槽确认。

- M9.125：国风浅涂/国风厚涂已形成14个 FLUX.1 Schnell GGUF Q8_0 仅测试候选池，风格5个、女性3个、男性3个、灵兽3个；骑士和古风肖像来源的官方示例均为女性，已移出男性并归入风格类；男性三项改为来源明确声明 Asian boy、male models、Men Model 的权重。候选独立登记于 `models/loras/测试LoRA索引.json`，正式模型索引和商用索引保持为空，逐项实图验收前禁止生产启用。

- M9.117：场景资产四张图固定为主平视全景、左45°斜侧全景、右45°斜侧全景、高角度微俯视；左右方向分别生成、分别保存，旧单45度和低角度槽在继续生成时自动替换为新角度定义。
- M9.116：固定角度图片“生成中”卡死已修复；前端项目恢复不再只查询定位基准图任务，同时按确定性角度任务名恢复人物、道具和场景的每个固定角度结果，完成任务回填图片并标记confirmed，中断任务重置为可继续生成。当前苏清璃和陆沉6张角度图已真实恢复并持久化，ComfyUI无运行或等待队列；Vue类型检查、Vite正式构建、21项单元测试与3项子测试通过。
- M9.115：道具和场景定位基准图已补齐与人物相同的“就要这张”入口；媒体浮层和图片简介内均可确认，确认后沿现有场景/道具固定角度链路生成其余三张，按钮按首张基准图键、生成状态和完成状态统一显示或隐藏。Vue 类型检查、Vite正式构建、20项单元测试与3项子测试通过。
- M9.114（历史候选测试，已被M9.163覆盖）：RealVisXL/IP-Adapter/OpenPose实图只保留为隔离验证记录，不属于当前固定角度链。
- M9.113：新版 6.5 头身 OpenPose 正面模板已完成 928×1664 真实出图；人物由九至十头身恢复为自然少女约 6.5—7 头身，头顶、双腿和双鞋完整入画，纯背景恢复。该图仅用于比例验证，因正式风格与人物 LoRA 尚为空未作为人物角度成品放行。
- M9.112：已清除正式目录内全部 19 个 FLUX LoRA，并将两份虽兼容 SDXL 但实图不符合纯灰背景、国风浅涂和自然人体比例的候选 LoRA 一并移出；正式 LoRA 索引当前为空，未通过实图验收的权重禁止进入生产。OpenPose 模板已按少女约 6.5 头身重新缩短躯干和腿部骨架，头脚控制范围由 14%—89% 收敛为 20%—82%。
- M9.110：根目录四份开发协作文档已统一重命名为 `Dev_INSPECTION_SPEC.md`、`Dev_MainDev.md`、`Dev_TESTING_SPEC.md`、`Dev_BUG_TRACKER.md`；AGENTS、README、产品愿景、项目记忆及系统 Agent Skill 内的全部对应路径已同步更新。
- M9.109：主线开发规划唯一文件已切换为根目录 `Dev_MainDev.md`；README、项目记忆、产品愿景、稽查规范及开发/稽查 Skill 提示中的旧 `AI_DEVELOPMENT_PLAN.md` 引用已全部同步清除。
- M9.106（历史人物角度链，已被M9.163覆盖）：IP-Adapter/OpenPose固定角度方案已废止；当前为Qwen-Edit多参考六格。
- M9.107：固定角度实图人工验收确认正面全身可完整出图，但 90 度侧面连续三轮均存在身份、发型、服装或身体比例漂移；本地自动视觉验收对这些图产生假阳性，侧面与背面暂未放行。
- M9.108：Schnell LoRA 性别选择已兼容中英文女性标记并通过真实出图；当前项目实际加载“暗夜仙境”风格 LoRA 与“汉服仙姝”女性 LoRA 后，仍未匹配林婉儿银灰及肩发、淡色长裙和国风浅涂淡彩风，当前 LoRA 人设与风格验收均不通过。

- 跨平台启动时动态识别物理内存和实时可用内存；系统保留容量只计算一次，禁止从实时可用内存重复扣减；绘图内存按分辨率动态估算。
- 当前 Mac 128GB 统一内存额外执行 AI 合计上限 90GB、系统预留至少 35GB 的更严格限制。
- 重负载任务全局串行；上下文、KV 缓存和批量生成强制限额，超限任务自动拆批；任务完成立即卸载模型并释放缓存。
- 稽查 Skill 永久 `writable=false`。
- 模型只能从平台注册表选择。
- 配置和任务变更必须形成可确认、可追溯提案。
- 未配置真实模型时明确失败。
- 当前项目路径：`/Users/aoo/Code/AI Agent`。

## 已知缺口

- M9.139（历史未落地候选，已被M9.163覆盖）：Visual Persona/PSHuman三视图方案未成为当前执行链；当前固定角度唯一执行链为Qwen-Edit多参考六格。其许可与远程服务门禁仅保留为历史记录。

- M9.138（历史人物角度链，已被M9.163覆盖）：Klein扩图与RealVisXL/IP-Adapter侧背方案已废止；当前为Qwen-Edit多参考六格，原比例及身份验收数据仅作历史依据。

- M9.135：场景与分镜画内文字已改为“扩散阶段留白、确定性字体层后置写字”的两段式链路。后端只识别明确文字指令并从生图提示中移除目标字符串，成图后按原始Unicode逐字绘制；缺少承载区域时自动补深色中性牌面，输出记录文字、字体、方向、锚点、SHA256与`exact_source_render`审计。人物基准图和普通道具图不会误触发。真实场景已生成并验证竖排“摘星楼”三字无乱码；7项后端回归、Vue类型检查和Vite正式构建通过。
- M9.136：Qwen Image Edit 2511 的MPS不兼容FP8混合权重已删除，主动局部修复固定改用`qwen_image_edit_2511_bf16.safetensors`。道具和场景固定角度不再自动调用Qwen‑Edit，改由FLUX.2 Klein 4B MLX直接生成角度候选；Qwen只在用户明确点击修复时加载。旧玉佩三张角度任务因FP8/MPS报错未产出最终文件，状态恢复后必须重新生成。
- M9.137：场景文字链路补齐生成前后双门禁：没有指定文字时，任何汉字、乱码、字母、数字、标题、标牌或水印均禁止放行；指定文字时也必须先得到无字底图，再执行确定性Unicode字体叠加。空场景同时禁止真人、人体、背影、剪影、人形雕像及海报人物，最多三张候选均不合格则明确失败。人物正面全身角度链路降低近照FaceID/服装构图权重、提高标准全身OpenPose强度并扩为四次候选，优先保证完整头脚和鞋底入框。

- M9.130：图片放大预览固定采用 Codex 式底部居中“缩小/实际百分比/放大”悬浮条，并支持滚轮、拖拽、双击复位与适窗缩放。人物全身角度图上下5%边距由生成后前景归一化程序确定性执行，不再由不稳定视觉模型单独裁决；苏清璃正面全身真实重测已完成并通过。
- M9.131：苏清璃旧全身图与基准图的人脸、辫子、浅青服装不一致，原验收是假阳性。现已提高身份锁定、加入基准图实际视觉特征提取和独立一致性复核，并禁止近照以高权重服装构图干扰OpenPose；连续真实候选均被新门禁正确驳回，错误图不再显示“就要这张”。

- 生产模型和媒体提供方需部署方注入。
- 插件签名、进程沙箱、真实部署、备份恢复和灾难恢复尚未验收。
- 生产运维文档仍需随真实环境验收同步。

## 下一步

- 当前主线：M9.149最终稽查残留项已整改，待独立软件测试。兼容流水线取消先写LangGraph权威状态，重启后保持cancelled且禁止批准、恢复或继续执行；JSON仅保存图投影。25项生产能力、8项基础设施、共享发现、远端数据面、双重租约围栏和服务端阶段编排保持有效。SQLite只支持同机多进程，跨主机高可用必须替换PostgreSQL/Redis提供方并完成故障测试。

- 继续完成短剧视频、配音、合片、增强与导出兼容接口恢复及真实全流程验证；保持 M11 暂停。
- M9.56：资产页“继续生成”已兼容已存在资产档案但分镜状态未及时恢复的项目；“就要这张”已补齐旧资产固定角度定义，并在生产台账兼容接口不可用时继续执行基准图确认和角度图生成。真实页面验证两个按钮均可见、可用且点击区域命中。
- M9.118（历史四视图口径，已被M9.163覆盖）：当前为Qwen-Edit六格；928×1664、背景、人物占比、头脚完整、方向、年龄、性别、脸型、发型和服装门禁继续适用于对应槽位。
- M9.119：人物基准图严格验收字段增加后，LLaVA沿用128-token输出上限导致JSON被截断并触发三轮无效重试，界面表现为生图卡住；现已启用JSON格式、1536上下文和320-token输出，重试提示不再嵌入截断验收全文。残留运行状态已释放，两个人物恢复为待生成。
- M9.120：人物基准图失败提示不再向界面暴露内部视觉验收JSON，改为仅显示未通过的中文规范项；自动生成由最多三张缩短为最多两张，新增“人脸占画60%—70%”明确门禁。苏清璃旧失败与队列状态已清空，人物资产恢复待生成。
- M9.121：人物基准图无法生成的最终根因是本地LLaVA对年龄、发型、服装和灰背景持续产生假阴性；基准图现按既定“生成→人工确认”职责拆分，自动门禁仅执行单脸检测、928×1664、规范化65%人脸裁切和InsightFace严格0度正脸，主观人设由“就要这张”人工确认。新增基于InsightFace脸框的自动裁切，苏清璃、陆沉真实页面生成均完成并回填，两张均显示“就要这张”。
- M9.122：正面基准图尺寸规范经人工校正为脸高占画40%—50%；自动裁切目标改为45%。因InsightFace脸框不含头发，新增黑色发顶检测并强制头发顶部保留5%安全边距，脸部与完整发型必须全部位于928×1664画框内；旧裁切基准图作废重生。
- M9.123：用户确认正面近照发顶安全边距由约5%调整为约2%；脸高40%—50%、自动目标45%、完整头发与肩胸入框、928×1664和严格0度正脸规则保持不变。
- M9.124：用户最终确认正面近照发顶安全边距恢复为约5%，左右两侧新增各约2%安全边距；自动裁切同步增加发宽检测和最大缩放限制，严格保证完整头发位于画框内。脸高40%—50%、自动目标45%、928×1664和0度正脸保持不变。
# M9.144 大纲生产任务可靠性与性能闭环（历史模型口径，已被M9.163覆盖）

- 当时采用27B Q8生成与122B终审；该模型口径已废止，当前固定为qwen3-vl:32b生成、qwen2.5:72b初审→最多一次修正→终审，失败保留结果与问题并停止，不自动重跑全剧。
- 当前两模型按需串行加载并在完成、异常或停止后释放；前端直接展示完整文本。
- 大纲已纳入文本任务唯一ID、真实线程、阶段、心跳、1800秒硬超时、按项目与客户端批次精确停止、重启恢复、关闭回收和failed终态保护。
- 当前模型runner继续按owner job_id隔离；停止等待锁的项目只取消自身，只有目标任务正是runner owner时才执行模型终止，防止跨项目误杀。
- 项目切换或页面中断会立即保存原项目失败状态并停止对应后端任务，不允许残留generating。
- 该历史BUG已由后续里程碑覆盖；当前执行口径以M9.163为准。
- M9.148：全链路规范唯一时长口径为55—65秒，后端相关文本入口越界明确拒绝。AI视频分段、剧本段落映射、3D遮挡、Blender头Mask、身份阈值、`_vN`选版、失败升级、共享SHA、风格阻断与视频选路均已写为目标验收条款；未完成正式动态接线前返回`capability_not_implemented`，不得声称已实现。
- M9.162（重新最终稽查通过，已关闭）：阶段进度按revision长轮询；取消键为tenant/user/project/stage/request完整作用域，跨项目同request互不影响。shutdown广播使活动watch立即503并释放槽；软件128项及3个子测试与最终稽查通过。
- M9.161（重新最终稽查通过，已关闭）：分镜按完成集增量持久化；前端每500毫秒读取并按集串行调用不推进LangGraph的`storyboard_preview`，旧项目晚到响应受project/session/abort围栏阻断。最终确认先停止轮询并等待预览队列，仅提交一次正式assets，保留已有图片与确认状态。软件110项及3个子测试与最终稽查通过。
- M9.166：当前3D输入唯一口径为人物左45°全身、道具45°三分之二、场景45°空场景全景。道具和场景资产页只生成一张2D基准图，确认后自动执行TripoSR→Blender并把七角度RGB/Mask/Depth/Normal回填卡片；人物仍保留六格2D身份档案，但TripoSR只读取左45°基准。
- M9.165（最终稽查通过，已关闭）：重启恢复权归服务端。前端加载不自动调用10类生产任务，空资产/旧census仅播种本地空卡；分镜视频不再50ms重提。服务端八阶段恢复同步失败化LangGraph、项目阶段和台账并递增revision，幂等且不占生产资源；最终独立回归128项与3个子测试及稽查通过。
- M9.167（重新最终稽查通过，已关闭）：统一资源调度器新增租户和项目分层背压。accelerator、cpu-media、control全局/租户/项目限额为16/8/4、64/32/16、256/128/64；同步及三条异步票据、waiting-memory状态投影、原子背压、软件131项与3个子测试及最终稽查通过。
- M9.167异步边界：视频即时、waiting-memory恢复和3D后台任务必须从持久body显式传入tenant/user/project；thread-local仅作为同步HTTP兼容上下文，不得作为跨线程身份来源。
- M9.167状态边界：`waiting_memory`是任务仓库真实子状态，投影LangGraph时固定映射`queued`，两层状态不得混用。
- M9.168：资产生成遇到`production stage is already running: assets`时等待服务端权威阶段完成并合并结果，不再直接标记失败。人物卡仅调整显示顺序为0°半身、0°正面全身、左45°、右45°、90°、180°；M9.166左45°作为TripoSR唯一人物输入的职责保持不变。
# 当前主线

- M9.170：服装已从人物本体拆为道具栏独立`costume`资产；分镜逐镜记录并解析`costume_id@version`换装，45°单图进入TripoSR/Blender独立网格链并按稳定ID归档。角色基础体穿衣/蒙皮/碰撞仍为待开发，前后端统一阻断并禁止冒充完成。当前状态：最终稽查通过，已关闭。
- M9.169（软件测试通过，待重新稽查）：统一任务事实以单个SQLite批量事务提交，并同事务写持久投影outbox；JSON仅后置兼容投影。第二条DB失败整批回滚、三类Graph失败保留completed与outbox、重启及心跳重放成功ack均通过动态测试。
- M9.169并发围栏：outbox事件使用全局单调revision，ack必须job_id+revision匹配；LangGraph按stage拒绝旧projection revision，防止并发旧drain误删新事件或回退图状态。
- M9.169跨进程围栏：任务SQLite按tenant/user/project/stage保存投影租约并自动续租；拿锁后重新读outbox，两个服务实例不得并发提交同一阶段checkpoint，崩溃后租约过期重放。
- M9.169失租围栏整改（待软件测试）：租约续期失败、owner变化、TTL过期统一置lost；Graph提交前后验证所有权，提交途中失租时从权威任务生成更高revision修复事件；SQLite抢锁busy按未取得处理，不得杀死heartbeat。
