# AI Agent 项目记忆

当前任务、并行工作流和排队顺序只从`docs/product/当前开发状态.md`恢复。本文件是长期原因与证据历史，不再裁决当前状态；旧“待测试/待稽查”按历史阅读。

- M10.5 / BUG065主线完成待独立测试：单节点备份改为五类白名单隔离快照、SQLite在线backup、边车排除、链接/特殊成员拒绝、manifest v2逐文件完整性及原子恢复。关联`26 passed`；WAL持续写小样本恢复`integrity_check=ok`、471条记录和1MiB媒体哈希一致，0.0076秒不作为生产RPO/RTO。
- M10.5 / BUG065首轮稽查退回并整改：根路径先`resolve()`会隐藏源/目标自身symlink并跟随到外部目录；现所有备份/恢复根在解析前拒绝链接，动态证明链接目标外部目录零写入。关联`27 passed`，待独立复测。
- M10.5 / BUG065首轮整改复测`27 passed`；二轮稽查退回并整改恢复权限漂移：manifest逐文件登记mode，安全提取显式应用并复核，动态`0600`秘密引用恢复后仍为`0600`。关联`28 passed`，待独立复测。
- M10.5 / BUG065二轮整改复测`28 passed`；三轮稽查退回并补齐目录mode，安全提取完成后逆层级应用，动态config `0700`与秘密引用`0600`同时保持。待独立复测。
- M10.5 / BUG065三轮整改独立复测通过待复稽查：冻结提交`090a7f0`覆盖SQLite一致性、完整性、安全路径及文件/目录权限`28 passed`无失败无跳过，Python编译通过。

- M9.198 / BUG063最终只读稽查通过并关闭：顶层架构v2.2与11阶段、ProductionLedger原子边界、`waiting_memory`投影、RecordExporter及provider inflight保护代码事实一致；独立`112 passed`无失败无跳过。下一步独立稽查Stage四处登记CI门禁实现完整性。
- M9.198 / BUG064最终只读稽查通过并关闭：11阶段四字段登记与规范阶段精确一致，后端启动恢复和前端流程直接消费且缺项失败关闭，继续函数存在性受CI门禁；独立`109 passed`和类型检查通过。下一项BUG039进入根因分析。
- BUG064补充关联测试同步：恢复测试从硬编码旧局部映射改为解析版本化登记表并核对image/video投影，门禁组合`4 passed`；属于测试事实源迁移，无产品代码变更。
- M9.198 / BUG039最终只读稽查通过并关闭：原子`register_once`复用活动provider且不改变handler/metadata/健康/inflight，真实替换与卸载仍严格阻断；独立`104 passed`及编译通过。进入BUG040只读分析并保留用户前端现场。
- M9.198 / BUG040首轮稽查退回并整改：成功媒体回填本身成立，但旧恢复轮询只比较project id，同项目新session会被旧completed晚到写入。现捕获发起session，每个异步返回及持久化前统一复核；Node矩阵证明旧session零URL写入、零持久化。App只暂存该隔离hunk，用户其余界面现场保持未暂存。
- M9.198 / BUG040稽查整改独立复测通过待复稽查：冻结提交`2b25217`显式Node成功/失败/同项目新session晚到矩阵及文档状态`21 passed`无失败无跳过，Vue类型检查通过；规范状态词首败已修正。
- M9.198 / BUG040最终只读复稽查通过并关闭：权威completed媒体才回填，失败不伪装成功；每个异步返回和持久化均绑定project+session，同项目旧session晚到零写入。进入BUG041历史对话资源隔离分析。
- M9.198 / BUG041独立软件测试通过待只读稽查：历史接口只接受tenant/user/project/session精确键和数组投影，唯一其他项目历史及畸形数据均返回空；冻结提交`a11efb9`关联`106 passed`无失败无跳过，Python编译通过，未删除历史数据。
- M9.198 / BUG041最终只读稽查通过并关闭：历史读取严格绑定tenant/user/project/session，缺失和畸形失败为空，旧项目自身历史保留且没有数据删除。当前代码范围仅余BUG057受CUDA兼容H3执行提供方强依赖阻塞。

- M9.198 / BUG061已关闭并登记BUG062：嵌套`subfolder`在`OUTPUT_ROOT`内安全解析，正式H3成功进入采样；但INT8 ConvRot在Darwin/MPS实际落入单核`_int_mm_cpu`，70分钟仍为0/20。任务经`cancel_pending`和Comfy重启收敛cancelled、队列归零；当前建立设备/算子兼容门禁及`model_blocked`真实状态，禁止把不可交付回退冒充运行。
- M9.198 / BUG062主线实现：固定H3制品只接受CUDA设备声明，MPS/CPU/未知设备在Context IR前进入持久`model_blocked`，任务投影和LangGraph均paused且允许提供方变更后显式重放。正式job`b92ede69-7963-4a9f-af20-6f16e23f89ae`约2秒阻断、无任何prompt、队列为空。关联159项通过；完整unit的4项失败均来自用户并行未提交前端界面变更，待其收敛后无deselect复跑。
- M9.198 / BUG062已关闭：独立关联159项无失败无跳过，最终稽查确认固定制品设备白名单、未知设备失败关闭、`model_blocked`/paused持久状态、无prompt副作用和提供方变更后显式重放边界。当前唯一真实阻塞回到BUG057：本机只有MPS，缺少CUDA兼容H3执行提供方，禁止静默改用Wan冒充验收。
- M9.198 / BUG062补充稽查退回：运行入口已阻断但能力目录仍发布H3 provider为健康。整改后注册健康使用同一Comfy设备事实，当前MPS公开`healthy=false`、CUDA白名单与availability_error；其他能力不受影响。正式接口和关联160项通过，待独立复测/复稽查。
- M9.198 / BUG062补充复测与复稽查通过：提交`2a94278`的关联160项无失败无跳过，正式能力接口精确显示H3 provider unhealthy、CUDA白名单和MPS阻断原因；能力选择与业务准入同一事实，BUG062重新关闭。

- M9.198 / BUG060已关闭：外部Comfy节点获明确授权后应用固定补丁；补齐helper局部`function_tool`导入并分别锁定补丁文件与应用态diff SHA。正式Ref2VA一次完成Context IR并持久`h3-prompt-writing`选择、优化提示词和原始JSON；关联33项、完整595项及9个子测试无失败无跳过。随后3D源视频多级`subfolder`被解析器截断，登记BUG061串行处理。

最后更新：2026-08-12

- M9.198 / BUG059正式复现：人物角度图Qwen产图后，原图片job心跳停止，LLava后验收却以`character-angle-audit-<random>`独立排队；后续图片job在该不可观测占用期内等待内存并超时。首个错误事实是人物baseline/角度验收未经`_run_image_validation`，且两次审核票据不属于原job；当前需先闭环后验收心跳、取消、超时和晚到隔离，再继续BUG057真实H3。
- M9.198 / BUG059主线整改完成：人物baseline/角度验收进入原job的`_run_image_validation`，角度、服装、画幅审核共用原job票据与从持久请求恢复的完整身份，并受同一180秒deadline及每步可运行性复核保护。正式job`ef558c27-c3b3-4bef-ad8d-f662fd6a8579`后验收心跳持续到真实质量失败终态，无随机票据或资源残留；关联`175 passed, 3 subtests passed`、完整unit`586 passed, 9 subtests passed`。待独立软件测试。
- M9.198 / BUG059独立软件测试通过：冻结提交`430397b`直接关联`175 passed, 3 subtests passed`，完整unit`586 passed, 9 subtests passed`，0失败0跳过；编译、文档与diff门禁通过，工作树干净。待只读稽查。
- M9.198 / BUG059首轮稽查退回：LLava子步骤已共享原job，但InsightFace/OpenPose/比例检查没有job/deadline；OpenPose prompt未持久，外层超时不能核销，验收线程可在终态后继续。当前须把全部确定性子步骤纳入同一有限、可取消边界后重新测试。
- M9.198 / BUG059稽查整改完成：InsightFace子进程归原job活动进程映射并持续检查取消/deadline，OpenPose prompt持久为`validation_prompt_id`并在后验收超时、用户停止、服务恢复和关闭时精确核销。动态证明子进程无终态残留、prompt超时精确取消且不启动下一步；关联`177 passed, 3 subtests passed`、完整unit`589 passed, 9 subtests passed`。待独立软件复测。
- M9.198 / BUG059稽查整改完成：InsightFace子进程归原job活动进程映射并持续检查取消/deadline，OpenPose prompt持久为`validation_prompt_id`并在后验收超时、用户停止、服务恢复和关闭时精确核销。动态证明子进程无终态残留、prompt超时精确取消且不启动下一步；关联`178 passed, 3 subtests passed`、完整unit`589 passed, 9 subtests passed`。待独立软件复测。
- M9.198 / BUG059稽查整改独立复测通过：冻结提交`9685d38`直接关联`178 passed, 3 subtests passed`，完整unit`589 passed, 9 subtests passed`，0失败0跳过；编译、文档与diff门禁通过。待只读复稽查。
- M9.198 / BUG059二轮稽查退回：OpenPose prompt虽已持久，但仍绕过原job资源调度；且核销返回false被外层、看门狗和关闭忽略，仍可写failed终态。须增加原job `audit` claim与持久`cancel_pending`对账，只有prompt确认离队后才能收敛。
- M9.198 / BUG059二轮稽查整改完成：OpenPose使用原job完整身份与`audit`claim，共享后验收deadline；Comfy/LLava核销未确认时统一持久`cancel_pending`及预定终态，看门狗、恢复、关闭和用户停止持续对账且保留subject所有权。动态证明历史completed也会先撤回非终态、确认离队后无损恢复；关联`110 passed, 3 subtests passed`、完整unit`593 passed, 9 subtests passed`。待独立软件复测。
- M9.198 / BUG059二轮整改独立软件复测通过：冻结提交`ec12a1c`关联`110 passed, 3 subtests passed`、完整unit`593 passed, 9 subtests passed`，0失败0跳过；编译、文档、diff和干净工作树门禁通过。待只读复稽查。
- M9.198 / BUG059三轮稽查退回：`cancel_pending`进入通用无owner清理后会被新错误覆盖原预定completed语义；持续LLava核销又未重新取得原job accelerator所有权，可能误停下一审核job。须优先原样对账持久预定终态，并仅在原job重新取得有限`audit`claim后重试停模。
- M9.198 / BUG059三轮稽查整改完成：通用清理无损恢复持久`pending_terminal_*`；LLava持续核销只有在原job完整身份取得250ms有限`audit`claim后执行，资源忙时保持pending且零停模调用。关联`112 passed, 3 subtests passed`、完整unit`595 passed, 9 subtests passed`。待独立软件复测。
- M9.198 / BUG059三轮整改独立软件复测通过：冻结提交`490d2a0`关联`112 passed, 3 subtests passed`、完整unit`595 passed, 9 subtests passed`，0失败0跳过；编译、文档、diff和工作树门禁通过。待只读复稽查。
- M9.198 / BUG059最终只读复稽查通过并关闭：OpenPose与LLava均受原job资源所有权保护；核销未确认持久非终态并持续对账，通用清理无损恢复预定终态，资源忙时不误停后续模型。软件复测`595 passed, 9 subtests passed`无skip；继续BUG057正式静音H3与不超过15秒全链。
- M9.198 / BUG060正式复现：隔离3镜项目已确认至image，正式3秒H3 job`d72dfc6f-bdb7-406a-8e51-97b7702f3b4b`两次Context IR均报`Tool h3-prompt-writing not found`。节点仅把主Skill内联到instructions，却未像两个style helper一样注册精确工具名；本地32B在Ref2VA请求中选择该工具后被Agents SDK确定性拒绝。当前修固定供应链补丁并重跑真实H3，BUG057暂阻塞。
- M9.198 / BUG060仓库内整改：固定补丁新增精确同名只读主Skill工具，返回已加载Skill+当前guide，不接收路径且不联网；安装器锁SHA并用AST强制验收。固定上游隔离apply/Comfy Python编译、关联`48 passed`和全unit`595 passed, 9 subtests passed`通过。正式节点在仓库外，等待用户扩权应用补丁并重启Comfy后真实复测。
- M9.198用户验收范围更新：最终成片只需2—3个镜头、总时长不超过15秒；大纲到成片各阶段、停止/恢复/隔离、音画字幕与导出契约仍须完整验证。

- M9.193 / BUG038最终稽查再次退回并完成框架整改，待独立软件测试：定位为重生成预清理删除仍被completed job及历史快照引用的成功媒体。现采用不可变媒体版本“生成成功后切换”，失败保留旧图；服务恢复会把完成但文件缺失的job降级failed。关联专项`92 passed, 1 skipped, 3 subtests passed`，后端编译通过。
- BUG038同一媒体生命周期继续修复角度图假运行：轮询此前只对账无baseline资产，已有baseline的detail angle后端失败后UI永久generating。现baseline/variant统一按job终态对账；真实苏璃左45°已从伪生成收敛为失败可重试。专项`77 passed, 1 skipped`、typecheck与83模块构建通过，待独立软件测试。
- BUG038补齐可执行角度终态矩阵：completed动态回填URL/等待确认/持久化，failed动态同步variant与item终态/持久化，`1 passed`。扩大回归暂被并行资源生命周期代码的两项内存时钟测试阻断，未覆盖并行改动。
- 并行代码冻结后BUG038关联组合重跑`82 passed`且无skip，文档门禁通过，重新提交独立软件测试。

- M9.193 / BUG038重开后独立软件测试通过，待只读稽查：真实9B job completed，浏览器资产卡已显示云长老928×1664图，顶/底安全区`8.9992%/5.0008%`；正式HTTP Handler成功/轮廓失败/VLM误判脱敏矩阵`10 passed`，关联`131 passed, 3 subtests passed`且无skip，文档状态门禁通过。

- M9.193 / BUG038因用户真实页面仍失败而重开，原关闭结论撤销。首个错误事实是VLM对真实偏航0.41°的正面图误判方向，且缺失`plain_background`字段，baseline却将两项作硬门禁。现改为确定性姿态、928×1664及GrabCut安全区三项硬门禁，主观项保留给人工确认；错误不再暴露截断JSON。真实9B HTTP与浏览器卡片点击生成均成功，页面已显示云长老图，job completed，顶/底`8.9992%/5.0008%`，关联`128 passed, 1 skipped, 3 subtests passed`，待独立软件测试。

- M9.198当前主线：按剩余框架清单执行真实新项目大纲到增强导出验收；先核验正式入口、模型/媒体provider、资源预算与既有集成覆盖，再补齐生产链证据。BUG039—041继续只排队。
- M9.198 / BUG046稳定复现：新项目`83eaa695-0a31-4119-aa94-060c232053fd`服务端stage_state为空，正式界面仍显示旧项目全阶段数据。创建保存分支未调用新project/session的流程加载，旧响应式投影未清空；继续操作可能把旧内容持久化到新项目。
- M9.198 / BUG046整改完成待独立测试：每次项目流程加载先同步清空十阶段投影，再按新project/session恢复；创建/编辑保存显式等待流程及资源/任务/版本加载。正式新旧切换立即帧与稳定帧均零旧内容，后端空stage_state未被污染；全unit`556 passed, 9 subtests passed`。
- M9.198 / BUG046独立测试通过待稽查：正式连续三轮跨项目切换立即/稳定投影均隔离，空新项目stage_state持续为空；定向/contract`45 passed`、完整unit`556 passed, 9 subtests passed`无skip，typecheck和83模块构建通过。
- M9.198 / BUG046（历史子问题记录，最终稽查通过，已关闭）：十阶段投影在任何新会话异步恢复前同步清空，创建/编辑保存等待新project/session加载，晚到响应继续由session围栏拒绝；正式动态与空stage_state一致。M9.198继续真实全链。
- M9.198 / BUG047稳定复现：真实大纲停止后UI为“可保留现有内容后重新生成”，任务与qwen3-vl:32b均释放；再次点击正式返回`workflow is cancelled`。阶段取消将项目工作流置为不可恢复终态，重新生成未建立新generation/reactivation。
- M9.198 / BUG047主线整改完成待独立测试：`ProductionOrchestrator.begin`只允许严格更新的stage generation从cancelled恢复，同代、generation 0和旧代晚到继续失败关闭。正式服务重载后同一project_id的新outline generation真实运行并完成，模型自动卸载；定向`120 passed`、全unit`558 passed, 9 subtests passed`、编译通过。
- M9.198 / BUG047独立软件测试阻塞：使用显式Node运行时定向`120 passed`无skip；完整unit `3 failed, 555 passed, 9 subtests passed`，失败仅为并行BUG038的索引待测试与Tracker已关闭冲突。测试身份未改其他工作流，BUG047保持待测试，不能稽查或继续下一框架项。
- M9.198 / BUG047独立软件复测通过待稽查：状态事实源收敛后定向`120 passed`、完整unit`560 passed, 9 subtests passed`，无失败无skip；关键编译、文档门禁及diff-check通过。
- M9.198 / BUG047首轮稽查仅退回记录口径：索引误写未注册的“软件测试通过，待代码稽查”；主线改为规范允许值“软件测试通过，待只读稽查”，业务实现未变，需重新测试文档同根因。
- M9.198 / BUG047最终复稽查通过，已关闭：cancelled恢复只接受严格更新generation，同/旧代晚到隔离；正式服务重启后真实32B大纲完成并释放模型，整改复测完整unit`560 passed, 9 subtests passed`无skip。继续真实全链。
- M9.198 / BUG048正式首败：大纲项目stage_state已confirmed，ledger多个outline scope为completed但confirmation为空，LangGraph outline仍pending_confirmation，剧本返回前序未完成。根因是非upscale公开projection可直接写completed/generation/confirmation，而前端只确认pending_confirmation，形成三事实源分裂。
- M9.198 / BUG048主线整改完成待独立测试：全部非upscale UI projection剥离confirmation/generation/权威证据；completed须先pending_confirmation再经服务端确认，同代现行确认不可被UI降级，新指纹清确认。正式重载后旧坏状态自动收敛，outline与script均权威completed，真实32B剧本完成，任务/模型释放；关联`135 passed`、全unit`564 passed, 9 subtests passed`。
- M9.198 / BUG048独立软件测试通过待稽查：关联`135 passed`、完整unit`564 passed, 9 subtests passed`无失败无skip；编译、文档门禁及正式outline/script权威确认、资源空状态复核通过。
- M9.198 / BUG048最终稽查通过，已关闭：全部非upscale UI projection统一失败关闭伪权威字段，completed经服务端确认，同代确认不可降级、新指纹清确认；正式旧坏状态收敛并完成真实剧本。继续分镜脚本全链。

- M9.193 / BUG038最终稽查通过，已关闭：GrabCut可见轮廓数值证据为唯一全身安全区证据；0° baseline与variant共用失败候选物理清理入口，baseline最多3次生成并具有明确completed/failed终态。真实HTTP Handler成功/失败链、失败文件无残留、关联回归及最终只读稽查均通过。

- M9.193 / BUG038最新独立软件测试通过，待只读稽查：真实HTTP Handler baseline测试2/2通过，前两次失败后第三次成功返200/completed，三次全失败返502/failed；provider恰好3次，失败候选全删除，成功链只保留有效图。关联`120 passed, 1 skipped, 3 subtests passed`。

- M9.193 / BUG038二次稽查整改完成，待独立软件测试：0° baseline与角度图共用全身候选准备入口，GrabCut/连通域/质量异常立即删除失败文件；baseline由共享候选循环限定最多3次，末次明确失败。动态测试覆盖重试次数、旧文件清理、最终成功及末次失败；关联`118 passed, 1 skipped, 3 subtests passed`，编译、Vue typecheck与83模块构建通过。全unit两项文档状态失败属其他并行工作冲突。

- M9.193 / BUG038稽查退回整改完成，待重新独立软件测试：删除GrabCut异常后的YOLO确定性证据回退，轮廓异常统一`foreground_segmentation_failed`并由候选循环清理后重生成；安全区门禁额外要求`source=grabcut_person_silhouette`。专项`3 passed`、真实旧图重复归一顶部`8.9960%/9.0084%`与底部`5.0040%/4.9916%`、图片关联`116 passed, 3 subtests passed`；正式8787 PID`58574`健康且资源空闲。

- M9.197 / BUG045（历史初始开发记录，已由最新软件测试状态覆盖）：原可观测模块只有进程内counter/gauge/span和单一日志sink。现以`RecordExporter`作为唯一扩展边界；JSONL负责单机持久证据，Prometheus textfile负责原子抓取，组合导出器允许后续接集中trace/metrics后端而不修改业务调用方。高基数关联ID不进入指标标签；request_id/trace_id进入日志、span、导出批次和告警。秘密字段递归拒绝，counter禁止负增量。当前只完成底座，尚未完成API到manifest全链强制贯穿及正式仪表盘/告警演练。
- M9.195 / BUG043最新独立软件复测通过，待只读稽查：generation优先、同代revision单调、revision0唯一首事件、legacy checkpoint迁移与真实阶段入口均通过；关联`132 passed`、完整unit`554 passed, 1 skipped, 9 subtests passed`，Node补充动态`23 passed`，Python编译/typecheck/83模块构建通过。
- M9.195 / BUG043最终稽查通过，已关闭：generation第一所有权围栏、同代revision严格单调、新代revision0唯一基线、legacy checkpoint一次迁移、被拒事件权威恢复及正式调用方revision推进均闭环。
- M9.197首轮独立测试：可观测专项/动态通过；全unit唯一失败是联网搜索测试仅mock DuckDuckGo/Bing为空，遗漏权威注册表中的Google News/Google后备，网络成功所以未触发全失败。正式实现仍在所有五provider无结果后硬失败；测试夹具已补全隔离，待独立复测。
- M9.197退回整改自检：测试夹具补齐全部免Key后备后，定向14项、全unit`521 passed, 1 skipped, 9 subtests passed`；未改变搜索生产代码，准备独立复测。
- M9.197第二轮复测首败：`labels=[("authorization","TOP-SECRET")]`转换为JSON二元数组后绕过只检查Mapping键的秘密检测。现`_contains_secret`识别二元序列敏感键，`_normalise_labels`、structured snapshot、JSONL及Prometheus exporter均独立失败关闭，防止调用方用手工快照绕过注册表。
- M9.197冻结快照主线自检：定向`18 passed`，完整unit`554 passed, 1 skipped, 9 subtests passed`；文档门禁、关键编译与限定diff-check通过。BUG045首状态已修正为与唯一索引一致的待测试，仍等待独立软件复测。
- M9.197 / BUG045最新独立软件复测通过，待只读稽查：可观测及关联`41 passed`，完整unit`555 passed, 9 subtests passed`无skip；二元敏感标签四层失败关闭，JSONL、Prometheus原子快照、counter、关联ID与告警均通过。
- M9.197最终稽查退回整改完成，待独立软件复测：NaN/+Infinity与`bad-key`曾可形成`requests_total{bad-key="x"} nan`。现指标注册与Prometheus手工快照入口双层强制有限数值、合法指标/标签名和二元标签结构；专项`6 passed`、全unit`555 passed, 9 subtests passed`。
- M9.197同根因扩大：JSONL与Prometheus共用指标快照语义门禁，NaN/Infinity、非法指标/标签及畸形集合在任何持久写入前拒绝，Composite不留部分JSONL证据。
- M9.197稽查整改独立复测通过，待只读复稽查：定向`41 passed`、全unit`555 passed, 9 subtests passed`无skip；注册与全部持久exporter边界一致。
- M9.197 / BUG045最终复稽查通过，已关闭：JSONL/Prometheus共享有限数值、合法指标/标签、畸形结构和秘密字段门禁，Composite无部分非法证据；全链trace与正式仪表盘/告警演练仍属后续框架任务。

- M9.196 / BUG044最终复稽查通过，已关闭：状态冲突夹具按BUG section动态定位首状态，强制相反状态族和实际替换，不依赖当前流转阶段；三类生命周期及相邻条目保护成立。最终专项与contract`21 passed`、全部contracts`40 passed, 51 subtests passed`、完整unit`555 passed, 9 subtests passed`且无skip。

- M9.194 / BUG-20260811-042 联网搜索最终稽查通过，已完成。此前M9.193 / BUG-20260811-038人物安全区问题保持排队状态，本轮未并行处理。

- 排队问题（只登记未分析）：BUG039云长老in-flight生成失败；BUG040生成图不显示；BUG041旧对话图片仍显示。必须等待当前BUG042完整闭环并经用户确认后逐条处理。

- 当前主线：M9.192 / BUG035最终稽查通过，已完成。review_export/upscale权威authority、项目写回与Graph已归owner+generation统一提交helper；ledger整批事务在Graph callback失败时回滚，取消先赢/失租旧代/失败路径零成功证据，Graph authority tuple支持重启失败关闭悬空pending。关联`147 passed`、完整单元测试`495 passed, 1 skipped, 9 subtests passed`，编译、类型检查及83模块构建通过；正式8787已加载且任务、资源池、Ollama、ComfyUI全空。

- M9.191（最终稽查通过，已完成）：分镜逐集数据到达后自动提取资产并串行生图；点击进入资产页只续跑缺失项，不清成品，不再提示上传。入口Promise按项目/session绑定，切项目后新项目首次进入不会复用旧Promise。P1/P2阻塞confirm/prepare动态2/2；定向102 passed、1 skipped；全单元490 passed、1 skipped、9 subtests；编译、类型检查与83模块构建通过，BUG-20260811-037已关闭。

- M9.190（最终稽查通过，已完成）：资产批次的具体人物错误仅在对应卡片显示；当前人物确认入口只清除带明确资产名前缀的兄弟卡片错误，真正的阶段错误继续保留；人物角度仍只绑定自身参考。定向118 passed、1 skipped、3 subtests；完整单元测试489 passed、1 skipped、9 subtests；编译、类型检查与83模块构建通过，BUG-20260811-036已关闭。

- M9.189（最终稽查通过，已完成）：内置生图能力改为只首次注册，禁止每次调用热替换正在运行的Klein 9B提供器；生产台账保持24列显式写入；人物全部全身角度执行发顶上方至少8%、鞋底下方至少3%的纯背景安全区，最低值不是固定值。动态inflight通过，定向209 passed、1 skipped、3 subtests；全部单元测试458 passed、1 skipped、9 subtests；编译、类型检查与83模块构建通过，BUG-20260811-033已关闭。

- M9.188 / BUG034（最终稽查通过，已完成）：生产stage入口不再以任意单条confirmed scope可信提升整阶段；确认、延迟确认、恢复/同步和入口共用统一gate fact。image/video/audio/subtitle以storyboard shot census按分集核对，额外/非法/别名碰撞scope、partial/missing/no-census、缺当前fp/batch或confirmation不匹配均失败关闭。软件与最终代码稽查均通过，正式8787已加载且任务、资源池、Ollama、ComfyUI全空；BUG034已关闭。
- BUG034最新整改：媒体gate保留全部实际scope并拒绝目标外、非法、重复/alias和非shot记录。四类媒体shot完成事实必须携带非空fp/batch，confirmation两项与顶层当前值精确一致；非媒体边界不变。定向`39 passed`、扩大关联`202 passed, 1 skipped`，编译/typecheck/83模块构建通过，待独立复测。

- BUG035（最终稽查通过，已关闭）：upscale物理执行只形成延迟authority payload，最终ledger+Graph发布归统一租约提交协议；Graph失败回滚整批ledger，重启核对精确authority tuple并失败关闭缺证pending；多集批次故障、幂等、身份隔离和崩溃窗口恢复均已动态与正式验收通过。

- BUG028 / M9.183（最终稽查通过，已关闭）：成片增强统一服务端四步、严格输入门禁、canonical证据、原子台账、single-flight/stop/FIFO及enhanced导出闭环。权威fp/batch/generation/证据/confirmation与前端progress物理隔离，跨实例CAS和不可变history拒绝篡改与旧代重放；正式新PID健康且全资源空闲。

- M9.187（最终稽查通过，已完成）：人物首张定位基准统一为0°正面平视完整全身，人工确认后按左45°→右45°→90°→180°→0°半身逐槽生成确认；六角度提示词逐项消除方向与景别冲突，人物身份描述剥离视角词。人物TripoSR输入同步为已确认0°正面全身；道具45°三分之二和场景45°空场景全景不变。定向73 passed、3 subtests；全部单元测试448 passed、1 skipped、9 subtests；编译、类型检查与83模块构建通过，BUG-20260811-032已关闭。

- BUG028 / M9.183最终权威边界整改完成，待独立软件测试：upscale权威fp/batch/generation/production+audit evidence/confirmation与projection progress物理分离，公开single/bulk请求及body authority元数据均无权改写。真实upscale跨实例持久分配单调generation，指纹绑定generation+batch，revision CAS与不可变history拒绝低代、旧tuple重放及同代伪证据；完全相同提交幂等。确认与enhanced导出三方绑定fp+batch+generation。专项`32 passed`、关联`161 passed, 1 skipped`，未启模型或操作正式服务。
- M9.186（历史左45口径，已被M9.187覆盖）：自动生成总上限3次、required失败项自适应提示及候选清理继续有效；首张角度由M9.187改为0°正面全身。

- BUG028 / M9.183最终稽查阻断整改完成，待独立软件复测：权威ledger仅在fingerprint+batch同代时允许missing/null保留证据；fp或batch任一变化都会先清旧production/audit evidence与confirmation，只接收本次非空规范证据，null及空字符串/对象/数组不进入新代。同fp新batch不再继承旧证，缺证确认仍被enhanced authority拒绝；新证据重新确认后跨实例放行。专项`36 passed`、关联`203 passed, 1 skipped`，编译、类型与83模块构建通过，未启动模型或操作正式服务。

- BUG028 / M9.183最新稽查整改软件测试通过，待稽查：正式前端sync动态证明嵌套证据原样提交且缺失字段不擦除；SQLite跨实例/确认保留，enhanced缺证拒绝、legacy仅base、fp+batch双代际精确授权。四步骤失败/取消8组、原子回滚、规范JSON、真实manifest及关联`181 passed, 1 skipped`，编译/类型/83模块构建通过，未启动模型。

- BUG028 / M9.183最新稽查整改完成，待软件复测：前端upscale深watch原样同步结构证据，SQLite progress保留式合并防missing/null擦除；enhanced缺证失败关闭、legacy缺省仅base，声明/台账/confirmation精确核对fp+batch并拒绝同fp旧批次。专项44项、关联`171 passed, 1 skipped`、交付链4项及编译/类型/83模块构建通过。

- M9.185（最终稽查通过，已完成）：资产生图/3D作为assets内部子任务，资源池与统一FIFO不变；页面级生成只补缺失项，单卡重做为唯一替换入口。服务端权威校验本地媒体并拒绝失效URL回写。关联`171 passed, 1 skipped`及最终稽查通过；墨无痕、宗门试炼场保留，苏璃、云长老pending待补，BUG-20260811-030已关闭。

- BUG028 / M9.183历史端到端证据软件测试记录（已由M9.183最终闭环覆盖）：权威scope跨SQLite实例重载与确认保持嵌套JSON类型，客户端篡改无效；enhanced files/manifest逐集携带version/fingerprint/batch/production/audit证据，旧批次拒绝，legacy base有明确not_available/not_applicable原因。关联`180 passed, 1 skipped`，编译/类型/83模块构建通过。

- BUG028 / M9.183（历史软件测试记录，已由M9.183最终闭环覆盖）：production evidence结构保留并拒绝空值、NaN、Infinity及不可序列化对象；全树递归规范哈希键序稳定、语义敏感。正式FIFO严格顺序、重复key单owner、stop/切换新owner与旧finally隔离通过；扩大回归`180 passed, 1 skipped`，编译/类型/83模块构建通过。

- M9.184（最终稽查通过，已完成）：资产图片生成、重做、修复、导入、逐槽确认、3D生成/确认和超分统一进入项目会话FIFO；停止/项目切换只使旧代未执行任务失效，不重置尾链并发启动新任务。全部已启动异步操作按捕获的project/session做响应围栏与定向持久化；旧任务结束后新项目任务才开始、最大并发1，旧响应零污染。单资产确认可延迟总阶段推进，冲突稳定返回409；BUG-20260811-029最终关闭。

- M9.183 / BUG-20260811-028（历史软件测试记录，已由M9.183最终闭环覆盖）：完整command及OCR/face/final证据逐项绑定指纹、键序稳定、未启用原因明确；缺证零ledger，确认与enhanced导出代际门禁通过。资产操作统一FIFO双向契约成立。扩大回归`178 passed, 1 skipped`，编译/类型/83模块构建通过，未启动模型。

- M9.182（最终稽查通过，已完成）：场景资产名称只允许地点型可复用空间，地点词覆盖古装、都市、校园、医疗、交通、商业、工业与自然环境；服务端读取项目权威人物名单，人物姓名、动作、姿态、状态、群体行为和剧情句子统一在登记前拒绝。空场景提示只保留空间/建筑/陈设/光线；当前项目已从20个错误动作场景迁移为唯一“宗门试炼场”空场景。

- M9.181（最终稽查通过，已完成）：所有`notify()`用户提示统一进入当前项目对话框，作为助手侧“系统提示”消息展示；连续重复提示去重、自动滚动，移除底部蓝色Toast。提示不写回助手历史，阶段状态/错误仍用于恢复和门禁；软件测试与最终稽查通过。

- BUG025 / M9.180（最终稽查通过，已关闭）：2D image投影与asset_3d边界、资产批次严格单飞/会话代际隔离、storyboard完整集零模型原样保留及仅补缺集全部闭环；扩大回归197项，正式资源与模型空闲。

- BUG025第四轮（已修，待复测）：资产批次公开入口为普通函数，直接返回唯一assetBatchFlight；同project/session first===second且只启动一次，内部async与异常/切换/停止释放契约不变。

- M9.180历史首败（已修）：公开入口曾因async包装破坏Promise identity，现已改普通函数直接返回权威flight。

- BUG024 / M9.179（最终稽查通过，已关闭）：review_export整批审核/导出、权威review与媒体证据、base/enhanced版本绑定、项目会话/single-flight/停止/代际围栏全部闭环；关联126/126及正式空闲验收通过。

- BUG024复稽查2（已由最终稽查关闭）：audit_required=false只豁免review，媒体completed+confirmation+fingerprint+batch始终强制；base/enhanced严格绑定composition/upscale，不交叉、不回落，多集缺一零副作用。

- M9.179（最终稽查通过，已完成）：权威导出、阶段边界、停止、项目会话与版本门禁均通过；正式worker、资源池和模型队列空闲。

- M9.179（历史软件测试记录，已由M9.179最终闭环覆盖）：`review_export`服务端执行器与前端单次整批入口完整闭环；审核证据、有限重试、权威导出门禁、停止恢复及项目隔离均通过。
- BUG-20260811-024（软件测试通过，待稽查）：最终审核与导出严格一次runStage；生产台账是审核与媒体版本唯一事实源，project/session/abort/epoch及完整身份停止围栏通过。

- M9.178（最终稽查通过，已完成）：审核chunk/初审/repair/final与八阶段取消检查点闭环；generation/commit guard、完整租户身份停止及跨租户保护通过。软件126项、稽查128项、类型与构建通过，正式资源/模型空闲。

- M9.178（历史开发记录，已由M9.178最终闭环覆盖）：此前仅storyboard检查stage cancel，其他阶段取消后仍可能继续提交模型/command。现8阶段统一复用现有lease event，在入口、循环、poll、local call、审核与fallback前后检查；stop先定向stage cancel再停子任务，完整tenant/user/project和child scope同时校验。关联135项通过。

- BUG-20260811-022（最终稽查通过，已关闭）：缺project旧image孤儿已持久确定性quarantine project与identity_recovery，正常投影Graph failed；outbox/nonterminal为0，普通terminal未改。

- M9.177（最终稽查通过，已完成）：BEGIN IMMEDIATE旧schema迁移、reservation完整契约及正式service_scope/唯一worker/资源空闲均通过。

- M9.177补充证据：worker reservation幂等绑定request ID与resource/service scope/estimated memory完整契约；同ID跨资源、scope或内存变化明确冲突，不复用旧worker。

- M9.176（最终稽查通过，已完成）：`max_concurrency`/`inflight`、同级公平分流、全满背压、异常释放、disabled/unhealthy过滤及在途卸载/替换阻断全部闭环；专项72项、20线程无超卖矩阵、关联96项及最终只读稽查通过。

- BUG-20260811-020（最终稽查通过，已关闭）：orphan自然结束后新PID加载；旧cancelled job未误恢复，任务、资源、Ollama与Comfy全空。

- M9.175（最终稽查通过，已完成）：专项32/32、补充8/8、关联182项及3个子测试通过；正式加载、旧orphan保护、资源与模型空闲和最终只读稽查通过。

- M9.174（最终稽查通过，已关闭）：前后端权威成功白名单统一为waiting_confirmation/confirmed/completed；pending_confirmation及failed/generating/cancelled/pending保持原卡片事实。三类资产96项动态矩阵和134项关联回归通过，媒体、确认与3D字段保持，最终稽查通过。

- M9.173（最终稽查通过，已关闭）：正式新PID披露8个唯一active/enabled扩展，contract_validated=8/8且metadata完整；任务、资源、Ollama与Comfy全空，最终关闭条件满足。

- M9.172（最终稽查通过，已关闭）：SQLite stage租约、持久generation、commit guard、Graph generation围栏与request_cancel唯一裁决均闭环；旧代晚到、纯失租、显式取消和同实例竞态已通过测试与稽查。

- M9.163（人物首格已被M9.187覆盖）：六格、Blender七角度、32B/72B、H3→Qwen‑Edit、版本与LoRA裁决继续有效；当前人物首格与TripoSR输入为0°正面全身。

- M9.160（最终稽查通过，已关闭）：基础设施注册表以`extension_point + provider_id`保存多个已安装提供方，每点只有一个active绑定；支持activate回滚、按provider启停/卸载。无效replace写入前拒绝，并发无空绑定；软件108项及3个子测试与最终稽查通过。
- M9.160边界：内置刷新采用replace_provider，不能删除插件或抢回插件活动绑定；管理接口披露active。有状态且hot_swappable=false的基础设施不得原地替换活实例，生产切换需排空、迁移、重启与恢复演练。
- M9.160测试证据：关联108项及3个子测试、失败替换原子性和8读1写并发动态验证、Python编译通过；正式8个扩展点均唯一active，资源与模型队列空闲。
- M9.159（最终稽查通过，已关闭）：生产能力唯一键升级为`capability + provider_id`，同能力可同时挂本地、远程和插件提供方；选择时过滤disabled/unhealthy，按priority并在同级确定性轮转，也支持显式provider。
- M9.159安全边界：默认提供方失败立即失败，禁止非幂等生成任务静默换模型重复执行；只有显式allow_fallback才尝试后备。内置刷新使用replace_provider，不移除插件绑定；管理接口返回priority/healthy/enabled。
- M9.159测试证据：关联105项及3个子测试、动态多提供方与200次并发轮转、Python编译通过；正式8787健康与管理接口均披露27条完整绑定，Ollama与Comfy队列空闲。

- M9.158（历史首格，已被M9.187覆盖）：六格与Qwen‑Edit人物档案能力继续保留；当前0°正面全身为首张基准及固定种子源。

- M9.157（最终稽查通过，已关闭）：资源调度器支持可插拔资源池、池容量与队列上限。短剧Mac节点将text/audit/image/video/3d/upscale放入accelerator单并发池，audio放入cpu-media双并发池，control为8并发；GPU安全互斥不变，但控制与CPU音频不再被GPU长任务阻塞。
- M9.157运行证据：池内保持优先级、超时与按job取消，满队列立即backpressure；健康快照新增active_items与pools并保留active兼容字段，worker capacity/active改为真实池容量与活动票据数。多实例仍由共享发现、健康路由和owner/generation租约负责。
- M9.157节点回收：WorkerRegistry与WorkloadRouter均按30秒心跳超时清退，服务受控重启后的旧PID投影不会长期污染管理接口或容量统计。

- M9.156（历史四格口径，已被M9.166覆盖）：保留为历史记录，不得作为当前执行依据。
- M9.156半身照：严格正面平视特写型，人物居中、头顶微小留白、腰部裁切、手部完全出画，头部至腰部约占画高75%，双肩不触边、左右留白适中、纯色无杂物；补强脸部/妆发/领口/近景/表情/口型，不作TripoSR几何输入。
- M9.156四图锁定：分镜推理与审核中0°半身为唯一`face_primary`，0°全身为`clothing_body`，90°/180°为`angle_continuity`；四图均为有效约束，人物图禁止标为`audit_only`。
- M9.156历史契约已被M9.187覆盖：当前人物基准、Klein9B元数据与后端视觉验收统一`front_full`。
- M9.156确认：0°人工确认需等ledger确认成功，失败保持待确认且90°/180°不启动；项目切换阻断旧项目晚到确认。
- M9.156角度隔离：90°/180°生成链有独立控制器与project/session围栏，旧项目90°晚到不落盘、不继续180°。
- M9.156 LoRA门禁继续有效；厚涂LoRA当前加载目标由M9.187统一为0°正面全身首图。
- M9.156恢复边界继续有效；当前0°人物建模输入由M9.187重新确立为唯一现行口径。
- M9.156历史3D门禁已被M9.187覆盖：当前请求标记`front_full`，服务端核验项目持久确认记录与精确基准URL。

- M9.155（开发完成，待软件测试）：场景/道具固定角度使用Flux.2 Klein 9B 8-bit，强制CFG 1.5、20步并回写结果元数据；人物多角度继续使用Qwen‑Edit CFG 1.0。

- M9.154（历史口径，已被M9.156覆盖）：曾采用手动上传；其project/session单飞与晚到隔离继续保留。
- M9.154项目隔离：资产提取单飞按项目/session键控，旧项目晚到成功或异常不得覆盖新项目资产、状态、错误或持久数据。
- M9.154手动上传：分镜下一步显示“上传资产图片”；资产页主操作仅提示逐卡导入，旧自动任务恢复时不续跑生图模型。
- M9.153（已完成）：正常合片迁入服务端LangGraph `composition`阶段。前端先同步镜头级视频/音频/字幕证据，再单次提交全剧可合片分集；服务端批量执行并返回待确认成片。单集人工返修保留局部接口。
- M9.153项目隔离：合片同步、提交、响应、替换与持久化均校验项目ID/session/abort，旧项目晚到结果不得写入新项目。
- M9.153批次门禁：composition执行前整批验证集数为正整数且唯一，空批、非法或重复集数均整体拒绝。

- M9.152（最终稽查通过，已关闭）：分镜后的资产提取已从前端旧接口迁入服务端LangGraph `assets`阶段。点击生成图片会先自动确认分镜、持久化并同步台账，再单次调用`runStage(assets)`返回人物/场景/道具/census；前端不再承担该阶段编排。
- M9.152确认门禁：分镜审核、持久化、台账同步或项目上下文任一失败均返回false并阻断资产阶段。
- M9.152资产入口使用single-flight Promise，重复点击共用同一事务，不会重复提交`runStage(assets)`。

- M9.151（历史人物角度已被M9.187覆盖，其他视频能力已关闭验收）：该里程碑曾以Klein 9B左45°基准进入TripoSR；当前唯一人物几何输入已改为M9.187的0°正面全身。其Blender 24FPS/96帧源视频、H3 Ref2VA、prompt ID、心跳、超时、停止、恢复、资源释放及故事弧生产门禁继续有效。
- M9.151真实3D证据：白瓷仙壶Blender worker完成1024×576、24FPS、96帧、4秒源视频，结果经确认进入hot归档；服务重启后任务、确认和视频规格保持，媒体接口HTTP 200。
- M9.151分镜生产修复：禁止Qwen3-VL-32B一次重写全集长JSON；模型仅输出512-token视觉导演方案，服务端按已确认剧本时间段确定性编译并硬校验。正式实测20镜、0—60秒、HTTP 200且模型释放；故事事实库把同集多镜归并为一个分集事实后校验，结果可持久化为待确认。错误展示优先使用服务端具体消息。
- M9.151稽查整改：新3D任务结果强制携带worker报告的`source_video_spec`；分镜遗漏任意一条剧本台词或旁白即硬失败，不再容许比例性漏词。
- 分镜对白校验锁定“说话人+正文”，禁止不同角色用相同正文冒充覆盖。

- M9.150（历史审核暂停口径，已被M9.163覆盖）：当前唯一执行口径为`qwen3-vl:32b`生成、`qwen2.5:72b`固定执行初审、最多一次修正和终审。

- M9.149（最终稽查残留整改完成，待复测）：LangGraph为唯一服务端内核；公开确认先图校验再写台账并支持补偿回滚，旧JSON checkpoint仅作图状态投影。兼容流水线取消先写LangGraph，服务重启后保持`cancelled`，并禁止approve、resume、begin和execute继续原流程。24处重负载入口在执行前和结果提交前双重校验owner/generation；五个前端入口只提交一次阶段命令，服务端接管拆批、有限重试、终审、修复与媒体包。共享发现、远端转发、25项能力、8项基础设施和三元媒体包门禁保持有效。规范见`docs/specs/短剧统一生产内核架构规范.md`。

- M9.147：对话框图片上传已接入结构化提示词反推；RealESRGAN x4图片超分、RealESRGAN x4+RIFE 4.9视频增强、MuseTalk口型同步、PuLID-FLUX FaceNet身份修正及ReActor最终修正均已接入。增强产物使用独立路径和版本，不覆盖原始资产；MuseTalk主链失败时才回退LatentSync。真实图片、视频、口型与两种身份修正烟测及26项回归测试通过。

- M9.146：`短剧3D资产生产规范.md`升级为3.0。核心人物3D脸永不进入成片，人物头部以蒙版交给2D身份链；多人镜头按摄影机深度从远到近逐层生成；TTS与动作偏差超过0.5秒退回分镜；3D空间硬错误优先修正2D，无法归类时暂停；系统级三次失败只进入人工降级审核；共享资产采用版本快照，资产ID冲突禁止静默覆盖；三机位以上锁定gaze_target。

- M9.145（历史角度口径，已被M9.163覆盖）：当时角度与Blender五角度链仅作历史记录；当前人物六格、Blender七角度及H3→Qwen‑Edit串行职责统一以M9.163为准。

## 当前主线

- 项目：可插拔、可配置、可对话操作的 AI Agent 平台。
- 当前目标：M8.10 通用对话智能体闭环增强；M11 已按用户指令暂停。
- 任务来源：根目录 `Dev_MainDev.md`。
- 长期愿景：`docs/product/VISION.md`。

## 已完成

- M9.143：本机 ComfyUI 0.31 自带的官方“一键多角度人物”模板已核对并接入。新增 Apache-2.0 的 Qwen Image Edit 2511 Multiple-Angles LoRA（295,140,688字节，SHA-256 `42426ded...6765`）与 Lightning 4步 LoRA（849,608,296字节，SHA-256 `22226e8d...904f`）；两者底模均为Qwen Image Edit 2511。苏清璃双参考右90度实图在BF16、两LoRA各1.0、4步、CFG1、Euler/simple、denoise1.0下成功生成严格纯侧面，头顶、全身与双鞋完整入框；40步质量档30分钟超时并已精确回收。正式代码候选已切为Qwen双参考独立角度工作流，旧RealVisXL+FaceID+Clothing+OpenPose+Depth叠加链路保留为隔离诊断能力；正式服务尚未重启，等待独立软件测试与稽查。

- M9.142（历史人物角度链，已被M9.163覆盖）：上述RealVisXL/IP-Adapter/OpenPose/Depth组合仅保留为历史验证记录；当前固定角度唯一执行链为Qwen-Edit多参考六格。

- M9.141：故事大纲、全剧剧本和全剧分镜脚本标题右侧已统一增加“复制全部剧集”和“引用全部剧集”图标；复制内容按剧集编号顺序包含剧名、全部集标题及全部正文，引用将同一完整文本逐行写入项目对话输入框。剪贴板写入补充浏览器兼容降级。真实网页已验证剧本完整复制1452字、首尾段落齐全、引用完整回填及图标对齐；Vue类型检查和Vite正式构建通过。

- M9.141：短剧大纲生成已补齐跨集状态闭环：分集前建立剧情阶段与不可逆变化状态表，标题、核心事件和人物姓名全剧唯一；后续批次携带全部既有分集，禁止阶段倒流、冲突重演和反派手段循环；后端直接拒绝分集数量/编号异常、重复标题及重复梗概。

- M9.141（历史文本路由，已被M9.163覆盖）：曾使用9B/27B/122B三档Qwen3.5；当前唯一口径为qwen3-vl:32b生成、qwen2.5:72b初审→最多一次修正→终审。剧情连续性、语义去重与结构化门禁继续保留。
- M9.142：官方 `stabilityai/TripoSR` 已下载至 `models/3d/TripoSR/`。`model.ckpt`大小1,677,246,742字节，SHA-256为`429e2c6b22a0923967459de24d67f05962b235f79cde6b032aa7ed2ffcd970ee`，与官方LFS记录一致；MIT许可证、来源清单和三维模型目录ADR已落盘，权重549个张量通过`weights_only`安全元数据加载。当前仅完成下载与校验，尚未接入网页三视图生产链路。
- M9.99：短剧运行数据目录已从迁移前项目 `Drama_Pipeline_ceshi/output` 改为当前项目根目录 `AI Agent/output`，避免旧目录被清理后项目、流程状态和生成按钮失效。
- M9.98（历史人物角度链，已被M9.163覆盖）：曾启用IP-Adapter锁脸锁服装及OpenPose；当前固定角度唯一执行链为Qwen-Edit多参考六格。
- M9.97：生图重负载任务已使用唯一任务ID并持久化排队、PID、进程组、心跳、超时和重试次数；超时及停止会终止完整进程树，普通失败最多重试一次，新任务启动与服务重启会回收旧状态和残留进程，独立看门狗持续检测任务/进程双向不一致。当前“符咒”道具图已解除旧人物角度任务占用并生成完成。
- M9.96：国风浅涂人物 LoRA 库已新增男性清逸出尘、温润清雅、浅染古风、漫光仙侠、淡墨清雅、雾感仙姿及女性清柔仙韵、浅涂淡颜、雾感温婉、清雅灵秀、柔光稚态、淡彩仙姿；保留男女空灵仙气、仙侠幻想，其他34个旧人物 LoRA 已移入废纸篓，正式目录16个文件与商用索引、大小和 SHA-256 全部一致。
- M9.95：人物、道具和场景资产统一为每个资产一张横向卡片；卡片内固定按基准照、后续角度照顺序横向排列并显示角度名称，窄屏保持横向滚动；导入和重新生成严格作用于当前图片槽位。
- M9.92：新建项目和项目设置已恢复“视觉风格”选择；项目 `category`、`style` 与生图风格统一使用当前所选本地 LoRA 风格，移除隐藏的“现代真人电影质感”默认覆盖。

- M9.48：全部短剧生产流程已统一为增量展示；大纲和剧本按集显示，剧本按段显示，分镜脚本按镜头显示，资产图、分镜图、分镜视频和成片按单项完成立即持久化并显示；生成期间自动展示全部已完成内容。
- M9.49：分镜脚本生成已从整集阻塞返回改为逐镜头真实调用；每个镜头生成成功后立即写入项目阶段并显示，接口已使用本地 Qwen3 8B 实测返回有效镜头。
- M9.50：资产生图任务存储已兼容旧版键值对数组并自动归一化，修复 FLUX 批量任务的列表索引错误；基础图生成顺序统一为男主角、女主角、配角、道具、场景，角色提取新增角色类型与性别字段。
- M9.51：资产页已增加每2秒实时结果回填；图片文件生成完成后会自动从任务记录回填项目资产地址并立即显示，不再等待整批任务完成或手动刷新。
- M9.52：资产页门禁改为依据实际完整分镜数据判定，修复分镜已完成仍提示“请先生成分镜脚本”；资产生成期间不再显示历史失败提示，批次结束后仅显示真实未完成项。
- M9.53：生图权威规范新增人物默认国籍与面孔规则；项目未明确指定其他国家、地区或族裔时，基准图、分镜图和视频首帧统一使用中国人身份、自然东亚面孔和中国影视角色审美，生图接口同步强制注入该规则。
- M9.54（历史三槽口径，已被M9.163覆盖）：人物基准确认按钮继续保留；确认后的当前固定角度链为Qwen-Edit多参考六格并逐槽确认。
- M9.55：“就要这张”已并入统一媒体操作组件，按钮使用与图内操作一致的深色半透明样式，白色遮罩已移除。
- M9.56：资产轮播已分离点击预览与拖动手势；轮播仅在移动超过阈值时滚动，静止点击图片直接打开统一放大预览，图内操作按钮不再触发轮播抢占；真实界面点击验证通过。

- M1：Skill 注册、独立智能体、1:1 映射、生命周期、上下文隔离、HTTP 与串联恢复。
- M2：短剧 11 节点、真实能力提供方、持久检查点、人工关口、取消、恢复和导出。
- M3：隔离任务查询、详情、取消、恢复、事件投影与前端任务中心。
- 平台基线：Python 安装入口、健康检查、身份上下文、事件总线、任务队列和插件生命周期。
- 契约基线：身份、授权、项目、任务、插件、资产、错误、事件和短剧流程契约。
- UI：原 Drama Pipeline 前端迁入 `plugins/builtin/short_drama/frontend/` 并通过平台插槽加载。
- M4.1：开发/稽查角色、模型、配置版本、对话消息和提案契约。
- M4.2：`system_main_developer` 与 `system_inspector` 内置 Skill；稽查 Skill 无写权限。
- M4.3：模型注册表、启停、能力/上下文约束、确定性选择和显式失败。
- M4.4：智能体配置创建、乐观版本更新、历史追溯、Skill 权限边界和模型能力绑定。
- M4.5：真实模型客户端驱动的对话、结构化配置/复杂任务提案、显式确认/拒绝、配置版本应用、任务执行入口及稽查只读约束。
- M4.6：模型注册/查询、智能体配置创建/更新/历史、对话消息与提案确认/拒绝 HTTP 路径及 OpenAPI 登记。
- M4.7：开发/稽查 Skill 选择、模型注册/切换、配置版本、对话和提案确认/拒绝前端界面。
- M4.8：角色隔离、模型切换、配置版本、复杂任务确认执行及无真实模型显式失败集成测试。
- M4 最终验证：相关功能验收通过；当前全项目累计 131 项 Python 测试通过。
- M5.1：模型与媒体提供方统一配置、密钥引用、限流配置和健康状态契约。
- M5.2—M5.8：OpenAI 兼容客户端、取消/超时/结构化校验、四类提供方注册、限流重试熔断降级、用量成本产物审计、短剧真实提供方绑定、HTTP/前端和生产集成测试。
- M5 最终验证：151 项 Python 测试、提供方 TypeScript 契约编译、Vue 类型检查和 Vite 正式构建通过。
- M6.1—M6.8：插件签名/SBOM 契约、安装前校验、文件/进程/网络/数据隔离、工具授权与脱敏、哈希链审计、供应链门禁和插件管理界面。
- M6 最终验证：163 项 Python 测试、SBOM 门禁、TypeScript 契约编译、Vue 类型检查和 Vite 正式构建通过。
- M7.1—M7.8：协作契约、只读稽查、整改回流、隔离、LangGraph 串并行/重试/暂停恢复/人工接管、HTTP、前端和闭环测试。
- M7 最终验证：166 项 Python 测试、SBOM 门禁、Vue 类型检查和 Vite 正式构建通过。
- M8.1—M8.10：11 节点/LangGraph 检查点契约、真实前期链路、媒体版本与局部重做、视频双模式、音画时间线、审核修复确认导出、工作台、持久恢复测试、中文 LoRA 项目锁定和上下文驱动通用对话闭环。
- M8 最终验证：199 项 Python 测试、SBOM 门禁、Python 编译、Vue 类型检查和 Vite 正式构建通过。
- M9.1—M9.8：行业/流程/Skill/机器人契约、通用 manifest 注册、租户项目隔离机器人、独立模型版本、LangGraph 拓扑、对话式配置、通用界面、短剧首模板和第二行业扩展验证。
- M9 最终验证：184 项 Python 测试、SBOM 门禁、Vue 类型检查和 Vite 正式构建通过。
- M10.1—M10.8：三环境模板、容器/健康/安装、持久化适配、日志指标链路告警、备份恢复、升级回滚、故障容量测试和运维文档。
- M10 最终验证：196 项 Python 测试、SBOM 门禁、Python 编译、Vue 类型检查和 Vite 正式构建通过。
- M9 产品范围已确认：全行业一个业务流程对应一个独立 Skill 机器人，每个机器人可独立设置模型；短剧作为首个行业模板。
- M8.9 产品范围已确认：新建项目和项目设置提供中文 LoRA 风格设置；默认自动确定性匹配并锁定，支持固定选择、手动覆盖和主动探索模式。
- 平台界面自适应修复：任务中心、能力提供方和插件管理已改为独立浮层；主工作区恢复全视口显示，并完成 760px、1280px、1920px 宽度验证。
- 本地开发 API 路由已分流：平台 `/api/v1` 接入 8080，短剧 `/api` 接入 8787；“新项目”创建、界面写回及清理接口均返回 HTTP 200。
- “生成图片/继续生成图片”已修复为立即进入“分镜画面”阶段；前置确认不足时在目标阶段明确显示门禁信息，满足条件后直接启动镜头图生成。
- M8.10：机器人通用对话闭环已完成，支持 Skill/项目/任务上下文、持久化长期偏好记忆、必要澄清控制、Skill 选择校验、结构化计划和确认式变更；199 项 Python 测试、供应链门禁、Python 编译、Vue 类型检查及 Vite 构建通过。
- 图片生成门禁已修复：分镜未确认时停留在分镜步骤并显示复检错误；资产未确认时自动进入资产步骤；错误详情恢复可见，禁止提前提交镜头图任务。
- M9.9：短剧 Skill 已按六类机器人接通前后端；支持一键启停、项目隔离机器人、自动模型策略、持久化恢复和执行门禁；当前项目六类机器人已全部启用并通过真实界面验证。
- M9.10：每个短剧 Skill 机器人卡片已增加“模型与 API”入口；支持云端提供方、密钥引用、模型注册、自动选择及独立模型绑定，并通过真实界面和接口验证。
- M9.11：Skill 模型配置已增加本地模型入口，支持 Ollama、LM Studio、LocalAI 和其他 OpenAI 兼容服务；本地 HTTP 仅允许 `localhost`/回环地址。
- M9.12：审核机器人界面已精简为省成本、均衡、高质量三档方案；审核页直接提供初审/终审切换，文本、视觉、音频可分别选择不同模型，不再显示通用“使用模型”。
- 短剧 Skill 界面名称已统一移除“机器人”字样，历史项目配置加载时自动兼容旧名称。
- “审核”开关已统一接管文本、图片、视频和音频自动审核；关闭后中止进行中的文本审核并直接进入人工确认，不再触发初审、终审或自动修复。
- 剧本完成后的“生成剧本”按钮已替换为“生成分镜”，点击后确认当前剧本并直接进入分镜生成。
- FLUX.2 MLX 启动入口已修复受限工作目录导致的导入失败；本地 256×256 真实图片生成通过，前端错误信息不再泄露原始 Python 堆栈。
- 短剧生产永久取消自动审核与自动推进；大纲、剧本、分镜、资产和镜头均由用户逐步人工确认，只有当前节点人工确认完成后才显示并允许手动启动下一节点。
- 资产页已支持从本地生成任务结果自动恢复遗漏的图片状态，并为生成中/待生成卡片提供稳定占位布局；当前项目 9 张资产图已全部回填为待人工确认。
- M9.13：模型配置已重构为两层选择；第一层选择当前机器人、初审或终审，第二层选择本地或云端，模型均使用预设下拉框，仅服务地址手动填写；三类绑定按项目独立保存。
- M9.14：进入机器人模型配置后 Skill 分类左栏自动收起为返回栏；返回按钮已移入左栏，返回机器人列表后左栏自动恢复。
- M9.15：本地模型和云端模型已改为互斥二选一；切换来源会取消当前角色的旧来源绑定，每个主机器人、初审或终审最终只保存一个模型。
- M9.16：本地与云端模型的独立接入按钮已移除；统一使用“保存当前配置”自动完成模型注册和机器人角色绑定。
- M9.17：已发现并注册本机 Ollama 的 Qwen2.5 72B、Qwen3 32B、DeepSeek R1 70B、LLaVA 7B；六类机器人及各自初审、终审已写入项目级默认本地模型绑定。
- M9.18：主模型、初审和终审已使用独立模型草稿及持久化键；切换角色不会继承上一角色的下拉值，保存后分别恢复各自模型。
- M9.19：Skill 列表、类型切换、模型来源和保存按钮已统一为白底、灰色描边、深色文字样式，选中状态使用浅灰底区分。
- M9.20：模型保存已修复；项目绑定先持久化，平台重启后自动重注册所选模型，作用域行业机器人可被配置服务解析，保存结果已通过项目阶段和机器人配置 API 双重验证。
- M9.21：模型配置已增加文本、视觉、音频、视频类别；项目策划和编剧默认筛选文本模型，视觉资产和视频制作默认筛选视觉模型，未检测到的音频/视频本地模型明确标记不可用。
- M9.22：本地视觉模型目录已补充 LLaVA、Qwen2.5-VL、Gemma 3 Vision、MiniCPM-V，并区分已安装/未安装；Skill 启用状态使用浅绿色，停用状态使用灰色。
- M9.23：Mac 快速跑通配置已完成；Qwen3 8B 已安装并验证响应，CosyVoice2 0.5B 与 Wan2.1 T2V 1.3B 已下载，项目策划、编剧、后期制作使用 Qwen3 8B，视觉资产与视频制作使用 LLaVA，审核机器人及全部初审/终审绑定已关闭。
- M9.24：Skill 卡片的“模型与 API”入口及其配置子界面已移除，界面仅保留流程启停，现有项目模型由系统自动配置。
- M9.25：人物、场景、道具、细节图和分镜图片已接入统一点击放大预览，支持键盘回车打开、缩放和前后切换。
- M9.26：短剧 8787 持久化兼容服务已恢复；历史项目、剧本、分镜、资产阶段与结果媒体重新接入，当前“都市女王的逆袭”项目和 9 张历史图片已验证可读取。
- M9.27：故事总纲与分集梗概生成接口已恢复，Qwen3 8B 结构化输出已通过真实接口测试，页面不再返回 `not_found`。
- M9.28：项目对话、历史记录、草稿、会话及能力查询接口已恢复；历史消息可读取，Qwen3 8B 对话响应已通过真实接口测试。
- M9.29：剧本单集与全剧分镜兼容接口已恢复，Qwen3 8B 真实剧本和连续时间轴分镜生成通过；页面不再显示 `not_found`。
- M9.30：资产提取、FLUX.2 MLX 人物/场景/道具与分镜图片生成接口已恢复；资源保存、项目版本和人工生产确认接口已补齐。
- 资产全部人工确认后，资产页右上角显示“生成分镜”按钮，由用户手动启动分镜画面生成，不自动推进。
- 资产历史任务的 `not_found` 失败状态已自动校正：已有有效图片时统一恢复为待人工确认并清除错误提示；当前项目资产失败项为 0。
- 资产页已移除人物、道具、场景下拉框，统一改为三个分类按钮；点击分类仅显示对应类别的资产图片。
- 分镜脚本完成后自动提取人物、道具和场景并建立全部资产图片框；空框显示“等待生图”，生成中的图片框显示“正在生图”；资产页顶部状态标题已移除。
- 图片放大预览在普通移动鼠标时保持不动；滚轮缩放时以鼠标所在位置为缩放中心。
- 全部流程顶部已统一为“流程名称　No:当前/总数　Time:耗时”格式；资产分类按钮移入内容区。
- 五个短剧 Skill 固定默认开启；每张卡片右上角开关改为该流程的“是否审核”开关，项目级独立保存，默认关闭。
- 大纲、剧本、分镜脚本、资产、分镜画面、分镜视频、成片和导出均使用项目级独立真实计时；开始生成时从 0 计时，结束后冻结实际耗时，不再共用累计时间。
- 视频运动策略固定为 70% 运镜、30% 人物或主体动作；分镜生成和视频任务提交均携带该比例。
- 历史分镜人物链（已被M9.163覆盖）：IP-Adapter Plus Face SDXL仅保留为历史验证能力；当前分镜人物一致性必须消费已确认Qwen-Edit六格人物档案，不属于单镜LoRA强度微调例外。
- 分镜视频任务增加进程内活动登记和中断恢复：服务重启遗留的 `generating` 状态会自动转为可重试失败，继续生成会重新提交，不再永久卡住。
- M9.29：人物、场景和道具资产卡片已改为双列等宽 9:16 大图布局，图片下方统一显示“导入图片”和“重新生成”，窄屏自动切换单列。
- M9.30：资产图片的“导入图片、重新生成”已移入图片内部底部；左上角新增“图片简介”，点击后在图片内显示名称、简介、状态和确认操作浮层。
- M9.31：资产、分镜画面和分镜视频已取消单张确认；点击下一流程按钮自动确认当前已生成批次，“生成分镜视频”可直接启动视频生产。
- 分镜画面页已新增“生成分镜视频”直达按钮；点击后自动保存已生成镜头、切换分镜视频页并立即启动视频生成，未完成镜头仍保留“继续生成”。
- M9.32：分镜视频接口已恢复并接入本地 Wan2.2；第1集镜头1真实视频已生成并通过 HTTP 200 媒体验证，历史 `not_found` 和卡住状态已清除。
- M9.33：本地 Qwen3-TTS 配音、MuseTalk 主口型、LatentSync 1.6 备用口型、CampPlus 声纹检查、情绪检查、口型完整性检查及音频混合接口已全部接入；真实音频、MuseTalk 视频和 LatentSync 视频均已生成验证。
- M9.34：OpenPose 全身、手部和面部骨骼提取已接入；SDXL OpenPose ControlNet 已与 IP-Adapter 人物身份锁定联合绑定，真实骨骼图和受姿态约束的人物图均已生成验证。
- Skill 左侧“软件开发”分类仅保留“代码稽查、主力开发”两个默认开启 Skill；两者均已保存独立 Skill 清单和系统提示词文件。
- 软件开发双 Skill 已接入协作闭环：主力开发提交交付物，代码稽查只读检查；不通过时自动生成整改任务交回主力开发，整改后可再次提交复验，超过轮次转人工处理。
- ComfyUI 的 79GB 共享模型、输入和输出目录已统一迁移到 `/Users/aoo/AI/ComfyUI-Shared`；短剧服务、ComfyUI 启动器、批处理和预览脚本均已切换新路径，旧根目录别名已移除。
- 任务列表已接通实时项目流程、单镜头任务和视频作业状态；任务窗口每2秒刷新，实时显示排队、运行、完成、暂停与完整失败信息，不再使用旧生产记录覆盖当前状态。
- M9.35：Wan2.2 分镜视频已增加持久化内存等待队列和5秒实时监控；macOS 使用系统内存压力的可回收内存判定，内存不足时保留任务，达到安全阈值后自动串行续跑，服务重启后自动恢复等待任务。
- M9.36：项目对话已接通任务意图识别、主力开发/代码稽查 Skill 自动路由、连续对话上下文、全部项目 Markdown 规范注入、Codex 文件/终端/测试工具执行和真实验证闭环；不再要求用户手动提供项目代码。
- 资产页的“人物、道具、场景”分类按钮已移入顶部 No/Time 同一行最右侧，距右边缘 10px。
- M9.37：系统 AI 对话已改为持久化异步任务，前端每 2 秒查询心跳；单条请求不再阻塞后续消息，31 分钟超时自动释放，服务重启自动恢复排队/运行任务，未回复历史消息自动续办，请求 ID 防止重复执行；“修复/解决/整改/处理”优先路由主力开发。
- M9.38：合成成片前已增加分镜级强制门禁；有对白镜头必须先完成配音、口型同步和字幕文本/时间轴，全部镜头完成后才允许合片；前端按钮与后端合片接口同时校验。
- M9.39：成片阶段已接入项目内自动生成 BGM、对白与 BGM 混音、字幕烧录顺序门禁；默认 BGM 音量为 18%，口型输出强制封装对白音轨，字幕重烧继续保留 BGM 和对白。
- 所有短剧流程操作栏已永久隐藏“未生成、待确认、已生成、未合片、未导出”等静态状态文字，仅保留真实运行中的动态进度提示和错误信息。
- 所有短剧流程操作栏的“导入”按钮已永久移除；单项素材替换能力保持不变。
- 三栏工作区顶部已按实时屏幕宽度自动压缩左右栏，窄屏自动隐藏预览栏；流程标题、No/Time、资产分类和操作按钮支持换行且不再横向溢出。
- 成片合并已改为逐镜头强制生成一致的 H.264/AAC 音视频流：对白镜头使用原始 TTS 音轨，无对白镜头补静音，再串联合片和混入 BGM；口型主流程已由 MuseTalk 切换为 LatentSync 1.6，旧口型产物会在再次生成成片时自动重做。
- “生成成片”已修复旧成片记录导致的重复生成门禁误判；每次点击会清理旧成片状态并重新执行合片，不再显示“配音、口型同步或字幕尚未完成”的错误提示。
- 成片混音已将对白提高 6dB，关闭 FFmpeg 自动归一化衰减，并使用侧链压缩在对白期间自动降低 BGM；BGM 已由固定持续音改为四段和弦循环，默认音量调整为 14%。
- 可覆盖媒体已关闭浏览器缓存并增加文件修改时间版本号；重新生成的同名分镜、口型或成片会立即加载最新文件，不再播放一小时内的旧缓存。
- 资产固定角度生产恢复强制门禁：第一张为定位基准图，仅生成定位基准图后等待逐项确认；确认后才允许引用该图串行生成其余固定角度，禁止未确认基准图提前生成后续角度图。
- M9.57：分镜脚本完成后的下一流程按钮统一为“生成图片”；点击后自动归档并清空当前项目旧人物、道具、场景资产数据，跳转资产界面，重新提取最新分镜资产并立即开始按顺序生成定位基准图。
- M9.58：分镜逐镜头生成已增加剧本段落进度约束、四次差异化重试和重复内容自动推进兜底；当前“逆光而行”第1集已在真实界面续生成完成15个连续镜头，状态恢复为完成并显示“生成图片”，不再因第11镜头重复而中断。
- M9.59：资产基准图任务已增加每次生成独立随机标识，历史同名任务不再回填到新批次；当前“逆光而行”点击“生成图片”后已真实验证旧资产清空、自动跳转资产页、重新提取14项资产并从0开始生成首张新定位基准图。
- M9.60：人物正面近景基准光照规范已统一为正前方柔和漫射平光、面部均匀通透、下颌颈侧浅阴影、服装保留层次、纯色哑光中性灰背景，并禁止硬阴影、侧逆光、强对比、光斑和渐变。
- M9.61：人物正面近景基准面部优化统一为弱化额头高光与下颌阴影、统一瑕疵、补充柔和眼神光、校正眉眼对称并保持面部亮度均衡。
- M9.62：大纲强制增加“核心人物简介”模块并固定显示在剧名下方、故事总纲与分集剧情之前；每人统一为“姓名｜身份｜性格｜核心动机”，生成结构、运行规范和前端展示已接通。
- M9.63：资产“图片简介”弹层已改为图片内部自适应定位，避免窄栏和首列卡片中向左越界或被滚动容器裁切；真实界面点击、可见性及边界包含验证通过。
- 主线开发永久新增修复回归强制规则：每次修复后必须识别并逐项实测所有可能受影响的关联功能、上下游流程、接口、状态、持久化与界面交互；未完成真实回归验证不得报告已修复。
- M9.64：全流程“生成下一流程”统一为仅清空紧邻下一流程的旧内容、媒体、状态和错误，保留上一流程及其他流程；清空后自动跳转并立即生成。分镜脚本进入资产已真实验证为旧资产即时归零、重新提取17项资产并自动启动首张定位基准图。
- M9.65：分镜脚本完成后自动理解并提取人物、道具、场景，在资产页预建简介和空图片位；点击“生成图片”只清空资产图片、角度图及生成状态，保留本次分镜提取的资产结构与简介并自动生图。
- M9.66：资产生图状态已绑定实际生成图片键；“此图正在生成中”和停止按钮只显示在当前图片卡片，其他角度及其他资产保持等待状态。真实界面已验证仅首张人物基准图显示生成状态，后台生图进程持续运行。
- M9.67：资产批量生图不再自动切换人物、道具、场景分类；后台完成图片只原位更新对应卡片，保持用户当前分类与查看位置。真实验证停留道具页期间人物及道具图片连续完成，页面始终未跳转。
- M9.68：新建项目界面已移除“中文 LoRA 模式”设置，后台继续使用自动题材匹配；人物定位基准照恢复“就要这张”按钮，每张已生成且待确认的基准照可直接确认并生成固定角度图。
- M9.69：大纲人物简介新增层级唯一性规范：全剧仅允许1名第一男主，其余男性必须标注男二号、盟友、反派等定位。
- M9.70：都市逆袭类人物简介强制单列女主；兼任其他职能时使用“女主/职能”双标签，女配不得替代女主。
- M9.71：人物简介规范已合并去重为固定格式、男女主岗位唯一、兼任双标签、正反派褒贬措辞适配和软件测试 Skill 优先验收；大纲模型提示同步执行。
- M9.72：道具和场景资产卡片统一为每行2张，单卡图片占满列宽；窄屏保持单列自适应。
- M9.73（历史人物角度链，已被M9.163覆盖）：曾使用三槽OpenPose/IP-Adapter；当前为Qwen-Edit多参考六格，尺寸与头脚完整等验收规则继续保留。
- M9.74：全部资产定位基准照生成队列已与资产页面从上到下的显示顺序完全统一，移除按主配角和性别重新排序；基准照严格逐张串行生成，上一张结束后才启动下一张，规则已同步写入生图规范。
- M9.75：道具图已强制禁止真人和真实人体，仅允许道具本身为雕像、雕塑、人偶或玩偶；生成后增加本地视觉真人检测，不合格自动重试。资产基准图队列改为首项失败立即暂停，后续保持待生成，界面显示具体资产和真实错误，不再将剩余资产连续标记失败。
- M9.76：资产“生成图片”按钮失效已修复；8787短剧服务恢复持续运行，资产生图任务在提交时立即持久化运行状态并在失败时保存真实错误，页面加载时自动释放服务中断遗留的无效生成状态，按钮不再被残留状态永久锁死。
- M9.77：已建立`models/loras/国风浅涂柔光治愈仙侠淡彩风/`目录，按国风厚涂同结构分为风格LoRA与人物LoRA；下载并登记5个风格、20个女性、20个男性SDXL LoRA，共45个、694MB，许可限定MIT/OpenRAIL++，全部完成Safetensors头、数量、大小、SHA-256和商用授权索引校验。
- M9.78：`国风浅涂柔光治愈仙侠淡彩风`目录新增与人物平级的`灵兽LoRA/`，下载并登记20个传说灵兽、神兽、仙宠和奇幻生物SDXL LoRA，共587MB，许可限定MIT/OpenRAIL++，全部完成Safetensors头、数量、大小、SHA-256和商用授权索引校验。
- M9.73：资产人物预制框强制覆盖大纲核心人物简介全部人物；资产提取请求携带完整人物清单，模型漏项时前端自动补齐并持久化，历史项目加载时同步修复缺失人物框。
- M9.74：补齐现代写实人像 LoRA；现代真人角色自动使用现代写实人像模型，权重0.75，场景和道具继续使用写实基础模型并避免误用人像 LoRA；FLUX.1-dev 开放镜像与 Diffusers MPS 兼容后端已接通，生成结果持续记录实际 LoRA 元数据。
- M9.75：已建立 `models/loras/国风厚涂/` 商用 LoRA 总目录；下分 `风格LoRA` 与 `人物LoRA` 两个子目录，收录高下载量国风厚涂风格5个、男性人物10个、女性人物10个，文件统一使用中文＋拼音＋SDXL命名；25个 safetensors 均完成头部、数量、许可证、来源、大小和 SHA-256 校验，明细写入 `models/loras/商用LoRA索引.json`。
- M9.76：外购 LoRA 工业规范已合并去重并写入 `docs/specs/LORA_ASSET_SPEC.md`，统一风格/角色物理隔离、单项目固定组合、权重区间、商用准入、资产归档、版本锁定、测试、稽查和异常处理规则。
- M9.77：全部生图请求已强制读取 LoRA 主索引和商用索引；实际 LoRA 由索引解析并校验登记、文件路径、SHA-256、触发词和底模，硬编码文件路径已移除，校验失败直接阻止生成。
- M9.78：新建项目“题材类型”已改为实时索引 `models/loras` 顶层风格目录；后端每次请求扫描实际 safetensors，界面打开期间每2秒同步，目录新增或删除后选项自动增减并自动修正失效选择。
- M9.79：“就要这张”点击反馈已改为同步生效；点击后立即关闭简介、隐藏确认按钮、将当前人物切换为生成中、在首张待生成角度图原位显示进度并提示已确认，后台继续执行确认登记和固定角度生产。
- 主力开发与软件测试 Skill 已统一新增根因排查规则：问题处理不得停留在表面故障点，必须追溯根因、扩展检查同类场景及上下游影响，一并整改并完成防复发回归。
- M9.80：人物定位基准照“就要这张”显示条件已改为依据基准图和固定角度的真实状态判定，兼容历史失败与恢复状态；未确认基准照恢复按钮，已进入角度生成或已完成时自动隐藏。
- M9.81：“就要这张”不再受历史固定角度数据影响；所有已有基准图且未完成、未生成中的人物卡片都显示按钮，点击后继续补齐缺失角度图。
- M9.82：固定角度图停止操作已改为精确终止当前角度任务，同步释放生成状态并恢复基准照“就要这张”按钮。
- M9.83：人物定位基准照已增加生成后视觉验收；固定风格 LoRA 继续全剧锁定复用，但纸伞、花木、户外场景、手部、道具、腰腿、非正面或非纯灰背景等违规结果会立即删除并在原槽位串行重试，连续三次不合格时暂停并返回真实验收结果。
- M9.84—M9.85（历史人物角度链，已被M9.163覆盖）：IP-Adapter服装参考仅保留为历史验证能力，不再作为当前固定角度执行链。
- M9.86：已根治剧本固定5秒分段与分镜均分时间轴；每集在60-70秒内依据剧情稳定确定实际总时长，剧本按内容强制分配2-9秒且至少3种段长，分镜继承同一总时长并采用非等长镜头节奏，加载、恢复与完成校验统一使用逐集真实时长。
- M9.87：分镜逐镜头生成已实现可靠断点续传；刷新、连接中断或单镜失败后保留连续有效前缀，从首个缺失镜头继续补齐，生成中半成品不再触发15-23镜头成品门禁，只有时间轴或内容前缀损坏才重建当前集。
- M9.88：所有生产流程标题栏右侧已统一接入集数下拉组件，支持“全剧集/第N集”；选择单集后，大纲、剧本、分镜脚本、分镜画面、分镜视频和成片列表仅显示对应集内容。
- M9.89：现有LoRA与原生MLX矩阵不兼容，LoRA生图保留FLUX.1-dev兼容通道并由20步优化为8步、文本序列限制256；全剧年代画风强制优先锁定LoRA家族，同一资产请求去重，保持928×1664、LoRA权重、串行生成和视觉验收不变。
- M9.83：新增 `docs/specs/styles/国风浅涂视觉生成规范.md`，固化国风浅涂的画风、光影、色彩、肤质、提示词、女主人设、运镜、场景、灵宠、LoRA 公式、锁参、自检和异常处理规则；LoRA 通用规范与短剧生产规范已建立强制索引。
- M9.84（历史三槽口径，已被M9.163覆盖）：原正/侧/背三槽已扩展为Qwen-Edit六格；身份、发型服装、方向、头身比及头脚完整验收继续有效。
- M9.87：分镜脚本“继续生成”已改为保留当前集全部已有镜头，仅按镜头编号补生成缺失镜头；续生成不再先清空未完成集数据。
- M9.88：流程顶部 `Time` 已统一显示该项目当前流程的累计实际运行耗时；继续生成、暂停恢复和再次运行均在历史耗时上累加，不再重新归零。
- M9.89：资产页人物、道具、场景分类按钮已显示各类别需要生成的图片总数，按资产数量乘以该类别固定角度数实时计算；按钮遮罩同步加宽并保持窄屏适配。
- M9.88（历史人物角度链，已被M9.163覆盖）：曾改为FLUX.2 Klein 9B三视图；当前为Qwen-Edit多参考六格。身份验收与禁止宣称像素级锁脸继续有效。
- M9.89：全流程下一阶段门禁已统一改为单集最小完整输入：大纲、剧本、分镜脚本任一完整集即可进入下一阶段；资产按该集分镜实际引用的人物、道具、场景全部有图后即可生成分镜画面；一集全部分镜画面完成即可生成视频，一集视频、配音、口型和字幕齐备即可合片，一集成片即可进入导出。未完成的其他集继续原流程生成，不再阻塞已完整集向后生产。
- M9.90：统一流程操作栏已允许生成任务运行期间同时显示“生成下一流程”按钮；大纲门禁只校验任一集标题与剧情完整，不再被尚未完成的全局字段和全剧生成状态隐藏。实时 Vite 页面已确认按钮条件代码生效。
- M9.91：资产清点已改为严格按当前已完成集数提取，不再混入全剧其他集；人物按本集实际出场名单强制补齐，场景按实体地点归并，道具排除服装、环境与光效。当前项目第一集已修正并通过真实界面验证：人物4个、道具1件、场景2个。
- M9.92：资产页分类角标已由“固定角度图片总数”改为“资产项目数”，当前真实网页显示人物4、道具1、场景2；资产清点增加版本与集数标记，后端拒绝旧页面继续用过期资产清单覆盖新结果。
- M9.93：剧本页已统一使用 WorkflowActionBar 常驻显示“生成分镜脚本”；存在剧本即显示，内容尚未完整时禁用，完整后可直接进入分镜脚本并自动生成。真实网页验证按钮可见且可用。
- M9.94：人物定位基准图改为肩部以上大头像、纯中性灰背景、人脸占画面60%-70%，验收失败自动换种子重试5次；界面不再暴露视觉模型原始 JSON，当前失败项已重置为可继续生成。
- M9.91：资产生图中断假死已修复；后端自动识别服务重启后遗留的无效生成任务，前端轮询回收失败状态并仅自动续跑一次未完成图片；运行中状态与流程累计计时保持同步，不再出现图片卡住且 Time 停止。
- M9.92：人物、场景、道具定位基准照已统一切换为本地 FLUX.2 Klein 9B 量化生成；基准照不再加载不兼容的 FLUX.1/SDXL LoRA，画风由项目锁定提示词控制。真实接口已生成并返回 9B 模型元数据与图片。
- M9.93：资产重复点击冲突状态已修复；同一资产已有真实后台任务时，前端读取后端返回的活动任务编号并恢复对应 nonce、生成中状态和流程计时，不再把“正在生成”误显示为红色失败。
- M9.94：9B 基准照流程已根治重复生成慢与失败循环；FLUX.2 Klein 9B 改为4步推理，移除每张图后串行加载视觉模型及最多5次整图重生成，普通失败不再被当作服务中断自动续跑。928×1664人物基准照真实接口耗时由77秒降至19秒并成功出图，类型检查和正式构建通过。
- M9.95：国风浅涂人物目录新增两个 FLUX.2 Klein 9B 仅测试 LoRA，分别按“男性_清逸出尘”“女性_清柔仙韵”命名；Safetensors、SHA-256、9B 元数据和 224/224 权重键加载通过。两者均按非商用隔离，不进入商用索引；本机加载后推理未产图，暂不接入正式流程。
- M9.96：国风浅涂已补齐男性5个、女性5个、风格5个 FLUX.2 Klein 9B 仅测试 LoRA；15个文件均通过9B元数据和224键校验。基准照正式测试分支已绑定项目固定风格LoRA与按性别/角色固定人物LoRA，真实接口同时加载两个 LoRA 并成功出图；全部保持非商用隔离。
- M9.97：本机文生图底模已按用户要求仅保留 `/Users/aoo/AI/ComfyUI-Shared/models/diffusion_models/flux1-schnell-Q8_0.gguf`；其他文生图底模及缓存共145GB已移入废纸篓隔离目录。图像编辑、ControlNet、IP-Adapter、LoRA、视频、音频、口型和审核模型均未改动。
- M9.97：Klein 9B 双 LoRA 已增加物理权重去重；风格目录与人物目录指向同一来源权重时禁止重复叠加，并从同类别其他人物 LoRA 稳定补选。林婉儿实际任务已加载“橙彩插画”风格与“女性_淡彩仙姿”人物 LoRA，16秒成功生成并返回完整绑定元数据。
- M9.98：已下载 Apache-2.0 的 FLUX.1 Schnell GGUF Q8_0（12,687,821,728字节，SHA-256 `f6694941...f10`），底模仅放在 ComfyUI `diffusion_models` 正式目录；20个 FLUX.1 LoRA 测试入口已直接放入 `models/loras/国风浅涂/` 上一层现有分类目录（人物10个、灵兽5个、风格5个），多余的 `Schnell_GGUF-Q8_0/` 嵌套目录已删除。
- M9.100：FLUX.1 Schnell GGUF Q8_0 已接通正式资产基准照接口；ComfyUI-GGUF、Apache-2.0 的 CLIP-L、T5-XXL FP8 与 FLUX.1 VAE 已安装并纳入共享模型目录，启动白名单已启用 GGUF。项目按 ID 固定1个风格 LoRA，人物按项目、角色与性别固定1个人物 LoRA，并按物理权重去重；真实512×768接口双 LoRA 出图18秒完成并返回完整模型与 LoRA 元数据。
- M9.99：生图服务关闭新增原子门禁，关闭锁内先封禁新任务，模型监督进程仅可在同一锁内完成门禁检查、启动和登记；快照后不再产生无登记重负载进程。国风浅涂正式目录、运行代码、下载脚本和41条商用LoRA索引已统一为`models/loras/国风浅涂/`，20个非商用测试权重保持索引外隔离。
- M9.101：资产基准图已接通两段式独立串行链路；FLUX.1 Schnell 单独完成生成并将中间成品保存到`output/images/intermediate/`、释放模型后，Qwen Image Edit 2511 单独读取该文件，通过真实噪声蒙版执行局部瑕疵修复并输出最终图。两套模型使用不同ComfyUI提示图与prompt ID，服务任务持久化`schnell_generating/qwen_repairing`阶段、prompt ID和心跳，并保持独立模型生命周期；人物、道具、场景角度图均读取已确认基准图，禁止嵌套为同一工作流。
- M9.102：按用户指令清除`models/loras/`全部98个LoRA权重并同步清空模型索引与商用索引；4.1GB文件已移入`/Users/aoo/.Trash/AI-Agent-all-loras-20260809/`，可恢复。LoRA风格接口当前返回空列表，Schnell只能使用无LoRA基础模型。
- M9.103：`models/loras/国风浅涂/`已重新安装20个可商用FLUX.1 LoRA，风格、女性、男性、仙宠各5个，文件名全部带“仙”；全部通过MIT/OpenRAIL++许可、FLUX.1-dev底模cardData与tag双门禁、Safetensors FLUX LoRA权重键、大小和SHA-256校验，并接入Schnell扫描。基准图Qwen局部修复由40.86GB BF16切换为20.53GB FP8Mixed并降至8步；停止、重启、看门狗及失败轮询同步取消Comfy prompt，已清除任务失败后仍占队列的幽灵Qwen任务。
- M9.104（历史三槽口径，已被M9.163覆盖）：独立Qwen Image Edit链继续沿用，但当前输入扩展为六格并逐槽确认；任务持久化、头顶完整与脚部可见门禁继续有效。
- M9.105：剧本任务已接入唯一任务ID、服务端真实活动线程登记、持久心跳、330秒超时、停止及服务重启回收和独立看门狗；前端中断状态会立即持久化为失败，空剧本占位不会再阻塞续生成，只补齐缺失集且普通失败最多重试一次。当前“宗门小师妹，全宗门抢着护我”第1集剧本已完整保存，生成分镜脚本门禁已满足。
- M9.103：生图任务增加 ComfyUI prompt 级防残留机制。重新生成、资产清理、服务启动恢复、异常退出和正常关闭均按持久化 prompt ID 定向取消本流程任务；启动清空 Qwen 临时参考图与蒙版，成功或失败均释放输入和模型，防止旧图及幽灵任务污染后续生成。
- M9.104：项目阶段持久化改为事务锁内按阶段合并，禁止旧副本覆盖整个`stage_state`；每次写入前保留最近50份有效快照，项目版本与回滚真实落盘，主文件损坏时自动读取最近有效快照。
- M9.94：人物 LoRA 已接通项目内唯一绑定；全剧继续固定一套风格 LoRA，每个人物按项目风格和性别独占一个商用 SDXL 人物 LoRA，文件名、路径、SHA-256 与底模持久化到人物档案，重复占用直接阻断。当前项目林婉儿、陆云渊、苏青梧已分别绑定三份不同人物 LoRA。

## 当前约束

- M9.146：`models/loras/国风厚涂/`只保留原生FLUX.2 Klein 9B候选：风格2、女性1、男性1、灵兽1；旧FLUX.1已迁移到缓存退休目录。5项均完成9B 8-bit真实加载出图，仅古代幻想风格达到国风厚涂候选标准，其余因偏平涂、伪文字或非神兽表现禁止进入生产索引。
- M9.146接入：国风厚涂Klein‑9B基准图正式加载已通过的古代幻想风格LoRA；男女与灵兽候选已完成技术接线，但逐模型`production_approved`门禁因视觉或许可不合格而将其排除。生产命令使用`--lora-paths/--lora-scales`，加载前强制校验索引、大小和SHA‑256，返回元数据记录实际权重。

- M9.140（历史分镜人物参考口径，已被M9.163覆盖）：曾仅让0°近照进入人脸推理、侧背图只参与终审；当前Qwen-Edit多参考六格均按已确认状态进入人物档案、生成与审核链。其InsightFace身份及画面一致性门禁继续保留。

- M9.134（历史人物角度链，已被M9.163覆盖）：FaceID/IP-Adapter三视图方案仅保留为历史验证记录；当前固定角度唯一执行链为Qwen-Edit多参考六格，人工确认和身份验收门禁继续保留。

- M9.133（历史人物角度链，已被M9.163覆盖）：OpenPose/Depth/IP-Adapter三视图方案已废止；当前为Qwen-Edit多参考六格，原尺寸、比例、方向与身份服装机器门禁继续保留。

- M9.129：道具资产必须是单个独立可拿取实体的纯中性灰产品图；场景、建筑、商店、街道、房间、家具、招牌、文字、人物或人体任一出现即禁止放行。道具基准图不加载人物风格 LoRA。
- M9.128（历史上下各5%口径，已被M9.187/M9.189覆盖）：当前六格人物链的全部全身角度统一执行发顶至少8%、鞋底至少3%的非固定安全区下限，任一未达标禁止放行。
- M9.127：两份 FLUX.1 风格 LoRA 已获准直接使用：国风浅涂项目的 Schnell 基准图加载“国风仙韵”，国风厚涂项目加载“油画仙韵”；固定角度阶段不加载任何 LoRA。
- M9.126（历史人物角度链，已被M9.163覆盖）：RealVisXL/IP-Adapter/OpenPose方案已废止；当前为Qwen-Edit多参考六格并逐槽确认。

- M9.125：国风浅涂/国风厚涂已形成14个 FLUX.1 Schnell GGUF Q8_0 仅测试候选池，风格5个、女性3个、男性3个、灵兽3个；骑士和古风肖像来源的官方示例均为女性，已移出男性并归入风格类；男性三项改为来源明确声明 Asian boy、male models、Men Model 的权重。候选独立登记于 `models/loras/测试LoRA索引.json`，正式模型索引和商用索引保持为空，逐项实图验收前禁止生产启用。

- M9.117：场景资产四张图固定为主平视全景、左45°斜侧全景、右45°斜侧全景、高角度微俯视；左右方向分别生成、分别保存，旧单45度和低角度槽在继续生成时自动替换为新角度定义。
- M9.116：固定角度图片“生成中”卡死已修复；前端项目恢复不再只查询定位基准图任务，同时按确定性角度任务名恢复人物、道具和场景的每个固定角度结果，完成任务回填图片并标记confirmed，中断任务重置为可继续生成。当前苏清璃和陆沉6张角度图已真实恢复并持久化，ComfyUI无运行或等待队列；Vue类型检查、Vite正式构建、21项单元测试与3项子测试通过。
- M9.115：道具和场景定位基准图已补齐与人物相同的“就要这张”入口；媒体浮层和图片简介内均可确认，确认后沿现有场景/道具固定角度链路生成其余三张，按钮按首张基准图键、生成状态和完成状态统一显示或隐藏。Vue 类型检查、Vite正式构建、20项单元测试与3项子测试通过。
- M9.114（历史候选测试，已被M9.163覆盖）：RealVisXL/IP-Adapter/OpenPose实图只保留为隔离验证记录，不属于当前固定角度链。
- M9.113：新版 6.5 头身 OpenPose 正面模板已完成 928×1664 真实出图；人物由九至十头身恢复为自然少女约 6.5—7 头身，头顶、双腿和双鞋完整入画，纯背景恢复。该图仅用于比例验证，因正式风格与人物 LoRA 尚为空未作为人物角度成品放行。
- M9.112：已清除正式目录内全部 19 个 FLUX LoRA，并将两份虽兼容 SDXL 但实图不符合纯灰背景、国风浅涂和自然人体比例的候选 LoRA 一并移出；正式 LoRA 索引当前为空，未通过实图验收的权重禁止进入生产。OpenPose 模板已按少女约 6.5 头身重新缩短躯干和腿部骨架，头脚控制范围由 14%—89% 收敛为 20%—82%。
- M9.110：根目录四份开发协作文档已统一重命名为 `Dev_INSPECTION_SPEC.md`、`Dev_MainDev.md`、`Dev_TESTING_SPEC.md`、`Dev_BUG_TRACKER.md`；AGENTS、README、产品愿景、项目记忆及系统 Agent Skill 内的全部对应路径已同步更新。
- M9.109：主线开发规划唯一文件已切换为根目录 `Dev_MainDev.md`；README、项目记忆、产品愿景、稽查规范及开发/稽查 Skill 提示中的旧 `AI_DEVELOPMENT_PLAN.md` 引用已全部同步清除。
- M9.106（历史人物角度链，已被M9.163覆盖）：IP-Adapter/OpenPose固定角度方案已废止；当前为Qwen-Edit多参考六格。
- M9.107：固定角度实图人工验收确认正面全身可完整出图，但 90 度侧面连续三轮均存在身份、发型、服装或身体比例漂移；本地自动视觉验收对这些图产生假阳性，侧面与背面暂未放行。
- M9.108：Schnell LoRA 性别选择已兼容中英文女性标记并通过真实出图；当前项目实际加载“暗夜仙境”风格 LoRA 与“汉服仙姝”女性 LoRA 后，仍未匹配林婉儿银灰及肩发、淡色长裙和国风浅涂淡彩风，当前 LoRA 人设与风格验收均不通过。

- 跨平台启动时动态识别物理内存和实时可用内存；系统保留容量只计算一次，禁止从实时可用内存重复扣减；绘图内存按分辨率动态估算。
- 当前 Mac 128GB 统一内存额外执行 AI 合计上限 90GB、系统预留至少 35GB 的更严格限制。
- 重负载任务全局串行；上下文、KV 缓存和批量生成强制限额，超限任务自动拆批；任务完成立即卸载模型并释放缓存。
- 稽查 Skill 永久 `writable=false`。
- 模型只能从平台注册表选择。
- 配置和任务变更必须形成可确认、可追溯提案。
- 未配置真实模型时明确失败。
- 当前项目路径：`/Users/aoo/Code/AI Agent`。

## 已知缺口

- M9.139（历史未落地候选，已被M9.163覆盖）：Visual Persona/PSHuman三视图方案未成为当前执行链；当前固定角度唯一执行链为Qwen-Edit多参考六格。其许可与远程服务门禁仅保留为历史记录。

- M9.138（历史人物角度链，已被M9.163覆盖）：Klein扩图与RealVisXL/IP-Adapter侧背方案已废止；当前为Qwen-Edit多参考六格，原比例及身份验收数据仅作历史依据。

- M9.135：场景与分镜画内文字已改为“扩散阶段留白、确定性字体层后置写字”的两段式链路。后端只识别明确文字指令并从生图提示中移除目标字符串，成图后按原始Unicode逐字绘制；缺少承载区域时自动补深色中性牌面，输出记录文字、字体、方向、锚点、SHA256与`exact_source_render`审计。人物基准图和普通道具图不会误触发。真实场景已生成并验证竖排“摘星楼”三字无乱码；7项后端回归、Vue类型检查和Vite正式构建通过。
- M9.136：Qwen Image Edit 2511 的MPS不兼容FP8混合权重已删除，主动局部修复固定改用`qwen_image_edit_2511_bf16.safetensors`。道具和场景固定角度不再自动调用Qwen‑Edit，改由FLUX.2 Klein 4B MLX直接生成角度候选；Qwen只在用户明确点击修复时加载。旧玉佩三张角度任务因FP8/MPS报错未产出最终文件，状态恢复后必须重新生成。
- M9.137：场景文字链路补齐生成前后双门禁：没有指定文字时，任何汉字、乱码、字母、数字、标题、标牌或水印均禁止放行；指定文字时也必须先得到无字底图，再执行确定性Unicode字体叠加。空场景同时禁止真人、人体、背影、剪影、人形雕像及海报人物，最多三张候选均不合格则明确失败。人物正面全身角度链路降低近照FaceID/服装构图权重、提高标准全身OpenPose强度并扩为四次候选，优先保证完整头脚和鞋底入框。

- M9.130：图片放大预览固定采用 Codex 式底部居中“缩小/实际百分比/放大”悬浮条，并支持滚轮、拖拽、双击复位与适窗缩放。人物全身角度图上下5%边距由生成后前景归一化程序确定性执行，不再由不稳定视觉模型单独裁决；苏清璃正面全身真实重测已完成并通过。
- M9.131：苏清璃旧全身图与基准图的人脸、辫子、浅青服装不一致，原验收是假阳性。现已提高身份锁定、加入基准图实际视觉特征提取和独立一致性复核，并禁止近照以高权重服装构图干扰OpenPose；连续真实候选均被新门禁正确驳回，错误图不再显示“就要这张”。

- 生产模型和媒体提供方需部署方注入。
- 插件签名、进程沙箱、真实部署、备份恢复和灾难恢复尚未验收。
- 生产运维文档仍需随真实环境验收同步。

## 下一步

- 当前主线：M9.149最终稽查残留项已整改，待独立软件测试。兼容流水线取消先写LangGraph权威状态，重启后保持cancelled且禁止批准、恢复或继续执行；JSON仅保存图投影。25项生产能力、8项基础设施、共享发现、远端数据面、双重租约围栏和服务端阶段编排保持有效。SQLite只支持同机多进程，跨主机高可用必须替换PostgreSQL/Redis提供方并完成故障测试。

- 继续完成短剧视频、配音、合片、增强与导出兼容接口恢复及真实全流程验证；保持 M11 暂停。
- M9.56：资产页“继续生成”已兼容已存在资产档案但分镜状态未及时恢复的项目；“就要这张”已补齐旧资产固定角度定义，并在生产台账兼容接口不可用时继续执行基准图确认和角度图生成。真实页面验证两个按钮均可见、可用且点击区域命中。
- M9.118（历史四视图口径，已被M9.163覆盖）：当前为Qwen-Edit六格；928×1664、背景、人物占比、头脚完整、方向、年龄、性别、脸型、发型和服装门禁继续适用于对应槽位。
- M9.119：人物基准图严格验收字段增加后，LLaVA沿用128-token输出上限导致JSON被截断并触发三轮无效重试，界面表现为生图卡住；现已启用JSON格式、1536上下文和320-token输出，重试提示不再嵌入截断验收全文。残留运行状态已释放，两个人物恢复为待生成。
- M9.120：人物基准图失败提示不再向界面暴露内部视觉验收JSON，改为仅显示未通过的中文规范项；自动生成由最多三张缩短为最多两张，新增“人脸占画60%—70%”明确门禁。苏清璃旧失败与队列状态已清空，人物资产恢复待生成。
- M9.121：人物基准图无法生成的最终根因是本地LLaVA对年龄、发型、服装和灰背景持续产生假阴性；基准图现按既定“生成→人工确认”职责拆分，自动门禁仅执行单脸检测、928×1664、规范化65%人脸裁切和InsightFace严格0度正脸，主观人设由“就要这张”人工确认。新增基于InsightFace脸框的自动裁切，苏清璃、陆沉真实页面生成均完成并回填，两张均显示“就要这张”。
- M9.122—M9.124（历史正面近照裁切口径，已被M9.187/M9.189覆盖）：曾使用脸高40%—50%、目标45%及发顶2%/5%规则；当前0°半身仅承担腰部裁切近景身份职责，0°正面全身是唯一人物基准和TripoSR输入，全部全身角度执行发顶至少8%、鞋底至少3%的纯背景安全区。
# M9.144 大纲生产任务可靠性与性能闭环（历史模型口径，已被M9.163覆盖）

- 当时采用27B Q8生成与122B终审；该模型口径已废止，当前固定为qwen3-vl:32b生成、qwen2.5:72b初审→最多一次修正→终审，失败保留结果与问题并停止，不自动重跑全剧。
- 当前两模型按需串行加载并在完成、异常或停止后释放；前端直接展示完整文本。
- 大纲已纳入文本任务唯一ID、真实线程、阶段、心跳、1800秒硬超时、按项目与客户端批次精确停止、重启恢复、关闭回收和failed终态保护。
- 当前模型runner继续按owner job_id隔离；停止等待锁的项目只取消自身，只有目标任务正是runner owner时才执行模型终止，防止跨项目误杀。
- 项目切换或页面中断会立即保存原项目失败状态并停止对应后端任务，不允许残留generating。
- 该历史BUG已由后续里程碑覆盖；当前执行口径以M9.163为准。
- M9.148：全链路规范唯一时长口径为55—65秒，后端相关文本入口越界明确拒绝。AI视频分段、剧本段落映射、3D遮挡、Blender头Mask、身份阈值、`_vN`选版、失败升级、共享SHA、风格阻断与视频选路均已写为目标验收条款；未完成正式动态接线前返回`capability_not_implemented`，不得声称已实现。
- M9.162（重新最终稽查通过，已关闭）：阶段进度按revision长轮询；取消键为tenant/user/project/stage/request完整作用域，跨项目同request互不影响。shutdown广播使活动watch立即503并释放槽；软件128项及3个子测试与最终稽查通过。
- M9.161（重新最终稽查通过，已关闭）：分镜按完成集增量持久化；前端每500毫秒读取并按集串行调用不推进LangGraph的`storyboard_preview`，旧项目晚到响应受project/session/abort围栏阻断。最终确认先停止轮询并等待预览队列，仅提交一次正式assets，保留已有图片与确认状态。软件110项及3个子测试与最终稽查通过。
- M9.166（历史人物口径，已被M9.187覆盖）：当前3D输入唯一口径为人物0°正面全身、道具45°三分之二、场景45°空场景全景。道具和场景仍只生成一张2D基准图，确认后自动执行TripoSR→Blender并回填七角度四通道；人物保留六格2D身份档案。
- M9.165（最终稽查通过，已关闭）：重启恢复权归服务端。前端加载不自动调用10类生产任务，空资产/旧census仅播种本地空卡；分镜视频不再50ms重提。服务端八阶段恢复同步失败化LangGraph、项目阶段和台账并递增revision，幂等且不占生产资源；最终独立回归128项与3个子测试及稽查通过。
- M9.167（重新最终稽查通过，已关闭）：统一资源调度器新增租户和项目分层背压。accelerator、cpu-media、control全局/租户/项目限额为16/8/4、64/32/16、256/128/64；同步及三条异步票据、waiting-memory状态投影、原子背压、软件131项与3个子测试及最终稽查通过。
- M9.167异步边界：视频即时、waiting-memory恢复和3D后台任务必须从持久body显式传入tenant/user/project；thread-local仅作为同步HTTP兼容上下文，不得作为跨线程身份来源。
- M9.167状态边界：`waiting_memory`是任务仓库真实子状态，投影LangGraph时固定映射`queued`，两层状态不得混用。
- M9.168（历史人物3D角度已被M9.187覆盖）：资产生成遇到`production stage is already running: assets`时等待服务端权威阶段完成并合并结果，不再直接标记失败。人物卡显示顺序保持0°半身、0°正面全身、左45°、右45°、90°、180°；当前TripoSR唯一人物输入为M9.187的0°正面全身。
# 当前主线

- M9.171（安装里程碑，自动接线已由M9.175完成）：MiniMax H3 Context IR Agent已安装并锁定供应链、32B源blob、SBOM和安全扫描；Ref2VA生产接线、持久阶段和卸载生命周期以M9.175为准。

- M9.170：服装已从人物本体拆为道具栏独立`costume`资产；分镜逐镜记录并解析`costume_id@version`换装，45°单图进入TripoSR/Blender独立网格链并按稳定ID归档。角色基础体穿衣/蒙皮/碰撞仍为待开发，前后端统一阻断并禁止冒充完成。当前状态：最终稽查通过，已关闭。
- M9.169（最终稽查通过，已关闭）：统一任务事实以单个SQLite批量事务提交，并同事务写持久投影outbox；JSON仅后置兼容投影。第二条DB失败整批回滚、三类Graph失败保留completed与outbox、重启及心跳重放成功ack均通过动态测试。
- M9.169并发围栏：outbox事件使用全局单调revision，ack必须job_id+revision匹配；LangGraph按stage拒绝旧projection revision，防止并发旧drain误删新事件或回退图状态。
- M9.169跨进程围栏：任务SQLite按tenant/user/project/stage保存投影租约并自动续租；拿锁后重新读outbox，两个服务实例不得并发提交同一阶段checkpoint，崩溃后租约过期重放。
- M9.169失租围栏（软件测试通过，待重新稽查）：租约续期失败、owner变化、TTL过期统一置lost；Graph提交前后验证所有权，提交途中失租时从权威任务生成更高revision修复事件。两进程抢锁、续租/释放/replay SQLite busy、shutdown不ack与正式空闲验收均已通过；关联`137 passed, 3 subtests passed`。
- M9.169 busy容错：释放锁行失败由TTL自动回收；text/image/video任一投影库瞬时异常只保留其outbox待下轮重放，其他库继续，worker heartbeat不得退出。功能复测已通过，正式空闲验收等待用户H3任务自然结束。
# M9.194 联网搜索能力框架接入（最终稽查通过，已完成）

- 时效/来源类问题必须先执行统一 `web.search`，成功后本地模型基于来源回答；失败不得猜测。
- 提供方顺序：Brave（有Key）→ GitHub API → DuckDuckGo HTML → Bing RSS → Google News RSS → Google HTML；结果带query/provider/searched_at/sources，网络全失败时仅同查询短TTL缓存可恢复。
- SSRF、协议、响应类型、体积、超时门禁为强制边界；展示仍统一进入对话框。
- 最终独立复测：空缓存LangGraph真实公网连续3/3并含权威仓库；GitHub 403/限流逐次记录后可审计降级。专项147 passed/1 skipped/3 subtests，全unit543 passed/1 skipped/9 subtests，pycompile+后端compileall/typecheck/83模块构建及文档状态检查通过。
- 中文领域查询先做约束保留的中英检索词归一；结果必须通过主题、开源及模型实体门禁。原句真实HTTP仅返回SenseNova U1、Flux.2 Klein、GLM-Image并按发布日期倒序，回答与来源同源且LLM调用为0；Google Translate/Tamil/字体/工具/提示库被拒绝。缓存仅同query/count短期恢复，cache_hit/live_attempts/恢复时间完整透传，跨查询、过期、损坏、私网来源失败关闭。
- M9.195 / BUG043开发完成，待独立软件测试。LangGraph此前只围栏生命周期/revision/generation，被拒绝输入仍会覆盖持久`event`，从而污染重启恢复与导演决策。现以`accepted_event`和按stage事件映射保存权威事件；旧revision、旧generation、已有正代际后的无代际回调均恢复最近已接受事件。完整单元测试`507 passed, 1 skipped, 9 subtests passed`。
- M9.195扩大冻结组合`117 passed, 1 skipped`，Python编译、Vue typecheck及83模块构建通过；未启动重模型或正式任务。
- M9.195 / BUG043独立软件测试退回：更高stage generation携带新代较小projection revision时，被当前“revision先比较”逻辑错误拒绝；动态`4/10 -> 5/1`后仍保留旧owner事件。正确边界为generation优先、revision仅在同generation内单调。按首败规则未继续扩大回归，未启动模型或正式服务。
- M9.195 / BUG043退回整改完成，待独立复测：`stage_generation`先裁决owner，新代建立独立revision序列；只有同代才比较projection revision。旧代、零代、同代旧/相等/缺revision均不能覆盖accepted event。双实例/reload专项`7 passed`，完整unit`525 passed, 1 skipped, 9 subtests passed`，编译、类型检查和83模块构建通过。
- M9.195第二轮退回整改完成：`revision=0`不再被当成“尚无历史”；正generation只要该stage已有accepted event，同代下一个revision必须严格更大。无stage event的旧checkpoint允许一次基线迁移。服务端开始/终态/确认/恢复调用已显式形成0→1→N序列。专项`9 passed`、完整unit`543 passed, 1 skipped, 9 subtests passed`，编译/typecheck/83模块构建通过。
# 2026-08-11 BUG049 当前事实

- M9.198正式项目`83eaa695-0a31-4119-aa94-060c232053fd`完成并确认16镜分镜后，资产人物/场景数据已落盘，但项目assets与LangGraph停在`generating/running`；任务仓库、stage lease、资源池和Ollama均为空，形成无执行者运行态。
- 首个错误事实是公开`/api/projects/stage`仍直接进入内部`_write_project_stage`并改写生产ledger/LangGraph；晚到UI快照没有stage generation围栏，可覆盖服务端正式阶段提交。BUG049进入主线整改，BUG038按用户指令跳过，BUG039—041不处理。
- BUG049主线结果：公开project stage已成为纯投影且受服务端generation终态保护；分镜入口与资产页批量入口都必须同时看到项目assets成功状态和LangGraph `pending_confirmation/completed`，否则先重跑正式assets并禁止生图。9B资产JSON解析失败仅重试一次。正式generation 2截断失败、generation 3重试成功、人物基准图完成及暂停资源释放均已取得证据；进入独立软件测试。
- BUG049独立软件测试通过待稽查：关联`111 passed`、全unit`565 passed, 9 subtests passed`无skip，typecheck、83模块构建、编译、文档门禁和正式资源终态通过。
- BUG049首轮稽查退回整改：仅保护生命周期不足以保护内容；终态后的晚到assets快照必须走现有名称合并并保留current-only正式census，其他阶段直接保留当前权威投影，避免状态正确但数据被旧快照删除。
- BUG049稽查整改开发完成待独立复测：assets受保护晚到投影强制名称合并，正式census独有场景/道具与媒体字段不被删除；非assets保留当前完整数据。关联`111 passed`、全unit`565 passed, 9 subtests passed`、typecheck、83模块构建、编译和文档门禁通过。
- BUG049稽查整改独立软件复测通过待只读稽查：关联`111 passed`、全unit`565 passed, 9 subtests passed`无失败无skip，typecheck、83模块构建、编译及文档门禁通过。
- BUG049最终只读复稽查通过，已关闭：公开projection-only不再写生产权威；终态assets晚到快照保留正式完整census及现有媒体字段，非assets保留当前完整权威数据。M9.198继续正式资产图片及后续全链。
- BUG050正式首败：正式道具“信纸/信件”生成因检测到文字而真实failed，项目assets及道具条目均持久化明确错误；浏览器卡片却回到“等待生图”且不显示错误，形成前端投影分裂。当前只处理该错误回填/展示链。
- BUG050主线整改完成待独立测试：轮询收到后端failed后保持failed和脱敏错误，显式重试前不清除；统一人物/道具/场景卡主体持续显示失败。关联`134 passed`、全unit`565 passed, 9 subtests passed`、typecheck、83模块构建及文档门禁通过。
- BUG050首轮独立测试退回整改：正式页面错误正文已显示，但失败无图占位仍写“等待生图”；共享资产卡改为依据slide failed显示“生成失败”，消除同卡状态矛盾。
- BUG050退回整改开发完成待独立复测：关联`134 passed`、全unit`565 passed, 9 subtests passed`无skip，typecheck、83模块构建和文档门禁通过。
- BUG050独立软件复测通过待只读稽查：正式失败道具同时显示“生成失败”和真实质检原因；关联`134 passed`、全unit`565 passed, 9 subtests passed`无失败无skip，typecheck、83模块构建及文档门禁通过。
- BUG050最终只读稽查通过，已关闭：后端failed、共享卡“生成失败”和卡片级错误一致，只有显式重试清除；继续正式资产队列。
- BUG051正式首败：同一道具两批有限生成均因文字质检失败且资源正常释放；持久提示词同时含正向“娟秀笔迹”和“无文字干扰”，与道具`no_text_or_signage`硬门禁构成不可满足契约。当前只处理无文字资产提示净化公共链。
- BUG051主线整改完成待独立测试：道具进入任一provider前确定性移除正向字形线索，保留非文字材质形态与否定约束，重试复用净化prompt；正式“信纸/信件”已生成成功。关联`135 passed`、全unit`566 passed, 9 subtests passed`及全部静态门禁通过。
- BUG051独立软件测试通过待只读稽查：正式道具waiting_confirmation、媒体存在、error为空、模型释放；关联`135 passed`、全unit`566 passed, 9 subtests passed`无失败无skip及构建门禁通过。
- BUG051首轮稽查退回整改：原子句级任意否定可被“带铭文且无人”旁路；现逐个字形cue检查紧邻前后直接否定，无关人物/背景否定不能保留正向字形要求。
- BUG051稽查整改开发完成待独立复测：支持“禁止出现任何文字/文字不得出现”直接否定，拒绝“带铭文且无人”无关否定旁路；关联`135 passed`、全unit`566 passed, 9 subtests passed`及全部门禁通过。
- BUG051稽查整改独立复测通过待只读稽查：关联`135 passed`、全unit`566 passed, 9 subtests passed`无失败无skip，构建门禁、正式产物和资源终态通过。
- BUG051最终只读复稽查通过，已关闭：逐cue直接否定与无关否定隔离、正式产物、有限重试及资源释放证据闭环。
- BUG052正式首败：已串行的第二道具在前一Comfy任务`/free`返回后立即被42GB门禁拒绝，提示仍要求“拆分串行”；首个事实为Comfy释放异步且下一任务无有限等待握手，当前只处理资源释放/准入生命周期。
- BUG052主线整改完成待独立测试：近期释放或空闲Comfy主动释放后才有限等待内存，逐秒检查取消/心跳，超时失败；活动Comfy及非释放窗口立即拒绝。正式三个道具连续成功。关联`139 passed`、全unit`570 passed, 9 subtests passed`及全部门禁通过。
- BUG052独立软件测试通过待只读稽查：关联`139 passed`、全unit`570 passed, 9 subtests passed`无失败无skip；正式无活动图片任务、子进程和Ollama模型，构建门禁通过。
- BUG052最终只读稽查通过，已关闭：accelerator单重任务、空闲释放、有限waiting_memory、取消/超时/重启与真实不足边界闭环。
- BUG053正式首败：空房间场景运行超过六分钟，候选物理PID已退出且Ollama为空，但job仍processing/klein9b_baseline、pid空、heartbeat停止；人工暂停后才清除。当前只处理候选后验收/回填的心跳、超时与终态。
- BUG053主线整改完成待独立测试：根因为物理图片子进程监督结束后，最长600秒的同步道具/场景LLaVA验收未继承持久job心跳、停止和阶段超时。现统一通过`_run_image_validation`进入prop/scene_validation，每两秒心跳并检查任务可运行性，180秒超时或停止会终止LLaVA并隔离晚到结果。正式job`5a5f555b-5620-4645-a2c6-a504b328ef27`从有PID生成切到无PID但持续心跳的scene_validation，约2秒后completed且三项验收全真。图片专项33项、全unit570项及构建门禁通过。
- BUG053独立软件测试通过待只读稽查：Node运行时绑定后关联87项、完整unit572项及9个子测试全部通过且无skip；正式job、2,462,708字节媒体、视觉证据、空PID/error与Ollama释放一致。
- BUG053首轮稽查退回整改完成待独立复测：稽查发现随机audit票据可排队900秒并越过外层180秒终态后晚到启动LLaVA。现验收资源票据绑定原图片job，排队与验收同为180秒，停止/超时先取消该job全部排队票据再终止活动模型；专项34项、完整unit573项及9个子测试通过。
- BUG053稽查整改独立复测通过待只读复稽查：关联88项、完整unit573项及9个子测试无失败无skip；排队票据取消、活动模型终止、原生命周期矩阵、构建门禁及正式场景证据一致。
- BUG053最终只读复稽查通过，已关闭：共享道具/场景验收具备持久心跳、停止、硬截止、180秒超时、原job票据取消、活动LLaVA终止和终态晚到隔离；M9.198继续正式全链。
- BUG054正式首败：通过正式scope upsert将“空房间”从failed重新激活并执行资产baseline确认后，权威记录已completed且confirmation存在，却仍保留`图片生成失败：图片任务已停止`。当前只处理ProductionLedger成功状态转换清除旧error的公共契约，尚未启动3D。
- BUG054主线整改完成待独立测试：ProductionLedger现在在reactivate到非失败态、直接completed及confirm CAS时清除旧error，pending显式诊断与失败态错误不变。正式scene:空房间复确认为completed/error空；台账84项、直接依赖225项通过，另1项用户跳过BUG038静态断言明确隔离。
- BUG054独立软件测试通过待只读稽查：台账及直接依赖225项通过，唯一deselect为用户明确跳过BUG038人物静态断言；成功/失败错误边界、正式记录、构建及文档门禁一致。
- BUG054最终只读稽查通过，已关闭：reactivate、直接completed、confirm CAS均保证成功error为空，pending诊断与失败态错误保留；正式场景记录已干净，继续M9.198。
- BUG055正式首败：正式空房间3D job`eb3eda72-a969-4334-9fd7-78c22b79ae41`在TripoSR后进入Blender清理，因仍有4条非流形边failed；生命周期正常，当前只处理公共网格封孔/清理/审核链，未进入视频。
- BUG055主线整改完成待独立测试：根因是Blender批量holes_fill遗漏一个4边简单微孔；公共worker增加严格度2闭环的有序封口兜底，分叉/开放损坏继续失败关闭。同一正式GLB动态由4条降至0条，正式job`fb1c04d2-f14f-4073-ac97-febc03ddc676`完成，10422顶点/20827面、7视图、24FPS×96帧源视频，资源已释放。
- BUG055独立软件测试通过待只读稽查：动态简单环4→0、度4分叉边界6→6；关联61项、完整unit576项及9个子测试通过，唯一deselect为用户跳过BUG038静态断言；正式报告和资源终态一致。
- BUG055最终只读稽查通过并关闭：共享worker无特判，简单闭环确定性封口、复杂边界失败关闭；正式3D已确认归档至项目hot区不可变版本，M9.198继续道具3D及后续全链。
- BUG056正式首败：`信纸/信件`3D job`d3783388-3b53-457e-9d94-159d8c340e17`几何成功但确认409；生成持久身份保留业务原名，确认却以`_safe_name`后的`信纸_信件`比较业务身份和subject key。当前只修复身份名/路径名边界。
- BUG056主线整改完成待独立测试：确认入口以精确业务原名校验任务和subject，以安全名处理候选fallback/归档；相近名称不能碰撞。正式原job已确认至`3d/props/信纸_信件`不可变版本，关联32项通过。
- BUG056独立软件测试通过待只读稽查：关联56项、完整unit581项及9个子测试通过，唯一deselect为用户跳过BUG038；原名身份、碰撞拒绝、安全归档及正式job一致。
- BUG056最终只读稽查通过并关闭：业务身份、subject和安全路径分界完整，正式含斜杠道具已确认归档；继续服装道具3D。用户追加H3视频无需音频输入，进入视频阶段前核验统一调用契约。
- BUG057正式事实：H3没有参考音频输入，但graph仍`VAEDecodeAudio`并把模型自生音轨封装进源MP4；该声音后续被独立TTS/口型覆盖。官方Ref2VA节点的`audio_vae`为必填，当前只移除采样后音频解码/封装并锁定静音源视频契约。
- BUG057当前阻塞：代码已移除H3音频解码/封装且81项关联测试通过，Comfy确认CreateVideo.audio可选；正式API被`previous stage is not completed: image`拒绝。image完成依赖用户要求跳过的BUG038，不能绕过生产门禁，故尚无真实静音H3/ffprobe证据、不得进入独立测试。
- BUG038角度与四肢硬门禁增量：用户确认当前人物角度正确，现行左右45°生成配置固定为35°提示目标、左右偏航确定性范围`+30°—+60°/-60°—-30°`、滚转≤7°、角度LoRA 0.35、Lightning 4步LoRA 1.0、steps 4、CFG 1.0；未经真实六格回归、独立测试与稽查不得改变。所有全身角度及0°baseline新增手、脚、肢体与数字解剖硬门禁，粘连/缺失/多余/重复/断裂/融化候选失败关闭并有限重生成。开发专项`36 passed`，编译、文档门禁及空闲重载通过，待独立软件测试。
- BUG038四肢门禁首轮测试退回后补齐：生产与测试共用姿态required-check helper；正式Handler矩阵覆盖左右45°、90°、180°×手/脚/融合缺失多余三种单字段失败及全真正常链，失败均2次有限重试后删除全部候选并502，正常首张200。关联`60 passed, 2 skipped`，编译和文档门禁通过，待独立测试在Node环境补跑跳过项。
- BUG038四肢门禁二次补证：0°baseline也已拆为hands/feet/no_fused三个单字段false的正式Handler用例，各自3次候选全删、502、job failed；开发关联`64 passed, 2 skipped`、编译和文档门禁通过，待独立复测。
- BUG038最终独立软件测试通过待只读稽查：0°Handler 19项、四个全身角度×三项解剖字段及正常链、扩大关联与规范补充合计146项及3个子测试，0失败0跳过；角度硬基线、编译、文档状态及全部运行资源终态一致。
- BUG058正式复现：确认“林婉清”0°基准图后，五个Qwen固定角度先后因60GB门禁报必须保留99GB/90GB/81GB而失败。根因是Qwen固定角度和独立修复仍直接`_require_memory`，未复用BUG052的Comfy异步释放等待协议；现统一改为带原job身份的有限、可取消`waiting_memory`握手，待开发及正式回归。
- BUG058主线正式验证完成待独立软件测试：林婉清左45° job `444f4db8...`通过有限Comfy释放握手进入Qwen并成功产图，末态失败仅为方向/身份/脚部等视觉质量门禁，不再出现60GB内存误拒绝；紧随其后的苏璃左45° job `b38fa97a...`也自然准入。资源快照仅一个accelerator active、Comfy单running；完整单元`584 passed, 9 subtests passed`无失败无跳过。
- BUG058独立软件测试通过待只读稽查：内存等待/取消/超时/恢复及关联`168 passed`，完整unit`584 passed, 9 subtests passed`，0失败0跳过；正式林婉清与苏璃串行Qwen准入、accelerator单执行体事实复核一致。
- BUG058最终只读稽查通过并关闭：固定角度与独立修复两个60GB入口统一复用有限、可取消释放握手；waiting_memory持久化、queued图投影、60秒超时、空闲释放及真实不足立即拒绝边界成立。正式任务从内存误拒绝恢复到Qwen执行，视觉质量仍失败关闭；定向`168 passed`、完整unit`584 passed, 9 subtests passed`无失败无跳过。
- BUG038最终只读复稽查通过并关闭：0°基准Handler与四个全身角度共享解剖字段契约，失败候选有限重试、物理清理、持久failed以及成功后发布边界成立；显式Node环境关联`117 passed, 3 subtests passed`，0失败0跳过。现行角度提示、偏航范围、LoRA权重、steps和CFG版本化硬基线未改动。
- 2026-08-12 资产卡界面精简：按用户明确要求，移除资产卡底部常驻红色错误提示和“AI图片超分”按钮，同步删除组件`upscale`事件及父层监听；错误状态、简介内错误详情、图片生成与其他分镜超分入口保持不变。定向测试`6 passed`、Vue类型检查、diff格式与文档状态检查通过。本次未操作8787/8194服务，未重启5173。
- 2026-08-12 工作流集数选择器布局：将所有工作流共用的原生`select`替换为可控弹窗，集数按每行5集的固定网格排列，“全剧集”单独占据整行，弹窗以触发器右边缘对齐；选择、点击外部关闭和Escape关闭行为保留。Vue类型检查、diff格式与文档状态检查通过；`pnpm typecheck`的包管理器前置检查因未批准`esbuild`build script而停止，改用已安装`vue-tsc --noEmit`成功验证，未修改依赖。在5173真实页面逐一切换大纲、剧本、分镜脚本、资产、分镜画面、分镜视频、成片、导出，8个流程均仅显示1个共用选择器；弹窗计算样式为5列，弹窗与触发器右边界均为1278px，右对齐成立；实际选择“全剧集”后文案更新且弹窗关闭，Escape关闭通过。本次未操作8787/8194服务，未重启5173。
- 2026-08-12 资产简介按钮精简：按用户截图指定范围，在人物、道具、场景照片共用的`UnifiedAssetCard`中移除简介面板内“确认基准图并生成其余人物角度”和“生成3D资产”两个带边框按钮，同步删除仅为它们服务的`canAcceptBaseline`参数、`confirmBaseline/generate3d`事件和父层监听。图片上的“就要这张”仍承担基准图确认和后续角度/3D自动链，3D等待确认、停止与下载保留。Vue类型检查、关联`36 passed, 3 subtests passed`、文档状态和diff格式检查通过。5173真实页面分别切换人物/道具/场景，共享资产卡数量分别为1/3/1，三类的两个指定按钮计数均为0，人物“就要这张”仍为1。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产操作栏底部布局：将“继续生成图片”所在的共享`WorkflowActionBar`从资产内容顶部移到全部资产卡之后；`asset-production-preview`改为纵向弹性布局，操作栏使用`margin-top:auto`吸附底部，并清除底部内边距。首次真页测量发现仅调整DOM顺序后仍有365px容器剩余空白，未提前报告通过；弹性布局整改后复测，操作栏为容器最后元素、`display:flex`、`flex-direction:column`、`padding-bottom:0px`，实测底部间隔`0px`。Vue类型检查、定向`17 passed, 3 subtests passed`、diff格式和文档状态检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 统一资产简介位置：用户复核确认原问题未解决，当前只处理该条。人物、道具、场景共用的`UnifiedAssetCard`将名称、提示词、错误、3D结果与状态组成常驻`asset-profile-summary`，放在整组图片轮播上方；共享媒体操作组件新增`showIntro`显示开关，仅统一资产图片关闭每张图上的“图片简介”按钮，分镜图/视频原简介交互不受影响。Vue类型检查、关联`44 passed, 3 subtests passed`、文档状态与diff格式检查通过。5173真实页面分别验证人物/道具/场景：资产卡为1/3/1，常驻简介区为1/3/1，全部简介底边均不低于图片轮播顶边，三类资产卡内“图片简介”按钮数均为0。未操作8787/8194服务，未重启5173。
- 2026-08-12 全流程底部操作栏：大纲、剧本、分镜脚本、资产、分镜画面、分镜视频、成片全部复用`WorkflowActionBar`；六类实际内容容器统一标记`workflow-stage-content`并使用纵向弹性布局，共享操作栏为最后视觉顺序、`sticky bottom:0`和零底部内边距。分镜画面的五列照片独立放入`workflow-image-grid`，外层只管理滚动内容与固定底栏。首轮真页验证发现仅sticky时短内容阶段仍离底182–480px；改为弹性布局后仍有8—25px默认底部内边距，均未报告通过；最终清零后5173逐阶段复测，7个阶段均只有1个操作栏，计算样式均为`sticky/bottom:0px/padding-bottom:0px`，底边间隔为0—1px边框。剧本内容实际纵向滚动1088px前后操作栏保持`top=582/bottom=714`；分镜脚本滚动1050px前后同样保持`582/714`；资产照片实际横向滚动至162.5px后底栏保持`top=636/bottom=714`。Vue类型检查、定向`19 passed, 3 subtests passed`、diff格式和文档状态检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 统一流程背景框与提示精简：新建`WorkflowStagePanel`作为大纲至成片及导出的唯一内容背景框，统一弹性布局、滚动、边框、圆角、背景和阴影，顶部内边距固定5px。资产持久错误仍保留业务事实，但统一卡片不再显示“部分固定角度图片生成失败，可继续生成”；已有基准图且待确认时不再显示“定位基准图待确认”，其他真实失败与3D状态保留。Vue类型检查、关联`46 passed, 3 subtests passed`、文档状态与diff格式检查通过。5173实页逐一验证大纲、剧本、分镜脚本、资产、分镜画面、分镜视频、成片：每阶段恰好1个`workflow-stage-panel`，计算样式均为`padding-top/right/left=5px`、`padding-bottom=0px`、`border-radius=10px`、白色背景、纵向自动滚动。人物/道具/场景的两条指定提示计数全部为0，同时其他“生成失败”真实状态仍可见。剧本在新统一框内实际滚动900px前后，底栏保持`top=636/bottom=714`。未操作8787/8194服务，未重启5173。
- 2026-08-12 底部操作栏外框加长：按用户截图将共享`WorkflowActionBar`的横向总长度增加30px，通过左右各扩展15px保持居中，大纲至成片全流程统一生效。Vue类型检查、定向`21 passed, 3 subtests passed`、文档状态与diff格式检查通过。5173实页测量大纲、剧本、分镜脚本、资产、分镜画面、分镜视频、成片，7个阶段的外框均比内容宽度精确增加30px，左右扩展均为15px，底部仍仅保留1px边框。未操作8787/8194服务，未重启5173。
- 2026-08-12 底栏5px边距与宽度自适应：用户进一步要求左右及底部留白均为5px，固定底框必须随右侧栏自动收放；该指令覆盖上一轮固定加长30px方案。`WorkflowStagePanel/workflow-stage-content`统一四周5px内边距，`WorkflowActionBar`恢复`width:100%;margin:0`，保持sticky底部定位并跟随面板宽度。Vue类型检查、定向`21 passed, 3 subtests passed`、文档状态与diff格式检查通过。5173初始实测背景框/底栏宽度为330/318px，左右和底部内留白均为5px；真实拖动右侧分栏后宽度变为400/388px，背景框和底栏同步增加70px，再收窄为380/368px，左右/底部留白全程仍为5px，底栏底边屏幕坐标始终为709px。未操作8787/8194服务，未重启5173。
- 2026-08-12 对话框栏300px最小宽度：新增唯一`CHAT_PANEL_MIN_WIDTH=300`并用于左/右栏宽度分配边界，中间`chat-panel`同步设置`min-width:300px`。首次5173真实拖动发现右侧栏仍被旧的视口32%上限限制在410px，对话框最窄只能450px，未报告通过；右栏动态上限改为“左栏至少180px+对话框至少300px”后，真实栅格达到`420px 300px 560px`，对话框实测正好300px；左、右分隔条继续越界拖动160px后仍保持该栅格，不会低于300px。Vue类型检查、定向`22 passed, 3 subtests passed`、diff格式和文档状态检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 右侧工作流栏600px默认宽度与图片尺寸上限：用户确认目标为最右侧工作流栏。右栏默认/双击恢复宽度改为600px，可拖动范围为260—900px，实际上限继续受左栏180px和对话框300px安全边界限制。统一资产图片保持比例收缩，单图最大宽度按600px右栏的真实显示尺寸锁定90px；5173实测右栏600px时图片90px，缩至400px时图片56.84px，放大至720px时图片仍为90px。资产顶部`assetStageError`提示组件永久移除，实页“生成失败，请重试”提示计数为0，错误事实与卡片终态不变。Vue类型检查、定向`23 passed, 3 subtests passed`、文档状态与diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 右侧工作流栏刷新默认保持600px：复核发现右栏初始化仍优先读取浏览器中历史保存的宽度，导致此前拖动到720px后刷新不会回到600px。右栏初始化改为固定600px并停止持久化拖动宽度；拖动和双击恢复仍保留，每次重新加载页面均以600px为默认。5173实测初始为600px，拖动放大至720px后刷新恢复为600px。Vue类型检查、定向`23 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产错误提示永久删除与四张半轮播：统一资产卡删除摘要区错误提示节点、对应格式化函数和父层错误参数；人物、道具、场景共用生效，业务错误状态仍保留。默认600px右栏的轮播宽度以四张整图、四个间隙和第五张半图计算单图宽度，右栏缩小时继续按4.5张比例收缩，放大时以上述默认尺寸为上限。5173实测轮播566px、单图121.33px，前4张可见比例均为1，第5张为0.5002，第6张为0；“生成失败，请重试”精确提示及摘要错误节点计数均为0。Vue类型检查、定向`23 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 对话文字随栏宽自动换行：针对对话框缩窄后长文本越过右边界并被最右栏遮盖的问题，为滚动区、消息行、用户气泡和助手回复补齐可收缩宽度约束，并对长链接、英文错误和无空格文本启用任意位置换行；横向溢出隐藏，纵向滚动保持。首次5173缩至300px实测发现外层栏虽为300px，隐式Grid列仍被消息固有宽度撑至697px，未报告通过；补上`grid-template-columns:minmax(0,1fr)`后复测，栅格为`226px 300px 754px`，对话滚动区为299px且左右边界均在300px栏内，所有消息元素越界计数0、横向滚动宽度299px。Vue类型检查、定向`24 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产摘要状态文字组件永久删除：按用户截图删除统一资产卡简介底部的状态文字节点及其完整状态映射函数，包含“生成失败”“待生成定位基准图”“正在引用定位基准图生成”“固定角度图已完成”和3D状态文案；人物、道具、场景共用生效。图片格内部的等待/失败占位仍用于识别每张图片结果。5173实测人物/道具/场景分别有1/3/1张资产卡，三类摘要状态节点均为0，全部相关状态文案均为空。Vue类型检查、定向`24 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产图片固定尺寸与默认三张半：按默认600px右栏、566px轮播可用宽度，将单张图片卡固定为157.429px，使初始范围显示3张完整图片及第4张一半；取消窄视口下会覆盖卡片宽度的媒体规则。左右拖动右栏只改变轮播可见范围，卡片不再缩小或放大。5173实测默认600px时卡片157.42px，前3张可见比例1、第4张0.5001；右栏缩至400px及放大至720px后，卡片宽度均仍为157.42px，仅可见数量改变。Vue类型检查、定向`24 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 工作流底部操作栏完全固定：复核资产道具内容纵向滚动500px时，共享底栏已保持`top=631/bottom=709`；为落实“一直固定不能动”，进一步补齐左右0、禁止弹性缩放、自身拉伸和禁用位移变换约束，使图片横向滑动及内容纵向滚动均不能改变底栏位置或宽度。5173复测资产轮播横向从0滚至350px、内容纵向从500滚至1000px，底栏前后均精确保持`top=631/bottom=709/left=691/right=1269/width=578`。Vue类型检查、定向`25 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 人物身体简介固定两行：人物资产摘要新增专用身体描述展示，只优先组合年龄、性别、外貌、妆发、体型、服装等结构字段；旧数据回退时按身体特征词白名单提取，并排除身份、主配角、性格、心理、动机、剧情、动作和摄影文字。分镜完成时的前端占位简介不再拼身份、性格、动机和“影视近照”；资产提取契约同步明确人物image_prompt仅允许身体与穿着信息。显示区域固定两行并截断溢出。5173当前人物实测文本为“女性”，禁用词命中0；简介高度37.1953px、行高18.6px，计算为1.9997行，clamp为2、溢出隐藏。Vue类型检查、定向`26 passed, 3 subtests passed`、文档状态及diff格式检查通过。后端代码契约已记录但遵照用户要求未重启8787，当前页面过滤已即时生效；未操作8194、未重启5173。
- 2026-08-12 资产图片栏外层底框遮罩删除：人物、道具、场景共用的资产卡外层移除内边距、边框、圆角、白色背景和阴影，保留单张图片自身的圆角、边框、占位状态及操作控件。5173实测人物/道具/场景分别1/3/1张资产卡，三类外层均为`padding:0/border:0/radius:0/background:transparent/shadow:none`，单图圆角仍为12px。Vue类型检查、定向`27 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产标题角色、简介去留白与永久删除修复按钮：人物标题统一显示`姓名(角色类型)`；身体简介由固定两行高度改为最多两行，短文本按实际一行收起空白。人物、道具、场景共用资产卡移除图片“修”按钮、repair事件、父层监听、队列入口及已无入口的局部修复函数，其他导入、重做、引用和确认操作保留。5173实测人物标题为`林婉清(女主角)`，单行简介高度18.59px等于一行18.6px，不再保留第二行空白；道具和场景长简介仍限制为37.19px两行。三类资产的“局部修复”及“修”文字按钮计数均为0。Vue类型检查、定向`28 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产统一导入与自动角度归位：人物、道具、场景每张图片上的导入按钮关闭，姓名行最右侧新增唯一“导入”按钮。人物导入先调用现有视觉识别接口，从六个固定角度标签中只接受唯一结果，再将图片写入对应基准或角度槽；道具和场景只有一个基准槽，直接归位。识别不明确或返回多个标签时失败关闭并提示，不猜测角度。5173实测人物/道具/场景分别1/3/1张卡，姓名行导入按钮分别1/3/1，每张图片区内导入按钮三类均为0；按钮均位于姓名之后、姓名行右侧7px内边距位置。Vue类型检查、定向`29 passed, 3 subtests passed`、文档状态及diff格式检查通过。为避免覆盖当前正式资产，本轮未提交真实导入文件；角度识别、唯一标签门禁及对应槽写入由定向契约覆盖。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产姓名下方简介全部删除：人物保留`姓名(角色类型)`标题，人物、道具、场景均保留姓名行最右侧导入按钮；统一资产卡删除姓名下方的简介节点、显示过滤函数和对应样式，不影响底层image_prompt生成与生图使用。5173实测人物/道具/场景分别1/3/1张卡，姓名下方简介节点三类均为0，标题分别保留，姓名行导入按钮仍为1/3/1。Vue类型检查、定向`30 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 预览模式留白固定50%透明：图片和视频共用的全屏预览遮罩背景透明度由68%改为固定50%，同时移除背景模糊，确保媒体之外所有留白区域真实透出下层界面；预览媒体、箭头、计数和缩放控件保持自身不透明度。5173真实打开人物图片预览，1280×720全屏遮罩计算背景为`rgba(20, 23, 28, 0.5)`、`backdrop-filter:none`，图片自身opacity为1。Vue类型检查、定向`31 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产卡内容整体下移5px：人物、道具、场景共用姓名行的顶部内边距由5px增至10px，使姓名、右侧导入按钮及后续图片区域整体向下增加5px，左右和底部间距不变。5173实测人物、道具、场景三类摘要计算`padding-top`均为10px，对比原统一值5px精确增加5px。Vue类型检查、定向`32 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 预览留白悬停变白修复：根因是覆盖全屏留白的关闭按钮命中全局`button:hover:not(:disabled)`背景色，鼠标进入时由透明变成浅白。首轮普通类选择器实测仍被更高优先级全局规则覆盖，关闭层为`rgb(241,243,245)`，未报告通过；改用带`button`和`:not(:disabled)`的专用hover/focus/active选择器后，5173鼠标实际停在留白时关闭层为`rgba(0,0,0,0)`，全屏遮罩保持`rgba(20,23,28,0.5)`，边框0、阴影none、transform none。Vue类型检查、定向`32 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 人物姓名标题单独下移5px：统一资产卡仅为人物姓名标题增加专用类和`translateY(5px)`，道具/场景标题、右侧导入按钮及图片区域位置不变。5173实测人物姓名顶部由122.75px变为127.75px，精确下移5px；导入按钮保持121px、位移0，图片区域位置不变，计算transform为`matrix(1,0,0,1,0,5)`。Vue类型检查、定向`33 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产图片多选导入：统一资产导入文件框启用`multiple`，所选图片在同一资产队列中按文件顺序逐张校验、识别角度、归档、审核并持久化到对应槽位；同一批次识别到重复角度时失败关闭，禁止后一张静默覆盖前一张。5173实测姓名行导入按钮存在，关联文件输入为`type=file`且DOM属性`multiple=true`。Vue类型检查、定向`34 passed, 3 subtests passed`、文档状态及diff格式检查通过。为避免覆盖正式资产，本轮未提交真实图片；未操作8787/8194服务，未重启5173。
- 2026-08-12 资产角度标签遮罩与文案统一：人物、道具、场景共用角度标签的深色遮罩透明度改为20%；仅显示层将角度统一格式化为`数字度 + 空格 + 说明文字`，例如`0度 正面全身`、`45度 左全身`、`180度 背面全身`，底层固定角度标签和自动导入识别契约不变。首次5173实测发现3D审核图仍为`3D front_0`等旧英文格式，未报告通过；补齐七类3D中文角度映射后复测，人物6个、道具24个、场景8个标签全部匹配统一格式，非法标签计数均为0，三类遮罩计算背景均为`rgba(28,31,35,0.2)`。Vue类型检查、定向`35 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 全剧集下拉框无法点击修复：5173复现确认点击后`aria-expanded=true`且菜单DOM已生成，但工作流标题栏`overflow:hidden`把菜单裁掉，菜单位置命中下方流程面板而非选项。标题栏改为`overflow:visible`并提升到z-index 60；复测菜单位置实际命中“全剧集”按钮、选项数2，选择“第1集”后触发器更新为“第1集”且菜单关闭，重新打开后Escape关闭通过。Vue类型检查、定向`36 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产角度标签缩放定位：人物、道具、场景及3D审核图共用角度标签整体缩放为0.8倍，transform原点固定左上；标签从原top/left 8px改为2px，最大宽度按缩放比例反算，避免缩小后过早截断。5173实测人物、道具、场景三类标签transform均为`matrix(0.8,0,0,0.8,0,0)`、原点`0 0`，相对图片框视觉顶部和左侧间距均精确为2px。Vue类型检查、定向`37 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产角度标签边距调整为3px：按用户最新要求覆盖上一轮2px设置，人物、道具、场景及3D角度标签的顶部和左侧边距统一改为3px，0.8倍缩放和20%遮罩透明度不变；最大宽度同步按3px边距反算。5173实测三类标签相对图片框顶部和左侧视觉间距均精确为3px，transform仍为0.8、背景仍为`rgba(28,31,35,0.2)`。Vue类型检查、定向`37 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 主力开发界面授权规范：`Dev_MainDev.md`新增强制规则，未经用户针对具体界面改动的明确允许，不得新增、删除、移动、改名或修改界面元素，也不得改动布局、文案、样式和交互；新增功能需要界面入口时，仅允许增加对应按钮，其他界面变化必须另行获得明确允许。本次仅修改规范与项目记忆，未操作8787/8194/5173服务。
- 2026-08-12 资产基准照确认按钮移位：人物、道具、场景共用资产卡将基准照确认入口从图片遮罩内移至姓名行，按钮文案改为“确定基准照”并固定在“导入”左侧；图片上的“就要这张”按钮及其确认事件绑定永久删除，确认入口继续复用原有基准照确认队列和台账流程。5173实测人物卡姓名行同时显示两个按钮，“确定基准照”右边界1208px、“导入”左边界1212px，前者确在左侧且间隔4px；全页“就要这张”按钮计数为0。Vue类型检查、定向`37 passed, 3 subtests passed`通过。未点击会启动正式角度生成任务的确认按钮，未操作8787/8194服务，未重启5173。
- 2026-08-12 全流程剧集下拉固定五集一排：共用`WorkflowEpisodeSelector`将可见剧集下限固定为5，项目剧集数超过5时继续按真实数量显示；菜单继续使用五列网格，避免单集项目只显示一项而被误认为一排一集。5173实际打开下拉框显示“第1集”至“第5集”，五项纵坐标均为135.5px，横坐标依次为971、1029.59、1088.2、1146.8、1205.4px，确认同排五列。Vue类型检查、定向`38 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产卡整体上移5px：人物、道具、场景共用资产卡姓名行顶部内边距由10px改为5px，使标题、确定基准照、导入按钮及后续图片区域整体向上5px，内部横向关系和其他间距不变。5173实测人物标题顶部由上一轮记录的127.75px变为122.75px，精确上移5px；确定基准照与导入按钮顶部均为116px，首张图片顶部157px，三类共用摘要计算`padding-top`均为5px。Vue类型检查、定向`38 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 对话消息操作栏整体下移2px：对话消息底部共用`.message-actions`由仅水平`translateX(-10px)`改为`translate(-10px, 2px)`，时间及复制、赞、踩、分享、引用五个按钮作为同一操作栏整体同步下移2px，其他消息布局不变。5173浏览器实测已加载规则为`transform: translate(-10px, 2px)`；当前三个持久项目均无消息操作栏DOM可供无副作用悬停测量，因此未发送会调用对话后端的新消息。Vue类型检查、定向`39 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 对话气泡垂直间隔增加5px：左右消息共用`.message-row`的底部间距由5px增加为10px，因此相邻左右气泡的上下间隔统一增加5px，消息内容、宽度和左右对齐不变。5173浏览器实测已加载`.message-row ... margin-bottom: 10px`；当前持久项目没有消息行DOM，未为测量间距而发送会调用后端的新消息。Vue类型检查、定向`40 passed, 3 subtests passed`、文档状态及diff格式检查通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 当前任务弹窗交互与视觉优化：不新增功能，仅优化现有任务弹窗。任务区改为浅灰分区背景，运行摘要与历史版本使用独立白色信息卡；任务树增加层级左侧蓝色指示、48px点击行、22px折叠热区、清晰悬停/键盘焦点及状态胶囊，补齐失败、暂停、失效状态配色；长列表保留独立纵向滚动。5173实际打开任务弹窗，任务行高度48px、鼠标指针`pointer`、折叠热区22×22px、进行中胶囊背景`rgb(234,242,254)`；根任务实际点击后`open=false`，再次点击恢复`open=true`；列表`scrollHeight=3343px`、可视高度565px且`overflow-y:auto`。Vue类型检查、定向`41 passed, 3 subtests passed`通过。未操作8787/8194服务，未重启5173。
- 2026-08-12 资产导入角度文字与文件名识别：人物多图导入按“文件名明确角度→图片内角度文字OCR→人物画面朝向”三级顺序归位，文件名支持0/45/90/180、左右、正背侧面及全身/半身；OCR与朝向拆成独立识别，避免人物姿态覆盖图片中明确的角度文字。仍无法唯一确认时统一在对话区提示“xx度图片不能识别，请把图片命名为对应的角度”，其中xx优先取文件名或识别文字中的0/45/90/180。实际8787视觉接口使用放大的“45度 左全身”标签截图取得过`左 45°全身`结果；同时确认混合姿态提示会误判，因而已拆分为OCR优先的两段调用。为避免覆盖正式资产未将测试图写入现有槽位。Vue类型检查、定向`41 passed, 3 subtests passed`、文档状态及diff格式检查通过。未重启或停止8787/8194/5173。
