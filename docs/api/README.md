# API 文档入口

本目录只提供 API 使用导航，不维护第二份接口规则。

- 设计与安全规则：`../contracts/API接口规范.md`
- 契约清单：`../contracts/API_CONTRACTS.md`
- 可机器校验契约：`../contracts/openapi.yaml`
- 通用命名：根目录 `AI_EXECUTION_NAMING_RULES.md`

新增接口固定顺序：更新 OpenAPI → 生成/更新共享类型 → 实现服务端 → 接入前端服务层 → 契约测试 → 真实链路验证。
