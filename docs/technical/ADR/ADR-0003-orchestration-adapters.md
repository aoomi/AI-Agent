# ADR-0003：编排与外部工具适配

状态：已接受  
日期：2026-08-07

## 决定

LangGraph 作为可恢复流程编排方向；Dify、ComfyUI 和模型服务通过平台适配层接入，业务插件只依赖平台能力契约。

## 原因

编排状态、业务规则和第三方工具 API 若混在页面或插件中，会阻碍替换、恢复和私有化部署。

## 影响

适配器失败必须返回统一错误；禁止静默切换云服务；工具更换不得改变业务数据契约。
