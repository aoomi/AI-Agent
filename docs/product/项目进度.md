# 项目进度

## 2026-08-11：M9.197 可观测性生产化最小V1（开发完成，待独立测试）

- 统一可插拔exporter已覆盖结构化日志、指标快照、span和告警；新增持久JSONL与原子Prometheus textfile实现，默认行为向后兼容。
- 队列积压故障告警、request/trace关联、嵌套秘密拒绝和counter/标签约束专项5项通过；未启动模型或正式任务。
- 正式服务生命周期接入、全链关联ID、仪表盘和生产告警演练仍为后续工作，本增量不提前宣称整个可观测性完成。
- 首轮独立测试专项通过，但全unit被联网搜索旧测试夹具阻断：用例未隔离现行Google News/Google免Key后备，实际取到网络结果。已同步为五provider全失败的确定性测试，当前仍按软件测试退回处理。
- 测试夹具同步后，联网搜索与可观测定向14项、全unit 521项及9个子测试通过，1项环境跳过；已重新提交独立软件测试。
- 第二轮复测发现敏感指标标签序列化后可绕过秘密检测；已在标签注册、快照及JSONL/Prometheus持久入口统一失败关闭，专项6项通过，等待冻结快照复测。
- 冻结快照主线自检已通过：定向18项、完整unit 554项及9个子测试通过，1项环境跳过；文档状态门禁、关键Python编译和限定diff-check通过。BUG045首状态已与唯一索引同步，继续等待独立软件复测。

当前任务和排队状态只以[`当前开发状态.md`](当前开发状态.md)为准。本文件以下内容均为累积交付证据，旧“待测试/待稽查”文字不恢复为现行任务。

- M9.198（历史启动记录，已被BUG046待测试状态覆盖）：启动单机V1真实新项目大纲到增强导出验收，需补齐停止、重启恢复、局部重做和全链权威证据；排队BUG039—041保持未分析。
- M9.198 / BUG046正式首败：全新project_id后端无stage_state，创建成功界面却保留旧项目大纲/剧本/分镜和资产投影；根因是创建分支切换store后未加载新项目流程，现进入框架级隔离整改。
- M9.198 / BUG046主线整改：统一同步清空十阶段投影，创建/编辑保存后等待新project/session完整加载；正式旧项目3人物切空新项目的立即帧/稳定帧均为零旧内容。定向/contract`45 passed`、全unit`556 passed, 9 subtests passed`、typecheck及83模块构建通过，待独立测试。
- M9.198 / BUG046（历史子问题记录，最终稽查通过，已关闭）：正式连续三轮旧项目3人物→空新项目切换，立即帧/稳定帧零旧内容且后端stage_state为空；统一十阶段同步清空、创建/编辑新会话加载及晚到围栏通过。定向/contract`45 passed`、全unit`556 passed, 9 subtests passed`无skip，typecheck、83模块构建和最终稽查通过。
- M9.198 / BUG047正式首败：真实大纲停止后任务/模型已释放，界面提示可重新生成，但再次运行返回`workflow is cancelled`；统一工作流缺少取消终态后的显式新代际恢复协议。
- M9.198 / BUG047主线整改：cancelled工作流只允许严格更新的stage generation重新激活，同代、零代及旧代晚到均拒绝；正式同项目重载后真实32B大纲新代际完成并释放模型。定向`120 passed`、完整unit`558 passed, 9 subtests passed`和编译通过，待独立软件测试。
- M9.198 / BUG047独立测试阻塞：定向`120 passed`无skip；完整unit为`3 failed, 555 passed, 9 subtests passed`，失败均由并行BUG038当前索引/Tracker状态冲突触发。保持待测试，等待外部事实源收敛后重跑，不进入稽查。
- M9.198 / BUG047独立软件复测通过：定向`120 passed`、完整unit`560 passed, 9 subtests passed`且无skip；编译、文档门禁和限定diff-check通过，待只读代码稽查。
- M9.198 / BUG047最终复稽查通过并关闭：严格更新generation恢复、同/旧代拒绝、正式重启生成及资源释放闭环；整改复测完整unit`560 passed, 9 subtests passed`无skip。继续真实剧本至增强导出。
- M9.198 / BUG048正式首败：大纲UI确认后剧本仍报前序未完成；项目stage_state confirmed、ledger伪completed无confirmation、LangGraph pending_confirmation三方分裂，进入统一公开投影权威边界整改。
- M9.198 / BUG048主线整改：非upscale公开projection不能伪造completed/confirmation/generation/权威证据；正式旧坏状态已自动收敛，真实32B剧本完成并推进storyboard。关联`135 passed`、完整unit`564 passed, 9 subtests passed`，待独立软件测试。
- M9.198 / BUG048独立软件测试通过：关联`135 passed`、完整unit`564 passed, 9 subtests passed`无skip；正式确认推进、编译与文档门禁通过，待只读稽查。
- M9.198 / BUG048最终稽查通过并关闭：统一projection权威边界和确认唯一入口闭环，正式旧状态收敛及真实剧本通过；继续分镜脚本至增强导出。

- M9.197 / BUG045（最终稽查通过，已关闭）：统一RecordExporter、持久JSONL、Prometheus原子快照与告警最小闭环通过；注册及全部持久exporter统一拒绝秘密、非有限数值、非法指标/标签和畸形快照。独立定向`41 passed`、完整unit`555 passed, 9 subtests passed`无skip，最终复稽查通过。

- M9.195 / BUG043（最终稽查通过，已关闭）：generation优先、同代revision单调、revision0唯一首事件、legacy checkpoint一次迁移、拒绝事件权威恢复及正式开始/终态/确认/恢复revision推进通过；独立关联`132 passed`、完整unit`554 passed, 1 skipped, 9 subtests passed`，Node动态`23 passed`，Python编译/typecheck/83模块构建及最终只读稽查通过。

- M9.196 / BUG044（最终稽查通过，已关闭）：唯一当前状态索引、Tracker双向完备、组合M/BUG编号归一、历史豁免、本地链接及contract门禁闭环；生命周期无关Tracker变异覆盖待测试/待稽查/已关闭并保护相邻条目。最终专项与contract`21 passed`、全部contracts`40 passed, 51 subtests passed`、完整unit`555 passed, 9 subtests passed`且无skip，最终只读稽查通过。

- M9.192 / BUG035（最终稽查通过，已完成）：review_export/upscale成功证据已延迟到owner+generation统一提交区；权威ledger批次与Graph callback共享失败回滚边界，cancel-first、失租旧代及Graph异常零成功证据，commit-first唯一消费租约。独立补测多集第二条故障整批回滚、重复提交幂等、三维身份隔离及Graph有/ledger无崩溃恢复；关联`147 passed`、完整单元测试`495 passed, 1 skipped, 9 subtests passed`，编译、类型检查与83模块构建通过。正式8787已加载当前实现且全资源空闲。

- M9.191（最终稽查通过，已完成）：分镜脚本生成后自动创建资产卡并进入统一FIFO生成缺失人物、道具和场景基准图；资产页不再要求手动上传，也不再清空已生成图片和确认状态。入口single-flight按项目/session隔离，旧项目未决Promise不阻塞新项目首次进入。P1/P2阻塞confirm/prepare动态2/2；定向102 passed、1 skipped；全单元490 passed、1 skipped、9 subtests；编译、类型检查与83模块构建通过，BUG-20260811-037已关闭。

- M9.190（最终稽查通过，已完成）：人物资产错误提示由全局批次层下沉到对应人物卡；打开/确认苏璃时不再显示云长老等兄弟资产的旧失败信息，人物角度引用仍按当前人物隔离。定向118 passed、1 skipped、3 subtests；完整单元测试489 passed、1 skipped、9 subtests；编译、类型检查与83模块构建通过，BUG-20260811-036已关闭。

- M9.189（最终稽查通过，已完成）：生图能力安装改为进程内幂等，避免并发请求重注册运行中Klein 9B提供器；生产台账使用升级后24列显式写入；人物全身图统一头顶留白至少8%、脚底留白至少3%，两者均为下限而非固定值，并进入提示词、归一、验收和规范。动态inflight通过，定向209 passed、1 skipped、3 subtests；全部单元测试458 passed、1 skipped、9 subtests；编译、类型检查与83模块构建通过，BUG-20260811-033已关闭。

- M9.188 / BUG034（最终稽查通过，已完成）：阶段入口已移除任意已确认scope的`trusted`整阶段旁路；人工确认、延迟确认、恢复和入口统一使用同一完成判定。image/video/audio/subtitle按storyboard shot census校验至少一个完整分集，额外/非法/别名碰撞scope、缺镜头、缺当前指纹/批次或确认不匹配均失败关闭。专项`79 passed, 1 skipped`、扩大关联`192 passed, 1 skipped`，最终代码专项`61 passed, 1 skipped`；正式8787已加载且全资源空闲。
- BUG034两轮首败整改：媒体gate保留并核对全部实际scope，目标外、非法、重复/别名及非shot类型失败关闭；媒体completed还须具备当前fp/batch和与之精确一致的confirmation。证据矩阵定向`39 passed`、扩大关联`202 passed, 1 skipped`、编译/typecheck/83模块构建通过，待独立复测。

- BUG035已完成最终稽查并关闭：upscale authority不再在物理执行函数内提前提交，统一提交协议及重启一致性恢复已正式加载。

- BUG028 / M9.183（最终稽查通过，已关闭）：增强四步已归统一review_export阶段；结构化证据从生成、SQLite、重载、确认到manifest端到端保留。server-owned证据与前端progress隔离，generation/revision CAS拒绝同代篡改与旧代重放；正式PID43900已加载最新代码，worker、资源池、任务及模型队列空闲。

- M9.187（最终稽查通过，已完成）：人物首张定位基准统一为0°正面平视完整全身，人工确认后按左45°→右45°→90°→180°→0°半身逐槽生成确认；六角度提示词已完成目标/禁用角度消歧，身份描述不再携带景别词。人物3D输入同步0°正面全身；道具和场景45°基准保持不变。定向73 passed、3 subtests；全部单元测试448 passed、1 skipped、9 subtests；编译、类型检查与83模块构建通过，BUG-20260811-032已关闭。

- BUG028 / M9.183最终权威边界整改完成，待独立软件测试：upscale证据与前端progress物理分列，公开single/bulk投影不能伪造或覆盖指纹、批次、generation、证据、confirmation及已确认生命周期；真实生成使用跨实例单调generation、authority history与revision CAS，拒绝旧代/旧tuple重放和同代篡改。确认及enhanced导出精确绑定fp+batch+generation。专项`32 passed`、关联`161 passed, 1 skipped`，未启模型或操作正式服务。
- M9.186（历史左45口径，已被M9.187覆盖）：自动三次生成和required失败项自适应提示继续有效；首张角度已改由M9.187的0°正面全身执行。

- BUG028 / M9.183最终稽查阻断整改完成，待独立软件复测：ProductionLedger按fingerprint+batch隔离证据代际；同代missing/null保留，任一代际变化先清旧production/audit evidence与confirmation，再只接收本次非空规范证据。同fp新batch缺证拒绝，新证据重新确认后跨实例authority放行。专项`36 passed`、关联`203 passed, 1 skipped`，pycompile/typecheck/83模块构建通过，未启动模型或操作正式服务。

- BUG028 / M9.183最新稽查整改软件测试通过，待稽查：前端deep-watch动态保留嵌套证据，SQLite missing/null跨实例与确认不擦除；enhanced缺证失败关闭、legacy仅base、authority精确绑定fp+batch并拒绝同fp旧批次。四步骤失败/取消8组、原子回滚、规范JSON、真实manifest及扩大关联`181 passed, 1 skipped`，pycompile/typecheck/83模块构建通过。

- BUG028 / M9.183最新稽查整改完成，待软件复测：upscale深watch与SQLite merge不再擦除结构证据；enhanced缺权威证据失败关闭，legacy缺省仅base；客户端与authority精确绑定fp+batch并拒绝同fp旧批次。专项44项、关联`171 passed, 1 skipped`、交付链4项及pycompile/typecheck/83模块构建通过。

- M9.185（最终稽查通过，已完成）：资产内部生图/3D子任务解除循环门禁；主按钮只补缺失图片，禁止全量purge，单卡重做为唯一替换入口。服务端持久化校验本地媒体，旧页面不能回写失效URL。媒体门禁12/12、路由17/17、迁移7/7、关联`171 passed, 1 skipped`及最终稽查通过；BUG-20260811-030已关闭。

- BUG028 / M9.183端到端证据软件测试通过，待稽查：SQLite跨实例reload/confirm保持嵌套证据类型，enhanced files/manifest只取权威ledger并忽略客户端篡改，旧batch拒绝，base有明确legacy边界；关联`180 passed, 1 skipped`，pycompile/typecheck/83模块构建通过。

- BUG028 / M9.183复稽查软件测试通过，待稽查：production_evidence结构化严格JSON、全树规范指纹、确认导出及资产FIFO owner/epoch隔离全部通过；专项45项、扩大回归`180 passed, 1 skipped`，pycompile/typecheck/83模块构建通过，未启动模型。

- M9.184（最终稽查通过，已完成）：资产页所有生产操作统一项目会话FIFO，自动和手动操作按提交顺序执行，停止即时并使未执行旧代任务失效。切项目不重置FIFO尾链；导入、修复、确认、3D生成/确认及超分具备固定project/session响应围栏和定向持久化。独立动态确认新项目严格等待旧已启动操作结束、最大并发1，旧响应零污染；关联`164 passed, 1 skipped`及最终稽查通过，BUG-20260811-029已关闭。

- M9.183 / BUG-20260811-028（历史软件测试记录，已由M9.183最终闭环覆盖）：增强指纹完整绑定输入与逐步证据、缺证失败关闭、原子台账及enhanced导出门禁通过；资产按钮统一FIFO与session/stop契约成立。扩大回归`178 passed, 1 skipped`，pycompile/typecheck/83模块构建通过。
- BUG028扩大关联：增强相关157项通过、1项跳过；两条共享资产队列旧源码断言失败（现行已为queue/enqueue统一入口），已按首败记录；编译/类型/83模块构建通过。

- M9.183（历史开发记录，已由M9.183最终闭环覆盖）：成片超分、字幕OCR、人脸一致性与最终审核已从前端逐集直调迁入一次`review_export/upscale`服务端批次；失败不再伪造skipped，项目/session/abort晚到隔离补齐。专项与关联`117 passed, 1 skipped`，未启动模型。

- M9.182（最终稽查通过，已完成）：场景资产已强制为纯空可复用地点；地点识别覆盖古装、居住、餐饮、医疗、校园、办公、商业、交通、工业、宗教、文体与自然环境。服务端按精确项目读取assets/outline权威人物名单，人物姓名、动作、状态、群体行为及以地点结尾的剧情句均在任务登记前拒绝；当前项目只保留“宗门试炼场”空场景卡。

- BUG025 / M9.180（最终稽查通过，已关闭）：二维生图统一image、asset_3d保持assets/3d；资产批次严格单飞与会话代际隔离、分镜完整集原样保留和仅补缺集闭环。扩大回归197项及正式空闲验收通过。

- M9.181（最终稽查通过，已完成）：全局`notify()`提示统一显示为项目对话框内的助手“系统提示”消息，连续重复提示去重并自动滚动；底部蓝色Toast已移除，提示不回写助手历史，业务状态事实源保持不变。关联115项、动态提示矩阵、类型检查与83模块构建通过。

- BUG025第四轮软件复测曾通过，现因持久投影边界稽查退回并已修、待复测：generateAllAssetImages严格同Promise/单批次，异常释放可重试；切换与stop建立不同新flight，旧响应/finally零污染。

- M9.180（历史第四轮软件复测记录，已由M9.180最终闭环覆盖）：动态专项39项、关联155项及3个子测试、Python编译、Vue类型检查和83模块生产构建通过；未启动模型。

- BUG024 / M9.179（最终稽查通过，已关闭）：review_export审核与导出已归统一内核；审核/媒体权威台账、base/enhanced版本绑定、多集原子门禁、single-flight、停止与会话隔离闭环。关联126/126及正式空闲验收通过。

- BUG024复稽查2（已由最终稽查关闭）：关闭审核仍强制媒体台账门禁；base只认composition、enhanced只认upscale，交叉指纹与静默回落均拒绝；多集缺一整批零导出调用。

- M9.179（最终稽查通过，已完成）：权威导出、阶段/会话隔离、停止与代际门禁全部通过；关联126项、编译/类型/构建及正式空闲验收通过。

- M9.178（最终稽查通过，已完成）：审核节点取消矩阵10/10、八阶段及停止隔离关联126项、稽查回归128/128、Python编译、Vue typecheck与82模块生产构建通过；generation/commit与跨租户停止围栏闭环，正式资源和模型空闲。

- M9.178（历史开发记录，已由M9.178最终闭环覆盖）：8个服务端阶段已统一lease/cancel checkpoint；所有正式停止先取消阶段再精确停止子任务，完整租户身份强制门禁；关联135项通过，前端构建待具备npm的测试环境执行。

- BUG-20260811-022（最终稽查通过，已关闭）：历史孤儿已持久quarantine identity与recovery证据，Graph image=failed、outbox/nonterminal=0；真实项目和普通terminal保护通过。

- M9.177（最终稽查通过，已完成）：8实例旧schema迁移、完整幂等契约、正式service_scope schema与唯一空闲worker均通过。

- M9.176（最终稽查通过，已完成）：提供方并发容量、实时占用、同级公平分流、全满背压、显式provider、异常释放及在途热插拔阻断均闭环；专项72项、动态20线程、关联96项及最终只读稽查通过。

- BUG-20260811-020（最终稽查通过，已关闭）：orphan自然结束后新PID加载；旧cancelled job未误恢复，任务/资源/Ollama/Comfy全空。

- M9.175（最终稽查通过，已完成）：专项32项、补充8项、关联182项及3个子测试通过；正式加载、旧orphan保护、资源与模型队列空闲及最终只读稽查通过。

- M9.174（最终稽查通过，已关闭）：前后端仅在waiting_confirmation/confirmed/completed且错误精确匹配时清理；pending_confirmation及所有非成功状态保持。三类资产动态矩阵96/96、关联134项及最终稽查通过。

- M9.173（最终稽查通过，已关闭）：新PID正式8扩展全部唯一active/enabled、contract_validated=8/8，任务、资源、Ollama与Comfy全空；同一orphan阻塞已自然解除。

- M9.172（最终稽查通过，已关闭）：run-stage使用分布式租约、commit guard和Graph generation围栏；SQLite request_cancel是唯一取消裁决，同/跨实例cancel-first与commit-first结果、项目状态和Graph完全一致。

- M9.163（文档整改完成，待软件测试）：统一人物六格、Blender七角度、Qwen3-VL 32B生成与Qwen2.5 72B审核、H3→Qwen‑Edit串行职责、`_vN`保留旧版及单镜LoRA受控例外；LangGraph/SQLite/统一任务仓库保持强制唯一事实源。

- M9.160（最终稽查通过，已关闭）：8类基础设施扩展点支持同点多提供方安装、唯一活动绑定、显式激活/回滚、按提供方启停与卸载；失败替换原子不变，内置刷新保留插件。软件108项及3个子测试、并发压力、正式8项绑定及最终稽查全部通过。
- M9.159（最终稽查通过，已关闭）：生产能力注册表支持同能力多提供方共存、优先级/健康/启停、同级轮转、显式provider和安全回退；内置刷新不再覆盖插件提供方，健康接口披露完整绑定状态。关联105项及3个子测试、动态并发路由矩阵、Python编译和正式27条绑定只读验收通过。

- M9.158（历史首格，已被M9.187覆盖）：六格与Qwen‑Edit人物档案能力继续有效；当前0°正面全身为首张基准及固定种子源。

- M9.157（最终稽查通过，已关闭）：节点内调度由全局单锁升级为可配置资源池。Mac默认accelerator 1、CPU音频2、控制8并发；各池具独立队列上限、优先级、超时、取消和背压，工作节点按真实容量/活动票据上报，快照保留旧字段并新增active_items/pools。
- M9.157节点清理：心跳过期worker同时从持久发现仓库和进程内路由投影清退，不再永久显示历史PID僵尸节点。

- M9.156（历史口径，已被M9.163覆盖）：当时人物资产为四格；当前唯一执行口径为M9.163六格。
- M9.156半身构图职责继续有效；“三张全身图”组合属于历史并已被M9.187覆盖。当前六格为0°半身加0°/左45°/右45°/90°/180°五张全身图，六图共同锁定人物；半身仍不进入TripoSR，唯一几何输入为0°正面全身。
- M9.156分镜参考：0°半身独占人脸身份，0°全身锁体型服装，90°/180°锁跨角度轮廓；四图均进入生成与审核，不再被旧`audit_only`过滤。
- M9.156后端：人物基准提示、Klein9B结果元数据、重试与验收统一front_full；旧45°资产自动失效。框架提取保留项目/session单飞隔离。
- M9.156门禁：0°确认同步ledger成功后才生成90°与180°，同步失败或项目切换不启动后续角度。
- M9.156隔离与LoRA：角度链逐请求校验project/session/abort；厚涂0°加载已批准Klein9B厚涂风格LoRA，未批准人物LoRA不加载，结果回写实际LoRA证据。
- M9.156恢复：load只补资产框架，图片中断不自动续跑模型；成片与3D资产两份现行规范均统一为0°全身→确认→90°/180°全身→0°半身，人物建模只复用0°全身基准。
- M9.156 3D闭环：正式人物3D请求、后端输入门禁及AI运行提示均为front_full已确认0°基准；缺图、未确认或旧45°标记均在重模型前拒绝。

- M9.154（历史口径，已被M9.156覆盖）：手动上传方案不再是当前流程；project/session单飞和晚到隔离保留。
- M9.154补充：提取单飞按项目/session隔离，晚到响应及排队持久化均有project/session/abort围栏。
- M9.154界面：资产页移除批量自动生图绑定，统一由人物/道具/场景卡片手动上传基准照和角度照。
- M9.153（已完成）：正常批量合片已由服务端`composition`执行器接管；前端单次提交全剧命令，合片前验证视频/音频/字幕媒体包证据一致性。
- M9.153补充：合片批次在执行前验证集数为正整数且唯一，并具有项目切换/session/abort隔离。
- 合片事务已增加项目ID/session/abort隔离，切换项目后旧响应不会污染新项目。

- M9.152（最终稽查通过，已关闭）：分镜确认与资产提取链已接入统一LangGraph。生成图片前自动确认分镜并同步生产台账，资产提取由服务端`assets`执行器完成，前端只发一次阶段请求。
- 分镜确认改为显式布尔事务，任何审核或持久化失败都不会启动资产提取。
- 资产入口已增加single-flight并发门禁，重复点击只执行一次确认与资产阶段。

- M9.151（最终稽查通过，已关闭）：Blender 3D阶段新增24FPS/96帧源视频；分镜命中已确认3D资产和人物正面定妆照时，正式视频入口自动使用本地MiniMax H3 Ref2VA，把源视频作为几何运镜参考、定妆照作为身份参考。缺少任一输入继续使用Wan2.2；H3 prompt已纳入任务心跳、超时、停止、恢复与关闭回收。剧本/分镜阶段按批次或分集权威范围确认，台词行/镜头明细不再造成重复门禁；当前项目已从台账恢复剧本完成态并开放分镜。
- 真实3D产物已验证：白瓷仙壶源视频1024×576、24FPS、96帧、4秒；确认归档、重启恢复与媒体读取均通过。
- 分镜Qwen3-VL-32B改为仅产出紧凑视觉导演方案，服务端按已确认剧本时间段确定性编译15—23镜并硬校验；正式项目已产出20镜、0—60秒并持久化待确认。同集多镜不再被故事事实库误判为重复集数；界面保留服务端具体错误。
- 稽查整改已补齐新3D结果`source_video_spec`持久化，以及分镜台词/旁白零遗漏硬门禁。
- 对白覆盖已增加说话人一致性门禁。

- M9.150（历史审核暂停口径，已被M9.163覆盖）：当前唯一执行口径为Qwen3‑VL 32B生成、Qwen2.5 72B固定审核。

- M9.149（最终稽查残留整改完成，待复测）：大纲至成片统一使用LangGraph 11阶段控制面及权威台账。公开确认执行图先验、台账后写与异常回滚；旧JSON流水线只投影LangGraph状态，取消由图持久化且重启后不可批准、恢复或继续执行；24处重负载入口执行前后复核租约世代；五个前端入口只提交单次阶段命令，服务端接管拆批、有限重试、终审、修复和媒体包。开发回归102项与3个子测试通过。

- M9.147：对话框支持上传图片反推正向/负向提示词；已接入AI图片超分、视频超分与RIFE插帧、MuseTalk口型同步，以及按需启用的PuLID和ReActor身份修正。所有结果独立落盘，原始资产保持不变，真实链路烟测和前后端回归验证通过。

- M9.146：3D生产规范升级到3.0；新增最终脸强制2D接管、多人同框按Mask/Depth分层、TTS动作偏差0.5秒退回、2D/3D冲突仲裁、系统故障人工降级、共享资产版本锁、资产ID冲突保护及多机位视线连续性，并同步总生产规范和AI提示词规范。

- M9.146：已建立FLUX.2 Klein 9B国风厚涂测试库：风格2、女性1、男性1、灵兽1，全部在9B 8-bit真实加载出图；古代幻想风格通过厚涂候选验收，其余4项保持测试不合格隔离，旧FLUX.1权重已退出活动目录。
- M9.146接入：国风厚涂Klein‑9B基准图已加载获批古代幻想风格LoRA；女性、男性、灵兽候选的接线保留，但逐模型视觉与许可门禁禁止其进入正式命令。运行前校验大小与SHA‑256，结果记录实际加载清单。

- M9.145（历史角度口径，已被M9.163覆盖）：当时的左45°首图与M9.156三槽均已废止；当前人物输入统一为六格，Blender统一七角度。

- M9.141（历史文本路由，已被M9.163覆盖）：曾使用9B/27B/122B三档Qwen3.5；当前唯一口径为qwen3-vl:32b生成、qwen2.5:72b初审→最多一次修正→终审。其剧情连续性与结构化门禁继续保留。
- M9.142：TripoSR官方MIT模型已下载至三维模型正式目录；1.68GB权重SHA-256与官方LFS记录一致，549个张量通过安全元数据加载。
- M9.95：人物、道具和场景资产统一为每个资产一张横向卡片；卡片内固定按基准照、后续角度照顺序横向排列并显示角度名称，窄屏保持横向滚动；导入和重新生成严格作用于当前图片槽位。
- M9.92：新建项目和项目设置已恢复“视觉风格”选择；项目 `category`、`style` 与生图风格统一使用当前所选本地 LoRA 风格，移除隐藏的“现代真人电影质感”默认覆盖。

最后更新：2026-08-10

| 里程碑 | 状态 | 验证 |
|---|---|---|
| 契约与平台基线 | 已完成 | 契约、单元、入口和隔离测试 |
| M1 Skill 智能体协作 | 已完成 | 注册、映射、生命周期、隔离、HTTP、串联恢复 |
| M2 短剧完整流程 | 已完成 | 11 节点、真实提供方、人工关口、失败/取消/恢复、导出 |
| M3 任务与实时进度 | 已完成 | 查询、详情、取消、恢复、事件投影、前端任务中心 |
| 原版短剧 UI 迁移 | 已完成 | Vue 类型检查、Vite 正式构建、本地入口 |
| M4 可配置系统 AI | 已完成 | M4.1—M4.8 已完成 |
| M5 真实能力提供方 | 已完成 | M5.1—M5.8 已完成 |
| M6 插件安全与供应链 | 已完成 | M6.1—M6.8 已完成 |
| M7 双 AI 协作闭环 | 已完成 | M7.1—M7.8 已完成 |
| M8 短剧真实视频工作流 | 已完成 | M8.1—M8.10 已完成 |
| M9 全行业流程 Skill 机器人 | 已完成 | M9.1—M9.8 已完成 |
| M10 私有化部署与灾备 | 已完成 | M10.1—M10.8 已完成 |
| M11 SaaS 商业化 | 待开发 | M11.1—M11.8 |
| 生产部署与灾备 | 未验收 | 待真实环境验证 |

## M4 状态

- [x] M4.1 开发/稽查 Skill、模型与对话配置契约。
- [x] M4.2 开发与稽查内置 Skill manifest。
- [x] M4.3 模型注册、能力约束和模型选择。
- [x] M4.4 智能体配置版本、权限边界和模型绑定。
- [x] M4.5 模型驱动配置对话与复杂任务入口。
- [x] M4.6 模型、配置和对话 HTTP 路径。
- [x] M4.7 前端 Skill、模型和对话配置界面。
- [x] M4.8 角色隔离、模型切换、对话配置和复杂任务测试。

## M5 状态

- [x] M5.1 提供方统一配置、密钥引用和健康状态契约。
- [x] M5.2 OpenAI 兼容模型客户端、超时、取消和结构化响应校验。
- [x] M5.3 文字、图片、视频和音频提供方适配注册。
- [x] M5.4 限流、有限重试、熔断、降级和错误归一化。
- [x] M5.5 Token、耗时、费用、请求和产物审计。
- [x] M5.6 短剧生成节点绑定真实提供方注册表。
- [x] M5.7 提供方 HTTP 和前端入口。
- [x] M5.8 生产提供方闭环测试。

## 当前验证基线

- 历史分镜人物参考口径（已被M9.163覆盖）：曾仅让正面近照/全身参与推理、侧背仅终审；当前已确认Qwen-Edit六格均进入人物档案、生成与审核链。InsightFace身份、群演差异化、服装及场景道具一致性门禁继续有效。

- M9.125：已下载14个国风浅涂/国风厚涂 FLUX.1 仅测试候选，分类为风格5、女性3、男性3、灵兽3；来源许可、底模、权重结构、大小和SHA-256登记独立测试索引，正式模型索引与商用索引保持0项。

- 历史人物角度构图门禁（已被M9.187/M9.189覆盖）：曾使用正面近照、三张全身图占画68%—78%及6—7头身；当前人物六格以0°正面全身为基准，所有全身角度执行头顶留白至少8%、脚底留白至少3%的安全区下限。
- 生图卡住问题已修复：基准图视觉验收改用强制JSON与320-token完整输出，移除向重试提示注入截断JSON的行为；旧失败状态已清空，队列恢复为空。
- 历史人物候选上限（已被M9.186覆盖）：曾从三张降为两张；当前正式规则为首图加最多2次重生成，总候选上限3张，第三次仍失败才暂停。
- 历史人物基准裁切（已被M9.187/M9.189覆盖）：曾将正面近照脸高归一为45%、发顶保留5%；当前0°正面全身才是唯一人物基准，所有全身角度执行发顶至少8%、鞋底至少3%的纯背景安全区。
- 历史正面近照唯一构图（已被M9.187覆盖）：正面近照不再是首张或唯一基准；当前0°半身仅承担近景身份职责，0°正面全身承担基准和TripoSR唯一人物几何输入。
- 资产固定角度任务已增加完成结果恢复：页面刷新、关闭或请求返回后未持久化时，重新加载项目会按项目、生成批次、资产类型、资产名和角度编号查询真实任务结果，自动回填图片、服装参考与完成状态。当前苏清璃和陆沉共6张角度图已从完成任务恢复，人物状态均为confirmed，ComfyUI队列为空，页面恢复“生成分镜画面”。
- 资产基准图“就要这张”已统一覆盖人物、道具和场景；按钮仅显示在各资产首张定位基准图，确认后调用同一持久化确认入口并生成该资产其余固定角度，生成中或全部完成后自动隐藏。Vue 类型检查、正式构建、20项单元测试和3项子测试通过，本地页面加载验证通过。
- 写实整套候选已完成真实全链路测试：RealVisXL V5.0、CineLightX SDXL 写实风格 LoRA、东亚女性 SDXL LoRA、东亚男性 SDXL LoRA均已实际加载；女性与男性均执行了全身正面、90度侧面、背面。正面比例基本可用，背面方向可控；侧面严格90度、服装/发型跨角度一致性和双鞋完整可见仍未通过，整套保持候选隔离且不进入正式索引。
- 正式 LoRA 目录已移除全部 FLUX 不兼容权重；两份 SDXL 候选权重因实图产生花树器物、脚部遮挡和九至十头身比例而未获准进入生产。固定角度 OpenPose 已改为少女约 6.5 头身模板，正式 LoRA 索引在新候选通过实图前保持为空。
- 历史人物角度链（已被M9.163覆盖）：曾采用 `Schnell → IP-Adapter/OpenPose → Qwen局部修复`；当前固定角度唯一执行链为Qwen-Edit多参考六格，禁止回退到该IP-Adapter固定角度旧链。
- 历史RealVisXL固定角度验证（已被M9.163覆盖）：该链仅保留失败诊断记录，不属于当前Qwen-Edit多参考六格执行链。
- 当前 FLUX.1 Schnell 风格 LoRA 与女性人物 LoRA 已真实加载；英文 `female` 性别归一化缺陷已修复。林婉儿实图仍未匹配银灰及肩发、淡色长裙与国风浅涂淡彩风，当前 LoRA 判定为人设不符合、风格不符合。
- FLUX.1 Schnell GGUF Q8_0 已接通资产基准照正式接口；ComfyUI-GGUF、文本编码器、VAE、项目固定风格 LoRA 与人物 LoRA 实际加载成功，512×768 双 LoRA 接口出图18秒完成。
- 历史人物固定角度工作流（已被M9.163覆盖）：曾固定为 `RealVisXL V5.0 + IP-Adapter Face/Clothing + OpenPose`；当前唯一执行链为Qwen-Edit多参考六格并逐槽人工确认。
- 两份已授权 FLUX.1 风格 LoRA 已允许直接用于 Schnell 基准图：国风浅涂绑定“国风仙韵”，国风厚涂绑定“油画仙韵”；固定角度阶段继续禁止加载这两份 LoRA。
- 历史三张全身图5%边距（已被M9.187/M9.189覆盖）：当前为六格人物链，全部全身角度执行发顶至少8%、鞋底至少3%的非固定安全区下限，未达标禁止放行。
- 图片预览已改为 Codex 式底部悬浮缩放控件；真实页面验收初始适窗34%、放大41%、缩小复位34%均通过。人物角度图新增确定性上下边距归一化，苏清璃正面全身已重新生成并通过全项自动验收。
- 人物角度一致性假阳性已修复：新增脸型年龄、发型细节、服装颜色/领口/扣饰/纹样复杂度二次对照；错误苏清璃全身图已作废，后续不一致候选均被自动拒绝，不再进入人工确认。
- “就要这张”按钮无响应已修复：生产台账改为异步记录，确认不再等待台账接口；失效服装引用自动回退基准图。真实页面点击验证已进入全身角度生成状态。
- 道具图误放行整幅场景的问题已修复：验收由单一“真人禁入”扩展为单道具、纯背景、无场景、无文字、无真人五项门禁，道具基准图同时停止加载人物风格 LoRA。当前错误玉佩图实测返回纯背景、无场景、无文字、无真人四项失败，已不能通过新门禁。
- 资产分类按钮显示人物、道具、场景各自应生成图片总数，按钮宽度随数量展示扩展；Vue 类型检查、Vite 正式构建和分类计数回归校验通过。
- 流程顶部 `Time` 显示项目级流程累计实际耗时，继续生成和暂停恢复均持续累加；Vue 类型检查、Vite 正式构建和累计计时回归校验通过。
- 分镜脚本“继续生成”保留已有镜头，仅补生成剩余镜头；Vue 类型检查、Vite 正式构建和续生成防清空回归校验通过。
- 196 项 Python 单元、契约、集成、端到端和安全测试通过。
- 18 条 OpenAPI 路径校准；TypeScript 契约编译、Vue 类型检查和 Vite 正式构建通过。
- Python 编译、隔离环境可编辑安装、正式启动入口和健康检查通过。
- M9.141：大纲跨集连续性已加强。运行规范新增全剧状态表、标题/事件/人名唯一、阶段单向、角色动机和反派差异化约束；分集批次携带全部历史分集，重复标题、重复梗概和编号异常由后端直接拦截。
- M9.143：已按ComfyUI 0.31官方多角度人物模板接入Qwen Image Edit 2511专用角度LoRA与Lightning 4步LoRA；两份LoRA均为Apache-2.0且哈希一致。苏清璃双参考右90度实图已得到严格纯侧面、完整头脚和一致浅青服装；4步BF16约15分钟，40步质量档30分钟超时。代码候选已切换为Qwen双参考独立角度生成，正式服务待独立软件测试与稽查后重启。
# M9.144 大纲生产任务可靠性与性能闭环（历史模型口径，已被M9.163覆盖）

- 当时采用27B生成与122B审核；该模型口径已废止，当前固定为qwen3-vl:32b生成、qwen2.5:72b初审→最多一次修正→终审。终审不通过保留结果与问题并停止，不自动重跑全剧。
- 当前两模型按需串行加载并在完成、异常或停止后卸载；前端保持整段展示。
- 大纲请求已接入唯一任务ID、真实线程、阶段、心跳、1800秒硬超时、精确停止、重启恢复和关闭回收。
- 模型runner继续登记owner job_id；跨项目并发时仅目标owner允许终止runner，等待锁任务取消后禁止再启动推理。
- 项目切换与页面中断会把原项目大纲持久化为失败并定向停止对应批次，禁止永久generating。
- 开发回归31/31、Vue类型检查与Vite正式构建通过，待独立软件测试。
- M9.148：完成全链路规范口径融合：55—65秒文本入口门禁已实现；剧本段落/分镜映射、视频分段、3D遮挡、身份阈值、版本引用、失败升级、视频选模和题材模板作为待开发验收条款登记，运行时未接线项明确禁止宣称已完成。
- M9.155：场景与道具的Flux.2 Klein 9B 8-bit固定角度生成已显式锁定CFG 1.5、20步，并在结果元数据记录；人物Qwen‑Edit多角度链保持CFG 1.0。
- M9.162（重新最终稽查通过，已关闭）：统一revision长轮询并移除500ms轮询；五元作用域取消防止跨项目干扰，shutdown广播立即释放活动watch。软件128项及3个子测试、HTTP隔离与关闭专项、最终稽查通过。

## M9.161 分镜生成期间增量建立资产卡片

- 状态：重新最终稽查通过，已关闭。
- 分镜服务端完成一集即持久化该集镜头，前端在生成期间持续接收增量。
- 新镜头到达后立即创建人物、场景空卡，并串行补齐道具及完整资产描述；不再等待全剧分镜完成。
- 全剧完成时保留已创建资产卡、已生成图片和确认状态，仅补齐尚未提取的集数。
- 增量读取响应返回后、镜头写入前和资产播种后均校验项目ID、session与取消信号，旧项目晚到结果不得污染当前项目。
- 生成中逐集使用`storyboard_preview`预览能力，不推进assets阶段；全剧分镜确认后先等待预览队列排空，再且仅再提交一次正式assets阶段。
- M9.166（历史人物口径，已被M9.187覆盖）：人物3D输入曾为左45°全身；当前人物改为0°正面全身。道具45°三分之二、场景45°空场景全景以及确认后TripoSR→Blender七角度回填规则继续有效。
- M9.165（最终稽查通过，已关闭）：页面加载不再自动重提10类中断任务；空资产与旧census只播种本地空卡，不提交正式assets。服务端重启同步失败化LangGraph、项目阶段与台账并通知revision；多项目、幂等和资源零占用测试、最终稽查均通过。
- M9.167（重新最终稽查通过，已关闭）：资源调度新增全局/租户/项目三层队列背压；三池限额16/8/4、64/32/16、256/128/64已在正式健康快照生效。同步与三条异步路径scope、内存等待状态投影、关联131项与3个子测试及最终稽查通过。
- M9.167稽查整改：视频即时线程、waiting-memory恢复线程与3D后台线程改为从持久请求显式传递identity，不再依赖HTTP thread-local，异步票据同样执行三层配额。
- M9.167状态整改：任务仓库保留`waiting_memory`，LangGraph仅接收其`queued`投影，修复内存等待视频无法持久化的问题。
- M9.168：修复点击生成图片时与既有assets提取任务冲突导致的英文失败；现在等待并接管服务端阶段结果后继续。人物六格显示顺序固定为0°半身→0°正面全身→左45°→右45°→90°侧面→180°背面。
# M9.170：服装独立道具资产与镜头级换装链（最终稽查通过，已关闭）

- 服装现归入道具栏并以`costume`独立资产管理；每镜逐人绑定服装ID和版本，换装由剧情/分镜决定。
- 每套服装使用单张45°三分之二基准进入TripoSR与Blender并按costume_id归档；角色基础体穿衣/蒙皮/碰撞及穿衣后H3→Qwen链为待开发，当前禁止冒充完成。
- M9.169（最终稽查通过，已关闭）：text/image/video任务保存统一为任务仓库批量事务先提交、JSON后投影；LangGraph投影使用持久outbox。整批回滚、Graph异常终态保护、重启/心跳重放和ack清理测试通过。
- M9.169并发整改：outbox新增全局单调revision与CAS ack，LangGraph新增stage级revision围栏；旧drain暂停与新completed写入交错时不会误删或回退。
- M9.169跨实例整改：新增SQLite stage级投影租约及自动续租；两个服务实例只能串行投影同一项目阶段，持锁后重新读取最新事件，崩溃后自动过期恢复。
- M9.169失租围栏（软件测试通过，待重新稽查）：续租异常或owner变化会向执行体暴露lost；Graph提交前后复核租约，提交途中失租则按SQLite权威任务重建更高revision事件，禁止旧持有者误ack并保证后续自动修复。软件已覆盖两进程抢锁、续租/释放/replay busy、shutdown及正式空闲，关联`137 passed, 3 subtests passed`。
- M9.169 busy容错：租约释放busy由TTL回收，逐库重放异常不逃逸heartbeat且不影响其他任务库；专项59/59，等待正式Comfy合法任务自然结束后补空闲验收。
# M9.171：MiniMax H3 Context IR提示词优化节点（安装完成，生产接线由M9.175覆盖）

- ComfyUI已安装并白名单加载FL2VA/Ref2VA提示词优化节点，本地32B/16K翻译模型真实输出H3结构化提示词。
- Ref2VA自动生产接线、独立持久阶段与卸载门禁已由M9.175完成；FL2VA等其他模式仍保持手动能力，不冒充MiniMax云Context-IR。
- 节点与本地32B翻译模型已补精确依赖锁、下载哈希、源blob哈希、可复现安装脚本、SBOM及漏洞/静态扫描；重装不再依赖浮动标签或浮动依赖范围。
# M9.179 审核导出服务端闭环（开发完成，待独立测试）

- 修复`review_export server stage executor is not installed`造成的大纲到成片最终链路断点。
- 最终审核与导出正式入口接入统一服务端阶段；增加分集唯一性、空命令拒绝、取消checkpoint和已确认审核通过才允许导出的失败关闭门禁。
- 证据：新增`tests/unit/test_review_export_stage.py`；与阶段取消关联测试合计21项通过；前端类型检查及Vite生产构建通过。全程未启动重模型、未操作正式任务。
- BUG-20260811-024已修：最终审核由“逐集×重试次数多次runStage”收敛为一次整批runStage；服务端逐集、有限重试并保留失败证据，前端增加project/session/abort晚到隔离与重复点击单飞。
# M9.194 联网搜索能力框架接入（最终稽查通过，已完成）

- 机器人已具备真实联网搜索路由、可替换提供方、来源引用、失败阻断和检索审计；非联网对话保持本地执行。
- 搜索过程、来源与缓存审计全部显示在对话框；真实公网空缓存LangGraph连续3/3、安全/意图/限流降级矩阵通过；专项147 passed/1 skipped/3 subtests、全unit 543 passed/1 skipped/9 subtests、pycompile+compileall/typecheck/83模块构建及文档状态检查通过。
- 已修复“最新开源文生图”返回翻译/Tamil站点的错误：原句真实HTTP仅返回SenseNova U1、Flux.2 Klein、GLM-Image并按发布日期倒序；工具、客户端、提示库等非模型实体失败关闭。回答与来源同源且LLM调用为0；查询附加条件不丢失，缓存仅同query/count恢复并完整透传审计，当前待最终稽查。
- M9.195 / BUG043（开发完成，待独立软件测试）：LangGraph现在把最近已接受事件与输入事件分离持久化；旧revision、旧generation及已有正代际后的无代际回调不仅不能回退阶段，也不能污染重启恢复/导演读取的权威事件。动态覆盖跨stage与SQLite重载；完整单元测试`507 passed, 1 skipped, 9 subtests passed`。
- M9.195扩大冻结组合`117 passed, 1 skipped`，Python编译、Vue typecheck及83模块构建通过；未启动重模型或正式任务。
- M9.195 / BUG043独立软件测试首败：确定性动态证明`generation=4/revision=10`之后的`generation=5/revision=1`被错误拒绝，生命周期、代际、revision及权威事件仍指向旧owner。围栏必须以generation优先，revision仅约束同代；已按首败规则停止后续回归并退回整改，未启动模型或操作正式服务。
- M9.195 / BUG043退回整改完成，待独立复测：generation优先于revision；新代可从较小revision重新开始，同代revision严格单调，旧/零代继续失败关闭。双实例SQLite收敛与reload在内专项`7 passed`，完整unit`525 passed, 1 skipped, 9 subtests passed`，Python编译、Vue typecheck及83模块构建通过。
- M9.195第二轮整改：新代revision0仅首次可接受，同代重复/缺失0拒绝，revision1推进；旧checkpoint无stage事件时只允许一次迁移。真实run-stage终态、确认与恢复均显式推进revision。事件专项`9 passed`、全unit`543 passed, 1 skipped, 9 subtests passed`，编译、类型检查和83模块构建通过，待独立复测。
