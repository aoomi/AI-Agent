# 部署规范

## 当前状态

本地 Python API 与 Vue 前端入口已具备。生产部署、容器镜像、离线安装、一键安装、数据库、对象存储、Redis、授权注入、升级和回滚属于 M9，当前未验收。

## 生产交付门槛

1. 锁定运行时与依赖版本。
2. 提供不含密钥的环境模板和独立密钥注入。
3. 提供数据库、对象存储、队列和检查点持久化。
4. 提供健康检查、日志、指标、追踪和告警。
5. 提供幂等安装、迁移、升级、回滚和卸载脚本。
6. 完成干净环境安装、故障恢复和备份恢复演练。

未实际存在且未验证的部署命令不得写入本文。
