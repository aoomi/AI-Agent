# AGENTS.md —— AI 总入口

本文件只维护所有执行身份共同遵守的长期规则。具体开发计划、测试方法和稽查细则由对应职责文档维护。

## 一、规则优先级

1. 用户当前明确指令。
2. 本文件的串行闭环、授权边界和完成门禁。
3. 当前身份对应的职责规范。
4. 项目计划、项目记忆和 BUG 记录。

低优先级内容与高优先级冲突时，以高优先级为准并记录冲突；不得自行放宽安全、授权和验收要求。

## 二、启动与记忆恢复

每次启动、上下文压缩、任务恢复或执行身份切换后，必须依次读取：

1. `/Users/aoo/Code/AI Agent/AGENTS.md`
2. 当前身份职责文档：
   - 主线开发：`Dev_MainDev.md`
   - 软件测试：`Dev_TESTING_SPEC.md`
   - 代码稽查：`Dev_INSPECTION_SPEC.md`
3. `/Users/aoo/Code/AI Agent/docs/memory/项目记忆.md`
4. `/Users/aoo/Code/AI Agent/Dev_BUG_TRACKER.md` 中当前问题记录（存在时）

涉及代码或目录变更时，再读取 `AI_EXECUTION_NAMING_RULES.md`、`DIRECTORY_README.md` 及本次变更对应的 API、数据库、安全或业务契约；禁止加载无关规范。

未恢复当前问题、处理状态、已完成证据、未完成项和下一允许动作前，不得修改文件或执行有副作用操作。聊天上下文不得作为唯一记忆来源。

记录归属：

- 长期通用规则：`AGENTS.md`
- 开发里程碑与当前状态：`Dev_MainDev.md`
- 根因、改动和验证结果：`docs/memory/项目记忆.md`
- 缺陷全生命周期：`Dev_BUG_TRACKER.md`

文档与实际代码或运行状态不一致时，先只读核验事实，再由有写权限的身份同步记录。

## 三、身份识别与边界

| 用户意图 | 执行身份 | 权限边界 |
|---|---|---|
| 开发、实现、修复、重构 | 主线开发 | 修改、自检、同步文档 |
| 测试、单元/接口/边界/异常测试 | 软件测试 | 独立只读测试，不修改代码和配置 |
| 检查、审查、验收、稽查 | 代码稽查 | 独立只读稽查，不修改任何文件 |
| 未明确 | 主线开发 | 按用户实际授权执行 |

测试和稽查只提交结果；需要持久化 BUG 状态时，由具备写权限的主线开发或协作记录器写入，测试和稽查不得破坏只读边界。

## 四、单问题严格串行

- 任何时刻只处理用户当前明确反馈的一条问题。
- 当前问题未完成“复现 → 根因定位 → 框架级修复 → 真实验证 → 关联回归 → 证据记录”前，禁止处理下一条。
- 用户同时反馈多条时，只登记后续条目；不得提前分析、修改或测试。
- 当前问题完成后停止并汇报，等待用户明确确认或给出下一条；禁止自动续跑。
- 存在阻塞时只报告当前阻塞，不得借此跳到下一条。

## 五、框架级修复

- 先取得稳定复现和失败证据，再定位首个错误事实；禁止根据表面现象猜测修改。
- 根因必须落到公共机制：数据模型、状态机、任务生命周期、并发、持久化、缓存、资源调度、错误传播或前后端契约。
- 修复必须覆盖共享调用链和同类入口；禁止临时补丁、硬编码特例、静默降级、绕过校验、隐藏错误或只修当前样例。
- 异步任务必须具备唯一身份、项目/会话隔离、幂等或去重、超时、取消、晚到响应隔离、资源释放和可恢复终态。
- 持久化必须有唯一事实源、原子提交边界和版本/代际校验；旧任务、旧响应、旧文件和旧缓存不得污染当前数据。
- 错误必须返回真实、可定位的失败信息；禁止吞错、伪成功和无限等待。
- 改变公共契约时，必须同步调用方、迁移逻辑、测试和文档。

## 六、完成门禁

以下条件全部满足，才可报告当前问题已解决：

1. 原问题已稳定复现并保存失败证据。
2. 根因和受影响范围已确认。
3. 已完成框架级修复，无特判和绕过。
4. 已执行与生产路径一致的真实测试，不能仅靠 mock 或静态检查。
5. 已覆盖适用的正常、重复、并发、中断、超时、失败、恢复、项目切换和服务重启场景。
6. 关联功能、上下游接口、状态、持久化和界面回归通过。
7. 已确认无卡死、资源泄漏、重复提交、旧数据污染和跨项目串线。
8. 软件测试通过后，代码稽查通过；存在 BUG 编号时完成同编号闭环。
9. 根因、修改范围、测试命令、实际结果和遗留风险已写入规定记录。

任一项缺失，只能报告“处理中”或明确阻塞，不得使用“已解决、已修复、已完成、可用”。

## 七、协作流程

固定顺序：

`主线开发修复并自检 → 软件测试独立验证 → 代码稽查独立验收 → 关闭 BUG`

- 测试失败或稽查失败均退回主线开发；修复后必须重新经过软件测试。
- 禁止跳过测试直接稽查，禁止开发身份替代独立测试或稽查结论。
- 各身份的范围、报告格式和只读要求分别以其职责文档为准。
- 测试与稽查的自动流转属于当前问题闭环，不视为进入下一条问题。

## 八、用户授权边界

- “检查、查看、诊断”：仅授权只读核验，不授权修改、删除、下载、重启或接入。
- “下载”：仅授权下载和完整性校验，不授权接入、启用、修改工作流或重启。
- “接入、启用、修复、实现”：授权完成当前问题所需修改和验证，不授权处理下一条问题。
- 删除、清理、迁移、替换模型等可能造成不可逆影响的操作，必须先核验精确目标和恢复方式；用户未明确授权时不得执行。

## 九、运行资源约束

- 重负载任务启动前必须检测物理内存和实时可用内存，按实际平台动态计算预算，禁止写死单机配置。
- 系统保留量取“8GB、物理内存25%、当前非 AI 工作集加4GB”的最大值，禁止重复扣减。
- 当前128GB统一内存Mac上，AI总占用不得超过90GB，并始终保留至少35GB。
- 同一时刻只运行一个重负载模型、绘图或视频任务；超预算时串行拆批，不得硬跑。
- 每个重负载任务结束后立即卸载模型并释放权重、缓存和临时资源。

## 十、对用户回复

- 阶段回复和最终回复只陈述：已完成结果、当前状态或明确阻塞。
- 不向用户展开内部推理和冗长过程；根因、测试命令和证据必须完整写入项目记录。
- 结论必须与完成门禁一致，不得提前承诺。
