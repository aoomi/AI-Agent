# AI 项目目录说明
**定位**：可插拔基座平台，支持私有化部署 + SaaS 多租户；V1 业务：短剧流水线；底座与业务插件严格分离。

## 快速决策表

| 我想做什么 | 应该放在哪 |
| :--- | :--- |
| 新增业务功能（短剧/法律/电商） | `plugins/builtin/{plugin_name}/` |
| 客户自己写插件 | `plugins/custom/{customer_name}/` |
| 修改底座能力（路由/鉴权/队列） | `platform/` 对应子目录 |
| 改前端平台 UI | `frontend/src/core-ui/` |
| 新增前端插件 UI | `frontend/src/plugin-slots/slots/`，组件通过 `registry.ts` 注册 |
| 写通用工具函数 | `platform/common/` |
| 全局Pydantic数据模型/Schema | `platform/schema/` |
| 新增数据库表/字段 | `scripts/migration/` |
| 添加新模型 | `models/registry.json` + `models/{model_type}/` |
| 做数据备份/恢复 | `scripts/maintenance/` |
| 项目记忆、沟通基线文档 | `docs/memory/` |

## 目录详解

### 🔒 `.kernel/` —— 内核只读目录，V1仅目录占位，禁止业务修改
- 存放平台底层内核原始逻辑，V1不实现，后续架构升级迁移至此
- ❌ 业务插件、开发者**严禁修改此目录下任何文件**
- ❌ 禁止在此目录新增任何文件

### 🧱 `platform/` —— 平台通用底座，与业务无关
> 所有 SaaS/私有化共用底层能力，业务全部通过插件实现，不改动 platform 内部代码。

| 子目录 | 职责 |
| :--- | :--- |
| `common/` | 通用工具库：加密、时间、校验器、常量、类型定义 |
| `schema/` | 全局Pydantic数据模型、DTO、数据校验schema |
| `bootstrap/` | 程序启动引导、生命周期、健康检查 |
| `core/` | 核心配置、全局异常、主入口 `main.py` |
| `adapters/` | 外部系统适配层：LangGraph / Dify / ComfyUI |
| `llm_gateway/` | 大模型网关、路由、Token 优化、模型注册表 |
| `security/` | 密钥库、加密、输入过滤、授权 License 校验 |
| `tenant/` | 多租户隔离逻辑，V1仅基础接口占位，完整SaaS多租户V2实现 |
| `events/` | 全局事件总线，插件之间解耦通信 |
| `discovery/` | 插件自动发现加载（V1 占位） |
| `circuit_breaker/` | 熔断保护（V1 占位） |
| `queue/` | 异步任务队列 Celery，流水线后台任务 |

❌ **禁止写入任何业务代码（短剧/法律/电商等）**

### 🔌 `plugins/` —— 可插拔业务插件目录
> 所有业务功能全部放此处，V1 只启用 `short_drama` 短剧插件；后续新增业务直接新增插件，不动底座。

| 子目录 | 说明 |
| :--- | :--- |
| `builtin/` | 官方内置插件 |
| `builtin/short_drama/` | 短剧流水线 V1 插件 |
| `builtin/short_drama/skills/` | 业务能力脚本：写剧本、分镜、定妆、审核、合片、导出 |
| `builtin/short_drama/assets/loras/` | 插件专属 LoRA |
| `builtin/short_drama/assets/images/` | 插件静态素材 |
| `builtin/short_drama/workflows/` | 流水线 YAML 定义（11 节点标准流程） |
| `builtin/short_drama/templates/prompts/` | AI 提示词 Jinja 模板 |
| `builtin/short_drama/manifests/` | 插件能力清单、配置声明文件 |
| `custom/` | 客户自定义第三方插件 |
| `manifest.schema.yaml` | 插件清单 YAML 校验 Schema |

❌ **插件之间禁止直接import调用，统一通过platform/events事件总线通信**
❌ **custom 插件禁止直接 import `io` / `subprocess`**
❌ **插件内部不允许新建独立数据库；数据表变更统一走 `scripts/migration/`**

### 🎨 `frontend/` —— Vue3 前端

| 子目录 | 职责 |
| :--- | :--- |
| `src/components/` | 通用业务组件 |
| `src/core-ui/` | 平台全局基础 UI 组件 |
| `src/plugin-slots/` | 插件插槽系统，插件在此挂载导航/侧边栏/画布/设置入口 |
| `src/views/` | 页面视图：仪表盘、短剧流水线、插件市场、系统设置 |
| `src/composables/` | Vue 组合式工具 |
| `src/assets/` | 静态资源：图标、样式 |
| `src/utils/` | 前端工具函数 |
| `public/` | 静态公开资源 |
| `tests/` | 前端单元测试 |

❌ **插件禁止直接 import `@/core-ui/**`，只能通过 `plugin-slots` 插槽调用**

### 🚢 `deploy/` —— 部署配置

| 子目录 | 职责 |
| :--- | :--- |
| `environments/` | 多环境配置：development / staging / production / customer_site |
| `docker/` | docker-compose、Dockerfile 打包脚本 |

### 📚 `docs/` —— 全套文档

| 子目录 | 职责 |
| :--- | :--- |
| `product/` | PRD、产品决策、路线图、愿景 |
| `technical/` | 架构、API 契约、数据库表、部署文档 |
| `risks/` | 风险清单、规避方案、合规文档 |
| `contracts/` | 插件前后端 Schema、License 协议模板 |
| `operations/` | 运维手册：安装、备份恢复、授权、故障排查 FAQ |
| `api/` | 接口文档 |
| `dev-guide/` | 开发者接入指南、AI编码命名规范 |
| `memory/` | 项目记忆.md、通用强制执行沟通基线.md |

### 🧪 `tests/` —— 自动化测试

| 子目录 | 职责 |
| :--- | :--- |
| `unit/` | 单元测试 |
| `integration/` | 集成测试：事件总线、插件加载、工作流 |
| `e2e/` | 端到端完整流水线测试 |
| `plugin-fixtures/` | 插件测试 Mock 样例 |
| `security/` | 安全沙箱、授权 License 安全测试 |

> 业务插件新增能力，必须同步补充对应单元/集成测试用例

### ⚙️ `scripts/` —— 运维/迁移脚本

| 子目录 | 职责 |
| :--- | :--- |
| `deploy/` | 初始化数据库、导入演示数据 |
| `license/` | 生成授权 License 文件 |
| `migration/` | 版本数据库迁移脚本 v1 → v2 |
| `maintenance/` | 重置密码、清理缓存、导出全部数据、修复数据库 |
| `security/` | 密钥轮换、审计日志导出 |

### 💾 `data/` —— 运行时数据（Git 忽略）
> 所有业务产生的数据全部在此，打包私有化部署只需要拷贝 `data/` 目录。

| 子目录 | 职责 |
| :--- | :--- |
| `hot/` | 高频读写数据：项目工程文件、缓存 |
| `cold/` | 冷归档：历史导出包、归档项目 |
| `tmp/` | 临时文件，**程序启动自动清理旧残留**：上传、渲染中间产物 |
| `system/` | 系统数据：License 授权、密钥、各类日志；密钥凭证需加密保护，禁止提交git |

❌ **禁止手动修改 `data/` 下的文件，必须通过平台 API**

### 🤖 `models/` —— 模型权重（Git 忽略）

| 子目录 | 职责 |
| :--- | :--- |
| `llm/` | 大模型权重 |
| `comfyui/` | Checkpoint、VAE |
| `loras/` | 全局公共 LoRA（插件私有 LoRA 放插件 `assets/loras/`） |
| `cache/` | 模型缓存 |
| `registry.json` | 全部模型注册清单 |

### 🔧 `tools/` —— 调试工具，非业务代码

| 子目录 | 职责 |
| :--- | :--- |
| `diagnostic/` | 诊断工具 |
| `recovery/` | 故障恢复工具 |
| `migration/` | 迁移辅助工具 |

### 根目录文件

| 文件 | 职责 |
| :--- | :--- |
| `README.md` | 项目快速上手 |
| `Makefile` | 开发常用命令集合 |
| `.gitignore` | Git 忽略规则 |
| `.env.example` | 环境变量模板 |
| `.eslintrc.js` | 前端 ESLint 校验，限制插件非法导入 `core-ui` |

## V1版本边界说明
1. `.kernel/`、`platform/discovery`、`platform/circuit_breaker` 仅创建目录占位，V1不实现完整逻辑。
2. `platform/tenant`模块V1做基础接口占位，**V1优先支持单租户私有化部署；SaaS多租户完整能力放到V2迭代**。

## 🤖 AI执行强制约束（AI修改代码必须阅读本条）
1. 新增文件前先查阅上方快速决策表，严格选择对应目录，禁止随意新建目录。
2. 业务逻辑绝不写入 `.kernel/`、`platform/`，业务全部下沉到 `plugins/builtin/*`。
3. 禁止硬编码本地绝对路径；全部读取环境变量/配置获取根路径。
4. 所有动态生成业务文件输出路径只能在 `data/hot/`、`data/cold/`、`data/tmp/`，不能输出到plugins、platform。
5. 修改前优先阅读 `docs/dev-guide/AI执行命名编码规范.md` 命名规范。
6. 新增配置优先写 `.env.example`，禁止直接写死配置在代码内部。
7. 禁止在custom插件中直接导入subprocess、io高危模块；custom插件能力受manifest声明白名单管控。
你看还有什么优化的吗?
